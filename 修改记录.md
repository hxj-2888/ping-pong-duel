# 乒乓对决 · 修改记录与项目状态

> 本文档记录历次修改的原因、内容与验证方式，供下次修改前阅读。
> 上次更新：2026-08-09

## 2026-08-13 · v1.7.0：移动时自机抽动修复（预测纠偏平滑化）

### 根因
- net.js 本地预测纠偏：服务器与预测偏差 >0.6m 时硬切 pred=服务器。移动网络下预测领先 RTT×速度 可达 0.6m+（跑步时）→ 阈值反复触发 → 预测周期性硬拉回 → 移动时反复瞬移/抽动，相机跟随 pred 一起抖
- 仿真证实：200ms RTT 下 0.6m 硬切产生 0.563m 瞬移跳变；平滑纠偏保持正常帧位移

### 修改（net.js）
- 偏差 >1.0m：按 20%/快照**平滑收敛**位置（pred.x += err*0.2），保留输入驱动速度（动画连续），同步 padX
- 阈值 0.6 → 1.0m（容纳 RTT×速度领先，减少误触发）
- 严重失步（>3m，重连/传送）仍硬校准

### 验证
- 仿真：snap@200ms=0.563m 跳变 → blend=0.053m（正常帧位移）无振荡
- test:all 全绿；ECS（searchdelta.online）1.7.0-local，HTTPS 200 / wss 建房成功 / 线上 net.js 平滑纠偏确认

## 2026-08-13 · v1.6.9：自机抽动 + 非持球方持球渲染修复

### 根因
- **自机抽动**（双方各自自机抖、看对方正常）：applyLocalPrediction 挥拍时整体退回服务器插值，预测仍在推进 → 挥拍结束瞬间 me.x/z/vx/vz 硬切回预测（位置差可达 RTT×速度 ~0.6m、vx 可反向）→ 行走动画无速度阈值 → 腿部/身体抽动；net.js 0.6m 重置周期性放大。对方走纯插值路径正常
- **非持球方看到球在自己身前**：applyLocalPrediction 里 ballInHand 平移无条件执行，非持球方用自己球拍的预测位移去平移持球方的球

### 修改
- **applyLocalPrediction 重构**：位置/速度/蹲姿始终用本地预测（挥拍前后连续），挥拍时仅球拍保持服务器插值轨迹；ballInHand 平移加 snap.sv===side 门控
- **characters.js 动画速度死区**：speed<0.05 冻结步态/步幅/身体起伏，杀掉停止后残留微动
- **servePathFromSnap 门控**：非持球方不显示持球方的发球瞄准线/落点

### 验证
- test:all 全绿；ECS（searchdelta.online）1.6.9-local，HTTPS 200 / wss 建房成功 / 线上代码确认

## 2026-08-10 · v1.6.8：联机抽动根治——快照缓冲跨帧插值

### 根因
- 此前修复（时钟单向推进/封顶）仍有残留：插值时钟未建立滞后 → alpha 恒为 1 → 渲染只显示最新帧 → 低广播率（20Hz）人物按整帧步进 = 原地抽动（仿真证实 20Hz 下 0.1m/帧跳变）

### 修改
- **net.js 快照缓冲**：新增 snapBuf（最近 6 帧含引擎时间 t），单调门/重连重置基于缓冲；**interpLagged 滞后建立**——缓冲满 2 帧时强制把插值时钟锚定到（最新帧 - 1.5×实测间隔，25~80ms），此后按真实时间推进，只做落后 3 间隔的向前追赶
- **render.js 跨帧插值**：在缓冲内找跨插值时钟的相邻帧对，alpha ∈ [0,1] 纯插值（时钟超出缓冲钳到最近帧）→ 任意广播率（60Hz/20Hz）平滑
- 仿真验证：60Hz/20Hz、多种延迟与抖动下 0 跳变 0 振荡

### 验证
- test:all 全绿；ECS（searchdelta.online）1.6.8-local，HTTPS 200 / wss 建房成功 / 线上 net.js 含缓冲+滞后

## 2026-08-10 · v1.6.7：人物原地瞬移修复（v1.6.6 残留）

### 根因
- v1.6.6 移除了时钟回退重置，但显示时钟按墙钟推进恒超前最新快照一个单向延迟 → alpha 被永久钳制 1.2（1.2x 前向外推）→ 放大快照位置微小抖动（60Hz 阻尼收敛/AI 微调/保底广播重复快照）→ 人物原地反复瞬移

### 修改
- **render.js 显示时钟封顶**：`clock = Math.min(interpClock, snap.t)` → alpha 回到 [0,1] 纯插值；窗口滑动时 sa=旧 snapB、alpha=0 位置连续，无回跳无抽搐；alpha 上限 1.2→1
- **net.js 追赶阈值收紧**：`interpGap*2 → *3`，避免保底广播间隔波动时反复触发向前跳

### 验证
- test:all 全绿；ECS（searchdelta.online）1.6.7-local，HTTPS 200 / wss 建房成功 / 线上 render.js 确认含时钟封顶

## 2026-08-10 · v1.6.6：联机画面回溯优化

### 根因
- **主因**：net.js 插值时钟 interpClock 按墙钟推进，快照 t 是服务器引擎时间（滞后单向延迟 D），`interpClock > snapB.t` 时被硬重置倒退一个广播间隔 → alpha 归 0 → 玩家/球拍/持球每收一条快照跳回上一帧位置（周期性画面回溯）
- **次因**：双槽窗口无单调保护（乱序/重连快照使 snapB.t 倒退 → 整帧回跳）；server.js 广播去重使静默相位断锚点

### 修改
- **net.js 时钟单向推进**：删除 `interpClock > snapB.t` 回退重置，只保留首帧锚定/向前追赶；超前部分走 renderOnline alpha>1 平滑前向外推（lerp 连续无跳变）
- **net.js 快照单调门**：丢弃同会话 t 倒退/重复快照；重连首帧/引擎重置（t 大幅变小）清空窗口重新锚定
- **server.js 保底广播**：去重改为「内容变化 或 距上次发送 ≥50ms」都发 → 静默相位 ≥20Hz 锚点
- 新增 test/broadcast-floor-test.js（静默期快照频率 ≥18Hz 断言）接入 test:all

### 验证
- test:all 全绿；保底广播实测静默期 39.2Hz
- ECS（searchdelta.online）已更新：1.6.6-local，HTTPS 200 / wss 建房成功

## 一、当前项目结构

```
乒乓对决/
├── server.js              # 本地联机 WebSocket 服务器 + 静态文件（零依赖）
├── desktop-launcher.js    # 桌面启动器（VBS → node，隐藏窗口启动服务器 + Edge 应用窗口）
├── 启动乒乓对决.vbs        # 快捷方式调用入口
├── wrangler.toml          # Cloudflare Pages 配置（高级模式 _worker.js + 绑定 game-room DO）
├── wrangler.room.toml     # game-room Worker 配置（Durable Object 宿主）
├── public/                # 前端
│   ├── index.html         # 主菜单 / 游戏界面
│   ├── _worker.js         # Pages 高级模式入口：静态资源 + WebSocket 转发到 DO
│   ├── css/style.css
│   └── js/
│       ├── engine.js      # 物理引擎（重力/空气阻力/马格努斯旋转/规则/计分）
│       ├── render.js      # 3D 透视渲染（球台/球网/球/场景 + 向量工具）
│       ├── characters.js  # 火柴人物理骨架（2-Bone IK/惯性/挥拍动量）
│       ├── ai.js          # 人机对手（落点预测/追球/推扣决策/四档难度）
│       ├── network.js     # 联机 WebSocket 客户端
│       ├── audio.js       # WebAudio 音效
│       ├── main.js        # 游戏循环 / 输入 / HUD / 模式分发
│       └── app/           # 按功能拆分的界面模块（state/net/modes/loop/…）
├── src/                   # 公网联机后端（Cloudflare Workers）
│   ├── index.js           # game-room Worker 入口（转发 WS 到 DO）
│   ├── room.js            # GameRoom Durable Object（Hibernation API + storage 持久化）
│   ├── room-core.js       # 房间核心逻辑（纯逻辑，Node 可测）
│   └── engine.js + engine/# 引擎（ESM 版，供 DO 侧模拟）
├── test/                  # 自动化测试（net/engine/ai/browser-smoke/do-smoke/do-persist/ws-live/ws-evict）
├── tools/                 # 开发者工具（preview、shot.js 截图）
└── 修改记录.md             # 本文件
```

## 2026-08-09 · v1.6.5：中画质档位 + 渲染帧率修复

### 1. 新增中画质档位（高/中/低三档）
- 中画质：电脑**高度 1920p 封顶**（16:9 下 ≈3413×1920，2K+ 级）/ 手机宽 750p；低画质不变（电脑 1080p / 手机 400p）；高画质不封顶
- `state.js`：resize() 三档 dpr 计算 / setQuality() 三值 / localStorage 记忆；`index.html` 下拉新增「中画质（平衡）」；`main.js` 切换状态文案
- 中画质保留观众席/尾影（low=false），低画质仍恒关置灰

### 2. 修复：分屏离屏缓存 dpr 2× bug（渲染帧率）
- **现象**：本地双人分屏 vw=半宽，离屏缓存 dpr 按 canvas.width/vw 算成 2× 全局 dpr → 半视口缓存按**整屏像素**烘焙，4× 冗余像素与内存（4K 下约 300MB），每帧 blit 4× 像素
- **修改**：`render.js` 三处（rebuildCrowdCache / rebuildAnimCache / low 分支）改按**整屏 CSS 宽**（canvas.clientWidth）计算缓存 dpr
- **验证**：crowd-anim-cache-test 13 项全过（测试桩无 clientWidth 时回退旧算法，行为不变）

### 3. HUD 每帧 DOM 写入缓存（hud.js）
- 球高（lastBallH）/ 联机状态栏（lastNetInfo）/ 发球点透明度（dotOpacity 恒 1 只写一次）：值变化才写 DOM

### 4. 版本 v1.6.4 → v1.6.5
- package.json / state.js / AndroidManifest（versionCode 9→10）/ build.cmd / 下载页 / 游戏页 / _headers / 使用说明
- APK 文件名 -v164 → **-v165**（新 URL 破缓存），重建 APK 分发全副本，Cloudflare DO + Pages 重新部署

## 2026-08-09 · v1.6.4：低端机帧率优化（消除多余物理计算）

### 1. AI 落点预测 predictCrossing 重写（ai.js）
- **现象**：每次预测从当前球状态**重头模拟 t 秒**（predictBall 内部克隆整球重放），70 个采样点 → 单次调用约 1.2 万子步（1/240s）；60Hz 调用下人机 ≈71.5 万子步/秒、AI 观战双 AI ≈143 万子步/秒，低端机掉帧主因
- **修改**：改为**单次前向模拟**——克隆一次，physicsStep 连续推进（内部按 SUBSTEP=1/240 细分，与 predictBall 同一物理实现），采样点检测 z 跨越立即返回；签名/返回值不变
- **收益**：单次 0.573ms → 0.0216ms（≈26×）；AI 观战每秒 AI 控制 62.8ms → 2.8ms（省 ~60ms/s ≈ 94%）；`tools/sim-fps.js` 基准可复测
- **验证**：ai-test 22 项全过；sim-match 宏观均衡不变（地狱vs困难 11.3:6.5、困难vs中等 11.2:7.2）；输出偏差 t 0.1ms / x 0.01mm / y 2mm，远小于 AI 判定阈值；对称对局单局结果有混沌分叉（20 局 12:8，无系统性偏差）

### 2. HUD 可扣杀/可高吊指示求解去重（hud.js）
- 节流 120ms→300ms；距离门限 2.6m→1.5m（球尚远时无需求解）；新增求解结果指纹缓存（球 pos/vel 量化 key + 300ms 复用），避免对同一球况重复全量弹道搜索
- 仅影响指示刷新频率与触发距离，不影响引擎实时判定

### 3. 版本 v1.6.3 → v1.6.4
- package.json / state.js / AndroidManifest（versionCode 8→9）/ build.cmd / 下载页 / 游戏页 / _headers / 使用说明
- APK 文件名 -v163 → **-v164**（新 URL 破缓存），重建 APK 分发全副本（android 资产/安装包/桌面副本/ECS 包），Cloudflare DO + Pages 重新部署

## 2026-08-08 · v1.5.2：敏捷惩罚 + 防扣显示对齐（AI 数值调控）

### 1. 敏捷倍率 <1 加惩罚（AI 观战/人机暂停面板滑杆）
- **现象**：拉低敏捷滑杆只有横向占空比按比例下降，前后(z)移动不受影响（形同虚设），且困难/地狱显示仍为"移动 100%"（bug）
- **修改**（ai.js）：
  - 新增 `agiUnder = max(0, 1 - agilityMul)`；惩罚占空比折扣 `moveDuty = clamp(agility × (1 - agiUnder×0.5), 0, 1)`（mul=0.5 时再打 75 折）
  - **前后(z)移动纳入占空比门控**（仅惩罚时生效，基准/溢出难度前后响应不变）
  - 追球死区放大：`moveDead = 0.045 × (1 + agiUnder×1.5)`
- **显示**（input.js）：修"困难/地狱拉低滑杆仍显示移动 100%"的 bug（改按实际值判断），与 ai.js 同公式，mul<1 追加"（减速惩罚）"
- **验证**：ai-test 全部通过（含移动/难度参数断言）

### 2. 防扣显示与实际体验对齐（接球滑杆）
- **现象**：显示"防扣 95%"（裸概率），实际有效反击率地狱 ~80%、困难 ~50%（位置门±0.35m + 高度/时序损耗，README 实测）；人机地狱接球率仍按废弃的 0.97 预估，显示"97%（每33球漏1）"但实际"永不漏球"
- **修改**（input.js）：
  - 防扣显示改为**实测有效反击率**：裸概率 × 每档实测定标系数 `DEF_EFF = { 困难: 0.91, 地狱: 0.84 }` → 地狱 ×1 显示"防扣约 80%"、困难 ×1 显示"防扣约 50%"
  - hellMul `0.97 → 1.0`（与 loop.js 实际传的 hellCatchMul:1 对齐）：人机地狱显示"永不漏球"
- **机制不动**：位置门/高度/时序与 50%/80% 定标是刻意设计，只改显示口径
- **验证**：smash-counter-test / ai-test 通过（smashDef 0.55/0.95 断言未变）

### 3. 版本 v1.5.1 → v1.5.2（手机可验证）
- package.json / state.js / AndroidManifest（versionCode 3→4）/ build.cmd / 下载页 / 游戏页 / _headers
- APK 文件名 -v151 → **-v152**（新 URL 破缓存），重建 APK 分发全副本

## 2026-08-08 · 工程修复与分发同步（v1.5.0）

### 1. 修复：非法 URL 打崩服务器（重要）
- **现象**：请求 `/%zz` 这类含非法百分号编码的 URL 时，`decodeURIComponent` 抛 URIError 且未被捕获 → Node 进程直接崩溃（局域网/公网部署均暴露）
- **修改**：`server.js` 静态请求的 URL 解析包 try/catch，解析失败返回 400 Bad Request
- **验证**：新增 `test/http-robustness-test.js`——畸形编码（`/%zz`、`/%`）返回 4xx；原始路径穿越（`/../server.js`）与编码穿越（`/%2e%2e/server.js`）被 403 拒绝；畸形请求后 `/api/info` 仍 200 证明服务器存活。已接入 `test:all`

### 2. 移除死代码
- `server.js` 删除从未被调用的 `snapshotToClient`（主循环与 leaveRoom 均为内联广播）

### 3. 新增引擎镜像一致性校验
- 新增 `tools/check-engine-sync.js`：逐字节对比 `public/js/engine.js` + `public/js/engine/` 与 `src/engine.js` + `src/engine/`（7 对文件），不一致 exit 1
- `package.json` 新增 `test:sync` 脚本，并接入 `test:all` 链尾（防两侧引擎漂移导致联机协议不一致）

### 4. 全量分发同步
- 电脑安装包（dist/installer）、ECS 部署包（dist/ecs）、%PKG% 中间目录同步到当前工作区；安装包文案 v1.4.3 → v1.5.0
- 手机 APK：public 两份 APK 已是 v1.5.0 最新构建，同步到各分发副本
- portfolio 内嵌游戏副本（projects/ping-pong-duel）同步并补全缺失文件（manifest/图标/APK/download.html）

## 2026-08-07 · v1.5.0 修改记录（全版本同步）

### 1. 手机端触控改版
- 移除「扣」按钮，改为**上滑扣球**：对打阶段抬手判定，向上 ≥45px 且 ≤350ms = 扣球，否则单击 = 推球；发球点两下、鼠标左推右扣不变
- 蹲下按钮移到右侧；分屏双人 P1/P2 各一套（摇杆 + 蹲）
- 触屏推球由"按下即发"改为"抬手判定"（input.js canvas pointerup，按 pointerId 记录，支持多点触控）
- 提示栏 / 说明书 / README / 使用说明同步更新

### 2. 说明书改版（平台自适应 + 胶囊化，字数不删减）
- 说明书改为可展开胶囊（每小节一个药丸条目，CSS `.manual-capsule`）
- 按平台分流（main.js 启动时按 isTouch 过滤）：手机端只显示手机内容（手机操作 / 手机安装PWA / 本地联机 / 个人生涯 / 模式 / 规则 / 提示），电脑端只显示电脑内容（电脑操作 / 模式 / 规则 / 提示）

### 3. 移除遗留电脑合成音乐
- audio.js 删除合成器（musicTick/musicNote/musicKick/musicSnare/musicHat/音序数据）与回退分支；背景音乐仅用真实音频 audio/music.mp4（音效/掌声保留）
- sw.js 缓存版本 ppd-v1 → ppd-v2

### 4. 手机端个人生涯 + 本地联机
- records.js：手机版（file://）**本地优先**存储（localStorage `ppd_career`，封顶 500 条），填服务器地址后异步尽力同步；桌面端同源 /api/records 不变；**网页版禁用限制保持不变**
- server.js /api/records 已具备 CORS（`*` + OPTIONS），手机可跨源同步战绩
- MainActivity.java 补 `setMixedContentMode(ALWAYS_ALLOW)`（file:// 跨源 http 放行）

### 5. 分屏背景一致 + 右半屏空白修复（承上）
- 分屏两端观众队色一致（红左蓝右，teamFixed）；修复分屏右半离屏缓存坐标错位（drawFloor vx 平移 + blit 到视口 + drawPerson 剔除本地坐标），右侧不再空白

### 6. 版本号统一 v1.5.0
- package.json / AndroidManifest（versionCode 2）/ 设置面板版本号显示 / 文档同步

### 7. 玩法优化（操作奖励 + AI 平衡 + 虚线面板）
- **反击扣杀奖励**：玩家成功反击 AI 扣杀（预判起拍+推球接回）时，该回球**视为扣杀**、人机应对概率**减半**（strokes.js 标记 `counterSmash`，仅人类击球、回球非扣杀、来球是扣杀时置位；ai.js 对带标记回球按 `smashDef×0.5` 应对；AI 玩家在 control() 中标记 `isAI` 以免 AI 观战误触发）
- **低平快球反击奖励（更高档）**：蹲下+推球成功接回低平快球（hitType 3）时，回球同样 `counterSmash`（视为扣杀、应对概率减半），并叠加**更高球速 + 刁钻边角落位**（shots.js `fastCounter`：推球 padSpeed 2.8→5.2、目标 x 向对手反方向边角偏 0.55m、带强上旋快抽；strokes.js 击球前设 `p.counterLowBonus`、求解后清除；解不出时回退普通推球保证不挥空）
- **反击免延迟窗口**：扣杀/低平快球来球（hitType 2/3）均从 t=0 起判箱（免 0.08s 起拍延迟，命中窗 [0, 0.28]s），可反应式按键反击；普通来球保留起拍延迟
- **地狱完全不再漏球**：人机对战地狱 `hellCatchMul 0.97 → 1`（与观战一致 0% 刻意漏球，高手仍可战胜）
- **虚线面板蹲下说明移入玩法说明**：左上角判定面板去掉「蹲下最低接球」行，蹲下贴地 0.02m 说明并入说明书胶囊（电脑操作/手机操作）
- **AI 观战虚线说明**：实时计算球高/进箱（aivai 也刷新），**不做**可扣杀/可高吊指示（观战隐藏这两行，与玩家操作无关）
- 引擎/说明文档同步（README / 使用说明 / 说明书胶囊）

## 二、2026-08-03 修改记录

### 1. 修复：首页被「房间已创建」面板遮挡（重要）
- **现象**：打开游戏看不到主菜单（人机对战按钮），只看到「房间已创建 / ---- / 等待对手加入…」
- **根因**：`css/style.css` 中 `.panel` 默认 `display:flex`，而 `#roomPanel` 的 `z-index`（20）高于菜单（10），页面加载时房间面板直接盖住菜单
- **修改**：`public/index.html` 给 `#roomPanel` 增加 `style="display:none"`（与 gameScreen/overlay 一致），只有点「创建联机房间」才弹出
- **验证**：浏览器加载后 `menu=flex / roomPanel=none`

### 2. 修复：球拍不可见（两边都看不到）（重要）
- **现象**：进入游戏后看不到自己和对手的球拍
- **根因一（NaN）**：`characters.js` 中 `anim.armSmooth = lerp(anim.armSmooth, wristR, t)` 把**标量** `lerp(a,b,t)=a+(b-a)*t` 用在**向量对象**上，产生 NaN/字符串，导致手臂与球拍投影失败（Canvas 静默忽略 NaN，因此无报错）
  - 修改为向量插值：`vadd(anim.armSmooth, vscale(vsub(wristR, anim.armSmooth), k))`
- **根因二（朝向/光照）**：球拍用 3D box 绘制，背面朝向镜头且光照低至 0.35 倍（近黑色），正面背对镜头被面剔除 → 看起来像没有拍
  - 修改为 **billboard 绘制**：球拍始终面向镜头，亮色拍面（红/蓝）+ 手柄，投影尺寸随距离缩放
- **验证**：像素检测红色球拍约 1700px 出现在正确位置

### 3. 优化：火柴人形象（去支点、无隔断）
- 移除所有关节/支点圆点（髋/膝/脚/肩/肘/腕的 `joint()` 圆点）
- 身体改为连续圆头骨线（`limb` 已带 round cap），关节处平滑衔接
- 保留头部与视线点

### 4. 优化：隐藏己方火柴人（避免遮挡视线）
- 人机对战（`main.js renderSingle`）与联机（`renderOnline`）设置 `view.hideOwn = true`
- 己方身体/头部隐藏，**球拍保留**显示，方便判断击球位置
- 本地双人分屏模式未改动（双方均可见）

### 5. 启动器优化
- `desktop-launcher.js` 增加 `--disable-http-cache`，确保每次打开都是最新页面

### 6. 优化：挥拍动作缓动（对应《火柴人动作设计方案》优化项 1）
- 对打挥拍（推球/扣球）的拍面路径由**线性插值**改为 **easeOutQuad 缓动**：前段加速、出球瞬间速度最快、收拍自然减速
  - `engine.js updateStroke`：位置用 `easeOutQuad(t)`，拍面瞬时速度用导数 `2(1-t)`（碰撞与力度计算同步更新）
  - 发球挥拍保持匀速不变（接触时刻按匀速求根，不能改）
- `characters.js` 的躯干前倾/侧倾与左手平衡臂同步使用缓动进度，视觉与引擎一致
- 验证：`physics-boundary-test` 断言挥拍 50% 时间点位移占比约 71%（线性=50%）、后段速度低于中段

### 7. 优化：拍面反弹方向融合挥拍速度矢量（对应《火柴人动作设计方案》优化项 3）
- `engine.js applyPaddleHit` 兜底路径的反弹方向不再固定为 `st.n`：按挥拍瞬时速度占比（最高 35%）与出球方向混合
- 挥速越快，出球方向越贴近挥拍方向；正常求解路径仍以物理求解器验证的方向为准

### 8. 新增：物理边界单元测试（对应《乒乓球物理效果》优化项 4）
- 新增 `test/physics-boundary-test.js`，覆盖：
  - 台面反弹边界：台内反弹贴台 / 台外 1cm 不弹台落地面 / 擦边（|x|=半宽）判触台
  - 球网边界：擦网顶（netclip 过网）/ 撞网（net 拦下）
  - 过网弧线：发球先本方后对方、网中心高度 > 网顶+1cm、最高点合理；回球第一落点对方半台且有上升弧线
  - 回球求解边界：对方半台有解 / 侧线外无解 / 低速度瞄本方半台无解
  - 马格努斯旋转：上旋 70 vs 无旋，飞行 1s 落点显著不同
  - 计分边界：未落台出界判对方得分 / 漏接判击球方得分 / 同半台连弹两次判失分
  - 挥拍缓动曲线（见第 6 条）
- `package.json test:all` 与 `README.md` 已加入该测试

### 对照两份方案文档的现状确认（已实现，无需再改）
- 《乒乓球物理效果》优化项 1：抛物线弧线由 `solveServe`/`solveRally` + 真实物理（重力/阻力/旋转）求解验证，过网高度已有测试覆盖
- 优化项 2：AI 已用 `predictCrossing`（含速度向量、反弹、旋转的 20ms 步进预测）而非简单跟随
- 优化项 3：马格努斯旋转已在引擎实现（`K_MAG`）并有测试覆盖
- 《火柴人动作设计方案》优化项 2：对手 AI 已按落点预测自动移动并触发挥拍
- 优化项 4：计分逻辑已实现（出界/漏接/双弹/拦击/发球失误）并有测试覆盖

### 9. 修复：红方按键左右反向（重要）
- **现象**：人机/本地双人中按左（A/←）角色或持球反而向右移动
- **根因**：红方（side 0）相机位于自己身后向对方半场看，透视投影里世界 +x 落在屏幕左侧，而引擎把「右」固定映射为 +x，导致红方按键方向与屏幕方向相反（蓝方视角无此问题，经投影数值验证）
- **修改**：`main.js` 新增 `applyViewMirror`，红方视口整体水平镜像；联机/人机/本地分屏的红方半屏均生效，只影响渲染不影响判定
- **验证**：投影数值验证 side0 世界 +x 经镜像后映射到屏幕右侧；全部自动化测试通过

### 10. 视角抬高至 45° 俯视 + 球阴影 + 台面撞击特效
- `main.js makeCam`：视线与台面水平线约 42°，台面完整入画（远缘约 155px、近缘约 649px/720px 视口）
- 相机改为**死区跟随**：角色在台面中部 ±0.62m 内相机不动，避免「按左，镜头反向带动画面右移」的反直觉感
- `render.js drawBall`：球在台面上方时阴影投在台面、否则投在地面；球越高阴影越大越淡
- `render.js drawEffects` + `main.js` 特效队列：球触台瞬间在落点生成扩散红圈 + 中心闪光（0.45s 衰减），本地/联机均生效

### 11. 重构火柴人形象 + 己方视角只显示上半身
- 形象改为**加粗圆头白线**（贴近参考图黑底白线风格）：骨线加粗、实心圆头、肘/腕/膝关节圆点，P1 红拍 / P2 蓝拍 + 深色手柄
- 己方视角不再整身隐藏：`view.hideLegs` 只隐藏腿脚，保留头部/躯干/手臂/球拍（本地双人、人机、联机均适用）
- 对照参考 HTML：红色拍面（#d0321e）、深色手柄、白色骨线实心关节，球自旋高光改为红色强调

### 12. 球网与台面边缘相切
- `engine.js RULES.NET_WIDTH` 由 ITTF 标准的 1.83m 改为 1.525m（与台宽一致），物理碰撞与渲染同步，网柱正好落在台面边缘
- 注意：此项按用户要求偏离 ITTF 标准（标准网宽 1.83m，两侧各超出 15.25cm）

### 13. 手机端触控按钮适配
- `index.html` 新增 `#touchControls`：左下 ◀▶ 移动、右下「推球 / 扣球」按钮
- 自动识别触屏设备显示（`pointer: coarse` / `ontouchstart` / `maxTouchPoints`），桌面端隐藏；`?touch=1` 可强制显示（调试用），`?auto=ai` 自动进入人机对战（截图/自动化用）
- `main.js`：按钮用 Pointer 事件绑定（down/up/cancel/leave），多指同时按互不干扰；映射到 P1 键组并接入联机 `syncKeys`
- `style.css`：68px 圆形大按钮（窄屏 ≤380px 缩小为 58px）、`flex-shrink:0` 防压缩、`touch-action:none` 防双击缩放、`env(safe-area-inset-bottom)` 适配刘海屏、提示栏上移至按钮上方避免重叠
- `viewport` meta 增加 `user-scalable=no, viewport-fit=cover`；触屏下提示文案改为「左侧移动 · 推球/扣球」
- 触控按钮控制己方角色：人机=红方、联机=自己、本地双人=红方 P1
- 验证：`browser-smoke` 新增触控按钮按下/抬起驱动移动与推球状态断言；计算样式验证推球黄/扣球粉、按钮 68px

### 14. 推球/扣球改为屏幕点击（单击推球、双击扣球）
- 删除游戏页面中的「推球 / 扣球」按钮（`index.html` 移除 `#btnPush`、`#btnSmash`，`style.css` 移除对应样式）
- `main.js` 新增画布点击监听：单击 → 推球，280ms 内再次点击同一侧 → 扣球；本地分屏按点击位置分侧（左半屏 P1、右半屏 P2），人机=红方、联机=自己
- 点击以短暂按键边沿注入引擎/联机输入（70ms 保持后复位），复用原有推/扣边沿触发逻辑；联机模式立即补发一次输入提升响应
- 键盘操作（W/S、↑/↓）保持不变；触屏移动按钮 ◀▶ 保留
- 验证：`browser-smoke` 改为「单击进入推球发球、双击进入扣球发球」断言，全部通过

### 15. 一局结束结算屏（渐变成黑屏 + 输赢标题 + 三个操作按钮）
- `index.html` 新增 `#gameOver` 结算屏：全屏黑底，顶部大字标题，下方三个竖排按钮「再来一局 / 返回主界面 / 退出游戏」
- `style.css`：`.game-over-screen` 黑色全屏，`opacity` 0.6s 渐入（`show` 类触发），标题 `clamp(42px~72px)` 白色大字带光晕，按钮竖排居中
- `main.js`：比赛结束事件（本地/人机引擎事件、联机快照事件）触发 `showGameOver`：人机/联机按胜负显示「您赢了 / 您输了」，本地双人显示「玩家1 获胜 / 玩家2 获胜」
- 三个按钮：再来一局 → 本地/人机直接 `resetMatch` 重置（人机同时重置 AI），联机发送 `rematch` 并处理服务器广播；返回主界面 → 复用 `backToMenu`；退出游戏 → `window.close()`（桌面应用窗口模式下关闭窗口并联动停服）
- 引擎比赛结束 8 秒自动重开时，HUD 阶段由 3 → 0 自动关闭结算屏，避免黑屏挡在新对局上
- 验证：`browser-smoke` 新增本地/人机/联机结算屏断言（标题文案、渐入类、再来一局重置、发送 rematch、返回菜单），全部通过

### 16. 增大推球/扣球判定范围 + 持拍手追踪球向
- `engine.js startRallyStroke`：击球判定球区半径由 0.62 增大到 **0.85**（推球/扣球统一），击球高度下限由 0.78 放宽到 0.72，击球窗口 windup/live 由 0.07/0.16 放宽到 0.08/0.20
- `characters.js`：待机且球朝自己运动时，持拍手沿球的运动方向最多伸出 0.10m（距离越近伸出越多，仅视觉，不影响判定；挥拍中不生效）
- 验证：`physics-boundary-test`「对打挥拍正常触发击球」及全部套件通过

### 17. 比赛页右上角：难度切换 / 暂停 / 退出
- `index.html` 新增 `#gameTools`（右上角）：人机模式显示「难度」循环按钮 + 暂停 + 退出；本地双人显示 暂停 + 退出；联机只显示 退出
- `main.js`：暂停冻结本地/人机模式的物理步进（`app.paused`），弹出暂停面板（继续游戏 / 退出比赛），Esc 键也可暂停/继续；难度切换立即生效；退出按钮返回主菜单；比赛结束自动取消暂停
- `style.css`：`@media (max-width: 720px)` 下 HUD 下移、netInfo 隐藏，右上角工具独占顶行避免与比分重叠
- 验证：`browser-smoke` 新增难度切换、暂停冻结物理、继续恢复、右上角退出回菜单断言

### 18. 联机测试抗抖动
- `net-test.js`「无回球时发球方得分」等待窗口由 3s+3s 放宽到 3.5s+5s，避免高负载下偶发超时误报

## 三、合并记录（2026-08-03 二合一）

> 本版本将桌面版（含尾影/发球轨迹/地狱难度/背景音乐等）与新 zip (3)(3)（含定位模式/前后移动/腿部步法/十字方向键等）合并为完整游戏。合并方向：**以桌面版为基础，操作方式采用新 zip 的 W/S 前后移动**。

### 19. 定位模式改为"球拍带动人"（引擎层）
- 玩家新增球拍定位坐标 `padX`：按键直接移动 `padX`，身体指数平滑跟随（滞后约 0.18m）
- 发球待机、对打/得分间隙（无挥拍时）球拍每帧回到 `padX` 准备位；挥拍中走挥拍路径不受干预
- `serveBallPos`、`strokes.js` 收拍复位改用 `padX`

### 20. W/S 键改为前后移动（z 轴）
- 引擎新增 `vz` 与输入 `f/b`；W=向前（近网）、S=向后（退台），范围 `Z_FWD=1.48 / Z_BACK=2.20`（半场对称）
- 键盘映射：`KeyW→P1F`、`KeyS→P1B`；P1 的推球/扣球改用鼠标单击/双击（P2 的 ↑/↓ 不变）
- 联机链路：客户端按键、位掩码、`setInput`、快照均携带 `f/b` 与 `z/vz`

### 21. 球员站位后移 + 画出腿部 + 乒乓球步法
- `PLAYER_Z 1.30→1.65`（球台半长 1.37，站其后方，腿不踏上球台）
- 腿部始终绘制（直腿：髋→脚一条线，无膝关节），移除各渲染函数的 `hideLegs`
- 步法随合速度推进：横向移动双脚开合、前后移动双脚进退，停止时收回中立站姿
- 髋与脚统一按世界坐标左右对应（修复己方双腿 X 型交叉）

### 22. 球台表面覆盖对面玩家腿部
- `drawScene` 绘制顺序调整：远侧（对手）玩家先画 → 球台/网/特效 → 近侧玩家+球按深度
- 桌面表面自然盖住对手腿部像素，上身仍显示在台面上方

### 23. 手机端十字方向键
- 触控区由左右两键改为 3 行十字：▲ 向前 / ◀ ▶ 左右 / ▼ 向后
- `index.html` 加 `btnFwd/btnBack`，`input.js` 绑定 `f/b`，`style.css` `.pad` 纵向排列

### 24. 接球手臂回摆修复
- 回摆力度 0.28→0.12、逐帧累加 1.0→0.05（有界衰减）；手臂必须保留前方分量（≥0.12），手腕不得越过肩部平面
- 接球靠身体移动到位，手臂不再绕到后背

### 25. 保留桌面版独有功能（本次合并不丢失）
- 对抗尾影、发球预测轨迹、地狱难度（4 档 + 解锁）、背景音乐（主菜单/比赛页右上角）、再来一局立即关闭、返回主页按钮、发球擦网判 LET、resetMatch 清事件、难度 0||1 修复

### 26. 测试同步
- `browser-smoke`：新增 ▲▼ 触控前后移动、W/S 前后移动断言；W/S 发球用例改为鼠标单击/双击发球
- `ai-test`：难度断言由 3 档更新为 4 档（含地狱）

## 三、部署方式

- **本地运行**：双击桌面「乒乓对决」快捷方式，或 `node server.js` 后访问 `http://127.0.0.1:8765`（本地联机走 ws://127.0.0.1:8765）
- **公网（完整版，含联机）**：https://ping-pong-duel.pages.dev
  - 架构：**Cloudflare Pages 高级模式（`_worker.js`）+ 独立 Durable Object Worker（game-room）**
  - 同域提供静态页面与 WebSocket：`wss://ping-pong-duel.pages.dev/ws`
  - 部署命令：
    ```bash
    npm run deploy:room    # 先部署 game-room DO Worker（wrangler deploy --config wrangler.room.toml）
    npm run deploy:pages   # 再部署 Pages（wrangler pages deploy public）
    # 或 npm run deploy:all 一次完成
    ```
  - 本地调试：`npm run dev:room`（独立测 DO）/ `npm run dev:pages`（Pages 高级模式整体）

## 四、公网联机方案（2026-08-04）

### 背景与弯路
- 最初的方案是独立 Worker（`pingpong-ws.hexiangjie694.workers.dev`）承载 WebSocket，**已删除**
- **踩坑：`*.workers.dev` 在国内网络被 DNS 污染**（解析到 Facebook IP 段 `face:b00c`），页面能开但 WS 连不上；`pages.dev` 正常
- 最终方案：联机端点挂到**游戏同域** `ping-pong-duel.pages.dev/ws`，客户端零硬编码域名

### 架构
```
浏览器 ──wss://ping-pong-duel.pages.dev/ws──► Pages _worker.js ──► game-room DO（idFromName('global')）
   全局 DO 内存维护 rooms Map + 引擎模拟；消息驱动 tick（替代 60Hz setInterval）
```
- **Pages 高级模式**：`public/_worker.js` 处理 WS 升级（转发 DO stub），其余请求回退 `env.ASSETS.fetch`
- **Pages 绑定 DO**：`wrangler.toml` 中 `[[durable_objects.bindings]]` 必须带 `script_name = "game-room"`，且 **Pages 配置不支持 migrations**（migrations 放 game-room Worker 的 `wrangler.room.toml`）；Pages 命令不支持 `--config` 自定义路径
- **WebSocket Hibernation API**：`ctx.acceptWebSocket` + `webSocketMessage/webSocketClose`，DO 空闲可休眠不产生时长费

### 关键修复
1. **DO 驱逐丢房间（重要）**：DO 约 30s 无消息即被驱逐，内存 rooms Map 全丢 → 房主等朋友超过 30s 就报"房间不存在"
   - 修复：房间持久化到 `ctx.storage`（SQLite，2h TTL）；`slots` 数组记录席位占用（不依赖 ws 引用）；驱逐恢复后旧连接按持久化的 attachment 重挂回席位；重挂时若对手已加入则补发 `room(wait:false)` 广播让房主进入对局
   - 验证：35s / 90s 空闲后加入均成功
2. **客户端房主 side 被覆盖**：加入方广播 `{side:1}` 也会发给房主，旧逻辑 `app.side = m.side` 把房主覆盖成 1
   - 修复：`sideSet` 标记——房主在创建响应（wait:true）即确立 side=0，后续广播不再覆盖；加入方以首条非等待消息确立 side
3. **等待期连接静默**：房主在房间面板期间不发任何消息，驱逐后无法重挂
   - 修复：客户端连接期间每 5s 心跳 `{t:'ping'}`（net.js），既保持 DO 活跃又触发重挂
4. **本地 server.js 的同类 side 问题**：与修复 2 同因同果，客户端一处修复两套服务器通用

### 客户端协议
- `public/js/app/state.js` WS_URL 分流：localhost → `ws://localhost:端口`（node server.js）；公网 → 同域 `wss://…/ws`
- 调试钩子（与 `?auto=ai` 同模式）：`?auto=host` 自动建房；`?auto=join&code=XXXX` 自动加入
- 协议与本地 server.js 完全兼容（create/join/in/rematch/ping/state/room/error/peer_left/pong）

### 验证
- 本地：`do-smoke` 15 项 / `do-persist` 15 项 / `net-test` 11 项 全过
- 公网：`ws-live` 11 项全过（create→join→in→state→发球→rematch→peer_left）
- 浏览器双窗口实测：建房 → 等 45s（经历驱逐）→ 加入 → 双方进入对局、side 正确、实时快照互流
- 免费额度说明：DO 用 Hibernation + 空闲休眠；一局约 2k–36k 计费请求，免费档 100k 请求/天够用

### 桌面版更新（2026-08-04）
- 桌面版（node server.js + localhost）新增「联机:本地/公网」切换按钮（仅 localhost 显示）：
  - 「联机:本地」= 原行为，走本地/局域网服务器（ws://localhost:8765）
  - 「联机:公网」= 直连 Cloudflare 联机端点（wss://ping-pong-duel.pages.dev/ws），公网好友可加入
- 实现：`state.js` 的 `WS_URL` 改为 `wsUrl()` 函数（连接时按 `app.publicServer` 选择端点）；`net.js` 连接时调用
- 调试钩子：`?net=public` 强制公网（与 `?auto=host/join` 同模式）
- 验证：本地版默认走 ws://127.0.0.1:8765 建房 ✓；`?net=public` 从 localhost 页面建房后，Node 从公网端点加入同一房间码成功 ✓（跨端互通）；网页版按钮隐藏、固定走同域 /ws ✓

### 联机版补齐发球预测轨迹（2026-08-04）
- **背景**：人机/本地双人模式发球时有预测轨迹（`renderSingle/renderLocal` 直接用引擎），联机模式客户端只有快照，轨迹缺失
- **实现**：
  - 服务端引擎（`src/engine.js`）快照新增 `sp` 字段：发球方案生成后（发球待发/挥拍期间）携带精确发球方案的 vel+spin（6 个数）
  - 客户端（`app/render.js`）新增 `servePathFromSnap(snap)`：有 `sp` 用精确方案，无 `sp`（未按发球键）用默认示意方案；物理采样与本地/人机完全一致（`physicsStep` 1.6s、落对方半台截断）
  - `renderOnline` 挂上 `view.servePath`，`drawScene` 已支持绘制
- **验证**：`test/serve-path-online.js` 13/13（快照 sp 生成/格式、精确与默认方案采样、落点、序列化传输）；公网 `test/ws-sp.js` 确认发球瞬间快照带 sp（`[-1.08,-0.80,4.40,-15,0,0]`）

### 观众席背景 + 得分欢呼 + 赛事风格音乐（2026-08-04）
- **需求**：①比赛场地两旁新增观众背景；②进球后观众欢呼，欢呼属于游戏音效；③背景音乐更贴合体育赛事紧张感
- **观众席（`public/js/render.js`）**
  - 新增 `crowdLayout()`：确定性伪随机生成**两端 + 两旁共四个方向**的观众席（端看台 z=±6.35 四排，两侧看台 x=±3.62/4.17/4.72 四排，面向球台）
  - 观众由"圆点"升级为**剪影小人**（身体椭圆 + 头 + 欢呼时举起挥舞的双臂，`drawCrowd()`）
  - 欢呼动画：`view.cheer`（0..1）驱动观众起跳浮动（`sin(time*12+相位)`）与双臂挥舞；得分时 `triggerCheer()` 置 1，主循环每帧衰减（约 1.8s 平息，与欢呼音效时长一致）
- **相机调整（`public/js/app/render.js makeCam`）**：机位由 (y4.6, z±4.2, 焦距 1.15vw) 改为 (y7.0, z±6.2, 焦距 0.8vw)、视线抬高到 (0,1.9,0) —— 两侧与远端观众席入画，形成"看台环绕赛场"的赛事转播纵深（球员/球会略变小，属预期取舍）
- **欢呼音效（`public/js/audio.js` 新增 `cheer()`）**：多层带通噪声模拟人群起哄 + 多人上滑"喔"声 + 掌声脉冲，持续约 1.8s；**走音效通道（受 🔊 静音开关控制，与 🎵 音乐开关分离）**；本地/人机/联机三模式在 `point`/`over` 事件触发（`app/hud.js`、`app/net.js`）
- **音乐优化（`public/js/audio.js`）**：16 步循环引擎，A 小调 Am-F-C-G 进行
  - 鼓组：底鼓 1/3 拍 + 军鼓 2/4 拍 + 八分踩镲（体育赛事节奏感）
  - 贝斯八分根音泵动 + 短促琶音旋律
  - **紧张强度 `setIntensity(0/1/2)`**：胶着（总分≥6）→ 加切分底鼓、踩镲加密、节奏加快 7%、音量 +0.04；赛点/局点/10 平 → 十六分踩镲、旋律高八度、再加速
  - 比分联动：`app/state.js updateMusicIntensity(score)` 每帧按比分求强度（`app/hud.js updateHud` 调用）；回主菜单自动复位 `setIntensity(0)`
- **验证**
  - `node test/browser-smoke.js` 54 项全过（含本地/人机/联机渲染无异常）
  - 无头浏览器 CDP 真实游戏像素验证：左/右/远端观众区像素分别为 4609/4522/3900（均 >0，观众入画）；得分后总观众像素 13031→17055（+31%，双臂挥舞/起跳生效）
  - 新增开发工具：`tools/find-cam.js`（数值搜索相机参数）、`tools/cdp-crowd.js`（CDP 真实游戏像素统计）、`tools/analyze-crowd.js` + `tools/shot-game.js`（截图分析）、`tools/preview-crowd.html`（静态预览）

### 桌面应用同步至最新版（v1.2.0，2026-08-04）
- **背景**：桌面应用（desktop-launcher.js + server.js）直接实时服务 `public/`，代码改动天然生效；本次把"版本/文档"层面同步到最新并实跑验证
- **改动**
  - `package.json`：1.1.0 → **1.2.0**（新功能：观众席 + 得分欢呼 + 赛事风格音乐）
  - `使用说明.txt`：修正过时操作说明（W/S 已为前后移动、推/扣改为单击/双击）；新增"赛场氛围"小节（观众席/欢呼音效/音乐强度/开关）
  - `README.md`：新增"赛场氛围"章节
- **验证**
  - 桌面启动器测试模式（`PP_APP_TEST=1`，无头 Edge 截图）实跑：服务器启动、页面加载、渲染、正常退出（app.log 无报错）
  - 正在运行的桌面服务器（8765 端口）逐文件核对：`audio.js` 含 `cheer()`/`setIntensity`、`render.js` 含 `drawCrowd`、`app/render.js` 为新相机（focal=vw*0.8）→ **运行中的桌面应用已在服务最新代码**
  - 无头截图 `tools/shots/game-crowd.png`：观众席在两侧/两端均入画（四区域观众像素 3175/2555/2732/3278）
- **说明**：若已打开的桌面游戏窗口是旧页面，刷新或重新打开窗口即可看到新内容（服务器端无需重启，每次请求实时读盘）

### 观众席改为像素小人 + 视角放低（2026-08-04）
- **需求**：观众小人按用户提供的 12×30 像素块 SVG 结构重做（绿/红/蓝等衣色、左/右举手欢呼、中间站得更高）；游戏视角放低
- **观众席（`public/js/render.js`）**
  - `crowdLayout`：每个观众带 `hue/sat/lit/tall` 字段——配色取自赛场常见衣色（绿 120/红 0/蓝 210/橙 45/紫 285 等 8 色，饱和度 42-56%、亮度 44-64%），**25% 观众站得更高**（身高 ×1.15）；生成顺序改为由远到近，保证遮挡正确
  - `drawCrowd` 重写为**像素小人**：以脚底为原点，一格 0.03m，身高 30 格≈0.9m，按用户 SVG 的方块结构绘制（头/双眼/嘴/身体/双腿+腿缝/手臂）；屏幕高度 <14px 时自动简化绘制（性能）
  - 欢呼状态：双臂举起（含手掌）+ 双手小幅挥舞 + 起跳浮动；常态手臂下垂
- **相机放低（`public/js/app/render.js makeCam`）**：机位由 (y7.0, z±6.2, 焦距 0.8vw) 放低为 **(y4.8, z±5.2, 焦距 0.9vw)**，视线抬到 (0,1.7,0)——更贴近球员高度的低机位转播感，球员更大，观众席仍入画
- **验证**
  - 无头浏览器 CDP 真实游戏像素验证：左/右/远端观众区均存在（198/182/1209）；得分后观众像素 1589→2380（**+50%**，举手欢呼生效）
  - 截图配色检查：观众区红/橙 11949、绿 8075、蓝紫 200（深蓝观众与台面蓝同色系被统计规则排除，实际已渲染）
  - `node test/browser-smoke.js` 54 项全过
  - 最新画面截图：`tools/shots/game-crowd.png`

### 像素小人形态补全（2026-08-04）
- **问题**：用户反馈"小人形态不完整"。排查发现两个根因：
  1. **纵向缩放符号错误（主因）**：`ph.y - pb.y` 为负（头在脚上方、屏幕 y 更小），原代码直接用它做 py 缩放，导致所有方块高度被 `Math.max(1, 负数)` 钳成 **1px 横条**——小人画成了断开的色条而非完整人物
  2. **头部只有顶部一块**：眼/嘴画在头脸与身体之间的背景空隙上，头部看起来缺失
- **修复（`public/js/render.js`）**
  - `personRects()` 抽出为纯函数（绘制与测试共用）：py 改用 `(pb.y - ph.y)/H`（正数），块坐标统一换算（`wy0 = (10 - y - h) * P_CELL`，脚底在第 9 行）
  - 小人结构**完全对照用户 `pixel-figures.html`**（11×14 格）：5×5 大头（含 2 眼 + 3 格嘴）、3×5 身体、1×3 腿、2×1 脚、5 格斜举手（欢呼）/ 2 格垂手（常态），头顶/脚底与投影对齐
- **验证**
  - 新增 `test/person-structure-test.js` **19 项结构断言全过**（头宽 5 格/身 3 格/脚 2 格、眼同高对称、嘴在眼下、左右臂各 5 格斜举收拢、总高 = 14 格投影高度、头顶脚底对齐）
  - CDP 真实游戏：观众区像素 7498/6754/29609（完整小人，约修复前的 20 倍）；得分事件触发欢呼状态（app.cheer 0→0.63）
  - `node test/browser-smoke.js` 54 项全过
  - 最新画面截图：`tools/shots/game-crowd.png`

### 观众席改为球员同款坐姿火柴人（2026-08-04）
- **需求**：观众席从像素小人改为"和球员一样的火柴人形象"，且显示为坐姿
- **实现（`public/js/render.js`）**
  - 新增 `seatedPose(s, time, cheer)`：坐姿火柴人骨架（纯函数，供绘制与测试共用）——圆头 + 髋→肩躯干 + 髋→膝大腿（前伸、比髋略低）+ 膝→脚小腿（下落至地面）+ 肩→手手臂；**面向球场中心**（侧看台朝 -x、端看台朝 -z）
  - `drawCrowd` 重写：用与球员相同的 `limb` 骨线绘制（圆头用 `arc` 填充 + 描边），配色为**浅色**（rgba(238,242,247,0.92)）——球员在亮色台面上用黑色可见，看台背景是深色，观众用浅色才能清晰辨识，画风（圆头+骨线）与球员一致
  - 欢呼状态：手从"搭在膝上"平滑举起（高于肩）+ 小幅挥舞 + 整体起身浮动；太远的观众只画头+躯干（性能）
  - 移除像素小人相关代码（PERSON/personRects 等）
- **验证**
  - 新增 `test/seated-crowd-test.js` **19 项骨架断言全过**（朝向球场中心、髋在座上、膝前伸且低于髋、双腿分开、脚低于膝接近地面、常态手搭膝、欢呼手高于肩且明显抬高、起跳浮动、朝向不变）
  - CDP 真实游戏：左/右/远端观众区浅色像素 7556/7376/15864（坐姿火柴人可见）；得分事件触发欢呼（app.cheer 0→0.57）
  - `node test/browser-smoke.js` 54 项全过
  - 最新画面截图：`tools/shots/game-crowd.png`

### 欢呼音效按 3 秒规格替换（2026-08-04）
- **需求**：按提供的音轨规格替换原有欢呼音频——3s / 44100Hz / 立体声 / 音量 0.85，含三轨：`base_applause`（0-3s 持续中等密度掌声，前轻→中高→尾渐弱）、`crowd_cheer`（0.4-2s 百人呐喊，峰值在 1s）、`decorate_whistle`（1-1.2s / 1.6-1.8s 两声清亮口哨），整体加 `reverb_small_hall` 小厅混响，2s 起 1s 淡出
- **实现（`public/js/audio.js` `cheer()` 重写）**
  - 混合总线：音量 0.85 + `fade_out`（2s 起 linearRamp 至 3s 静音）；干声 0.72 直出 + 湿声 0.42 进小厅混响
  - 小厅混响：程序生成双声道脉冲响应（0.7s 指数衰减 + 早期反射包络），`ConvolverNode` 实现
  - 掌声：泊松到达调度（约 80-110 声，密度随"前轻中高尾弱"包络变化），每声随机带通噪声短脉冲 + 随机声像（立体声）
  - 百人呐喊：左右两层带通噪声（中心频率 0.4→1s 上滑、1→2s 回落，峰值增益在 1s）+ 5 声"喔"上滑（百人起伏感）
  - 口哨：两声各 0.2s，正弦基音 + 泛音（更清脆）+ 颤音 LFO + 音高微摆，左右错位声像
- **配套**：`app/loop.js` 欢呼动画衰减 0.55/s → 0.30/s，视觉约 3.3s 与 3s 音频一致
- **验证**：CDP 真实浏览器触发得分 → `GameAudio.cheer()` 在真实 AudioContext（挂起）下无异常、欢呼状态正常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过（无 AudioContext 环境安全早退）

### 欢呼音效改为礼花绽放（2026-08-04）
- **需求**：欢呼声音换成"礼花绽放"（烟花绽放声）
- **实现（`public/js/audio.js` `cheer()`）**：保留 3s / 立体声 / 音量 0.85 / 小厅混响 / 2s 起 1s 淡出的整体结构，内容替换为礼花合成：
  - **三枚礼花弹错峰绽放**：0.2-0.7s 主礼花、1.0-1.5s 第二枚、1.7-2.2s 小礼花（尾段压在淡出里）
  - 每枚礼花 = **升空呼啸**（带通噪声中心频率 450→1900Hz 上滑 + 正弦扫频 180→640Hz "咻——"）→ **炸响"砰"**（宽频噪声爆 + 低频轰 120→45Hz）→ **噼里啪啦**（约 60-90 声高频火花爆裂，密度指数衰减、随机声像、随机音高）
- **验证**：CDP 真实浏览器触发得分 → `cheer()` 在真实 AudioContext（挂起）下无异常、欢呼状态正常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 得分音效改为裁判哨声（2026-08-04）
- **需求**：去掉礼花欢呼音效，得分改为哨声
- **实现（`public/js/audio.js`）**：`cheer()` 重写为 `whistle()`（导出同时保留 `cheer` 别名指向它，得分触发点无需改动）——两声短哨"哔——哔"（长 0.55s + 短 0.28s，间隔 0.78s）：
  - 每声哨 = 基音 2920Hz + 2x/3x 谐波（豌豆哨的响亮特征）+ 30Hz 颤音（哨声抖动感）+ 极轻带通噪声气声（吹气质感）
  - 保留小厅混响（湿 0.45），哨声在馆内回荡
- **验证**：CDP 真实浏览器触发得分 → `whistle()` 无异常、欢呼状态正常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 哨声降低尖锐感（2026-08-04）
- **需求**：哨声太尖锐，降低尖锐感
- **调整（`public/js/audio.js` `whistle()`）**：
  - 基音 2920 → **2350Hz**
  - 高次谐波压弱：2× 0.28→0.18、3× 0.15→**0.07**（刺耳高频大幅削减）
  - 总线新增 **4200Hz 低通**柔化顶端；混响湿声 0.45→0.5（回响更润）
  - 起音 12ms→25ms（去掉尖锐的起声"click"）；哨声加长（0.55/0.28s → 0.6/0.3s）
- **验证**：CDP 真实浏览器无异常、触发正常；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 得分音效改为鼓掌声（2026-08-04）
- **需求**：哨声换成鼓掌声
- **实现（`public/js/audio.js`）**：`whistle()` 重写为 `applause()`（导出 `cheer` 别名指向它，得分触发点无需改动）——约 2.4s 密集人群鼓掌：
  - 每声鼓掌 = 带通噪声短脉冲（中心频率 1200-3200Hz 随机）+ 快速起落（4ms 起、40-75ms 衰），随机声像（立体声）
  - 密度包络：起势(0-0.3s) → 高潮(0.3-1.2s) → 渐弱(1.2-2.4s)，泊松到达调度约 100-130 声
  - 尾声 5 声零星掌声（自然收尾）+ 1.8s 起总线淡出至 2.5s + 小厅混响（湿 0.4）
- **验证**：CDP 真实浏览器触发得分 → `applause()` 无异常、欢呼状态正常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 掌声提升质感（2026-08-04）
- **需求**：鼓掌声质感提升，更真实
- **升级（`public/js/audio.js` `applause()`）**：
  - **双层鼓掌**：每声 = 掌心低频"砰"（170-240Hz 短正弦、频率下坠）+ 高频拍击"啪"（2600-4200Hz 带通噪声极短瞬态）——不再是单一的"节拍器"噪声
  - **人群波浪**：密度/音量随 ~0.9Hz 正弦波浪起伏（人群齐拍感，替代均匀雨点）
  - **领掌**：8% 掌声更响更居中（个体突出的领掌，增加真实层次）
  - **空气感底噪**：极轻的低频人群噪底（0.045 增益）随 0.4Hz 慢起伏
  - 混响湿声 0.4→0.5、总线音量 0.85→0.9，掌声更"润"
- **验证**：CDP 真实浏览器无异常、触发正常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 掌声加响亮感（2026-08-04）
- **需求**：掌声要有响亮感
- **调整（`public/js/audio.js` `applause()`）**：
  - 总线新增 **DynamicsCompressor**（阈值 -18dB、比例 12、快起慢放）——密集掌声压成一道结实的"声墙"，持续响亮不破音
  - 总线音量 0.9→**1.0**；单掌声量提高约 60%（领掌 0.30→0.46）
  - 掌声密度提高（26+85 → **40+130** 声/秒）——气势更足
  - 掌心低频分量加强（0.55→0.65 权重、衰减加长）——拍手更"结实"
- **验证**：CDP 真实浏览器无异常、触发正常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 掌声替换为真实录音 WAV（2026-08-04）
- **需求**：用用户提供的真实掌声录音（`掌声_游戏版.wav`）替换合成掌声
- **实现**
  - 新增资源 `public/audio/applause.wav`（PCM 立体声 44.1kHz 16bit，**3.2s**，与 3s 欢呼动画匹配）
  - `public/js/audio.js`：
    - 新增 `loadApplause()`：`fetch('audio/applause.wav')` → `decodeAudioData` → 缓存；`ensure()` 创建上下文后预加载
    - `applause()` 改为：WAV 已加载 → 播放录音（音量 1.0，尾端 60ms 淡出防爆音）；未加载/加载失败 → 合成掌声 `applauseSynth()` 兜底（并顺带触发加载，下次得分即用录音）
    - 新增导出 `applauseLoaded()`（加载状态查询，供验证）
- **验证**：CDP 真实浏览器 `applauseLoaded()=true`（WAV 加载解码成功）、得分触发播放录音无异常（app.cheer 0→0.77）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项 + `net-test` 11 项全过

### 庆祝声：音量 -25%、时长 1.5s（2026-08-04）
- **需求**：庆祝声（掌声）音量减小约 25%，时长减至 1.5 秒
- **调整（`public/js/audio.js`）**：
  - WAV 播放：音量 1.0 → **0.75**；只播录音前 **1.5s**（`src.stop(t0+1.5)`，结尾 0.12s 淡出防爆音）
  - 合成兜底 `applauseSynth()` 同步：总线音量 1.0 → 0.75，时长 2.4s → 1.5s（高潮段 0.25-0.8s、渐弱至 1.5s、尾声零星掌声收在 1.5s 内）
- **验证**：CDP 真实浏览器 WAV 加载解码正常、得分触发无异常（app.cheer 0→0.78）；`browser-smoke` 54 项 + `seated-crowd-test` 19 项全过

### 观众分阵营：左我方右敌方、得分方欢呼对方摇头（2026-08-04）
- **需求**：观众左侧=我方观众、右侧=敌方观众；哪方进球哪方欢呼、对方摇头；默认我方红色、敌方蓝色
- **实现**
  - `app/state.js`：`app.cheer` 改为 `app.fan = { cheer:[0,0], shake:[0,0] }`（红/蓝双方球迷的欢呼量/摇头量）；`triggerCheer(winner)` 置得分方欢呼、对方摇头
  - `app/hud.js` / `app/net.js`：`point`/`over` 事件传入得分方 `e.s` 触发
  - `app/loop.js`：cheer/shake 四路每帧衰减（0.6/s，约 1.7s 平息，与 1.5s 掌声接近）
  - `public/js/render.js`：
    - **分阵营**：`drawCrowd` 按"世界 x<0 = 屏幕左侧"分配球迷归属——每方视角下左侧=己方球迷（红色）、右侧=敌方球迷（蓝色），红 `rgba(240,110,92)` / 蓝 `rgba(110,160,246)`（P1 红 / P2 蓝各自在左侧看到自己颜色的球迷，本地分屏/联机双侧视角均成立）
    - **摇头动画**：`seatedPose(s, time, cheer, shake)` 新增 shake——头部约 3.5Hz 左右快速摆动（失望），手仍搭膝
    - 欢呼时举手起跳（沿用）；`drawFloor`/`drawScene` 透传 `view.side` + `view.fan`
  - `app/render.js` 视图模型 `cheer` → `fan`
- **验证**：CDP 真实游戏（视角 side 0）左侧 8871 红 / 0 蓝、右侧 330 红 / 8672 蓝（左红右蓝 ✓）；蓝方得分后 `fan.cheer[1]=0.53`（蓝球迷欢呼）+ `fan.shake[0]=0.53`（红球迷摇头）✓；常规状态球迷平静 ✓；`seated-crowd-test` 22 项（新增摇头 3 项）+ `browser-smoke` 54 项 + `net-test` 11 项全过

### 观众加座位（看台长条座椅，2026-08-04）
- **需求**：为观众加上座位
- **实现（`public/js/render.js`）**：新增 `benchLayout()` + `drawBenches()`
  - 与观众行对齐生成 16 条长条座椅：两侧看台 8 条（沿 z 方向排座，x=±(3.62+row·0.55)）+ 两端看台 8 条（沿 x 方向排座，z=±(6.35+row·0.5)）
  - 座椅为看台台阶式钢蓝色块（`box`，`#5e7594`，从行地面到座面高 0.16m、深 0.36m、全长），`drawCrowd` 先画座位再画观众（观众坐在其上）
  - 导出 `benchLayout`/`drawBenches`（供验证）
- **验证**：CDP 真实游戏座位像素 3891（座椅渲染 ✓），左侧红球迷/右侧蓝球迷、得分方欢呼对方摇头、掌声 WAV 全部保持；`browser-smoke` 54 项 + `seated-crowd-test` 22 项全过
- 最新画面截图：`tools/shots/game-crowd.png`

### 球员穿上队色上衣（2026-08-04）
- **需求**：对方球员穿蓝色上衣、我方穿红色上衣；上衣不覆盖手臂
- **实现（`public/js/characters.js`）**
  - `STICK` 配色新增 `shirt`：P1（红方/side 0）`#d0321e`、P2（蓝方/side 1）`#2563eb`
  - **上衣 = 躯干（髋→肩脊柱线）与肩线**用队色加粗圆头绘制（lineR 1.6 / 1.25）；**手臂（肩→腕）、腿、头、颈仍是黑色骨线**——上衣不覆盖手臂
  - 默认场景（人机/本地）即"我方红上衣、对方蓝上衣"；联机按各自队伍色（红方红、蓝方蓝）
- **验证**：CDP 真实游戏近侧红上衣像素 2453、远侧蓝上衣 1492（颜色正确、排除木地板干扰）；`browser-smoke` 54 项全过
- 最新画面截图：`tools/shots/game-crowd.png`

### 球台下方加投影阴影（2026-08-04）
- **需求**：球台下方添加阴影，增加立体感
- **实现（`public/js/render.js`）**：新增 `drawTableShadow()`，在 `drawTable` 开头先画：
  - 4 层柔化四边形（接触影 α0.34 → 外层扩散 α0.06），覆盖约 **1.6 倍台面范围**（低视角下台面自身会遮挡近侧阴影，扩散足够大才能让台面四周可见地板变暗）
  - 轻微偏移模拟光照方向；颜色 rgba(6,10,16) 半透明，木地板上呈暗色柔影
- **验证**：CDP 真实游戏亮度梯度——台侧近处 242 vs 远处 300（**暗 19%**，阴影明显且正确渐变）；`browser-smoke` 54 项 + `seated-crowd-test` 22 项全过
- 最新画面截图：`tools/shots/game-crowd.png`（新增 `tools/check-table-shadow.js` 分析工具）

### 球员胯下阴影 + 正对面观众座位更明显（2026-08-04）
- **需求**：①人物胯下增加阴影；②正对面（远端）观众的座位显示调整得更明显，与左右两侧可见性一致
- **实现（`public/js/render.js`）**
  - **球员胯下阴影**：新增 `drawPlayerShadows()`——在每位球员脚下画软化径向渐变椭圆（半径 0.34m）；近侧球员脚部常被画面底部裁切，阴影**贴画面底部显示**（`cy = min(投影y, 高-12)`），保证可见
  - **远端座椅提亮**：`drawBenches()` 座面加高至 0.2m、靠背加高至 0.34m（从人群中露出）；端看台（axis='x'）新增**面向镜头的前脸提亮**（`poly` 亮钢蓝 `#8fa9c8`，绕过 3D 光照暗部）——正对面/远端座椅从原来几乎看不见（598 像素）变为清晰可见（**9626 像素**）
- **验证**：CDP 座位像素 30689、正对面远端座椅 9626（与左右一致）；球台阴影、球迷阵营、掌声、球员上衣全部保持；node 结构检查确认球员阴影渲染（径向渐变椭圆 ×2 贴底）；`browser-smoke` 54 项 + `seated-crowd-test` 22 项全过
- 最新画面截图：`tools/shots/game-crowd.png`

### 球桌阴影精简为两层（2026-08-04）
- **需求**：球桌阴影保留两层即可
- **调整（`public/js/render.js` `drawTableShadow`）**：4 层 → **2 层**——接触影（1.0×台面，α0.36）+ 外围柔影（1.40×台面，α0.18）
- **验证**：CDP 台侧阴影 243 vs 远处 300（暗 19%，两层效果保持清晰）；`browser-smoke` 54 项全过


## 二、2026-08-04 修改记录（发球轨迹瞄准改版）

### 1. 电脑端：发球轨迹跟随鼠标瞄准
- 待发阶段移动鼠标，发球轨迹线实时跟随；落点由“火柴人站位（发球点）+ 鼠标位置”共同决定，夹取在对方半台台面安全区内
- 单击发球（操作不变）；发球方案 = 瞄准时求解的方案，预览轨迹与实际发球完全一致（所见即所得）
- 实现：`engine/shots.js` 新增 `solveServeTo`/`setServeAim`（沿“发球点→目标落点”方向搜索速度/角度/旋转，用真实物理验证先本方后对方半台、不过网不出界），`strokes.js` 发球时直接复用瞄准方案

### 2. 手机端：点两下屏幕发球
- 第一下点按进入瞄准，手指移动到哪、轨迹线跟到哪；第二下点按发球
- 对打操作不变：单击推球 / 双击扣球

### 3. 解不出合法发球：轨迹消失、发不出球
- `setServeAim` 求解失败时置 `serveAimBlocked`：渲染层隐藏轨迹（本地/人机/联机），`startServeStroke` 拒绝出手
- 快照新增 `sb` 字段，联机模式下服务端判定不可达后，客户端同样隐藏轨迹并禁止发球，并提示“该位置无法发球，请调整瞄准”

### 4. 联机支持
- 客户端输入帧携带瞄准落点 `a:[x,z]`，本地 `server.js` 与 Cloudflare `room-core.js` 均会应用（服务端求解发球方案并随快照返回）
- 本地版快照补上 `sp` 字段（此前仅 Cloudflare 版有），联机双方都能看到瞄准后的精确轨迹

### 5. 其他
- `resetMatch` 增加挥拍/冷却复位，避免上一局残留动作影响新一局
- 冒烟测试新增：鼠标瞄准跟随、落点始终在对方半台、不可达时轨迹消失且发不出球、手机两下点按发球、对打单击推球/双击扣球
- `serve-path-online` 新增联机瞄准与“不可达”端到端用例

验证：`node test/browser-smoke.js`、`net-test.js`、`engine-stress.js`、`physics-boundary-test.js`、`ai-test.js`、`serve-path-online.js` 全部通过

## 三、2026-08-04 修改记录（推球抬高过网 / 扣球变快变难）

### 1. 推球（接球后回球）
- 出球角度改为从水平偏上开始搜索，并按击球高度在网顶上方留净空（约 1.2~5.5cm），弧线明显抬高、干净直接过网，不再贴网/擦网
- 低球（击球点接近台面）推球本来就无法直接过网，保持“无解即打空”的物理限制

### 2. 扣球
- 挥拍速度 8.5 → 9.5，实测出球速度约 20.9m/s（原约 18.8），球速更快
- 增加“贴网下压”约束：过网净空必须落在 0.4~8cm 区间，并封顶出球角度（去掉高抛“兜底”）
- 效果：球不够高时扣球求解失败（打空/下网），发球回球等低球不再能直接扣杀，符合真实乒乓球规律

### 3. 实现
- `shots.js solveRally/rallyFlightOk`：增加逐子步的过网高度检测（插值估算网面处高度，要求落在允许的净空区间），`computeShot` 按推球/扣球分别传入净空参数

### 4. 测试更新
- 压力测试：扣球回合改为“高空球互扣”（发球回球太低无法扣杀，原场景已不成立）；脚本化对打增加“完成若干拍后停手”，让回合自然结束（推球可靠后，完美回球回合不再自行终止）
- AI 测试：多拍回合增加停手机制
- 物理边界测试：合法用例改为高球扣球，本方半台用例改为真正朝后的目标

验证：`node test/engine-stress.js`、`ai-test.js`、`physics-boundary-test.js`、`browser-smoke.js`、`net-test.js`、`serve-path-online.js` 全部通过

## 四、2026-08-04 修改记录（击球判定范围加大 + 球拍自动伸向球）

### 1. 判定范围（球形）加大，球本身大小不变
- 球的视觉/物理大小（BALL_RADIUS）不变
- 击球判定范围从 0.85m 加大到 1.2m（球形，以球员持拍接触点为球心），范围内点击推球/扣球即可击球

### 2. 球拍自动伸向球
- 点击推球/扣球后，如果球在判定范围内但偏离正常挥拍路线，球拍从挥拍一开始就实时跟踪球的位置（拍面略越过球），击球瞬间拍面贴着球
- 球正好在挥拍路线上时保持原挥拍动作（不抖动）
- 实现：`strokes.js updateStroke` 接触窗口内用“点到线段距离”判断偏移，`vlerp` 跟踪并同步挥拍锚点

### 3. AI 同步
- `ai.js` 接球判定距离 0.80 → 1.05，与加大的判定范围保持一致

### 4. 测试更新
- 物理边界测试新增：运动中的球也能被自动伸拍击中、最小拍球距离 < 0.15m
- 挥拍缓动曲线改在自动伸拍生效前采样（自动伸拍会改变中段拍位，但缓动速度曲线不变）

验证：`node test/physics-boundary-test.js`、`engine-stress.js`、`ai-test.js`、`browser-smoke.js`、`net-test.js` 全部通过

## 五、2026-08-04 修改记录（球拍判定范围加大 + 真实击球动作）

### 1. 球拍视觉大小不变，判定范围继续加大
- 球拍渲染尺寸未改（characters.js / RULES 不动）
- 球员可及范围（球形判定）1.2m → 1.4m，点击推球/扣球的容错更高
- 新增球拍判定范围 `hitR=0.18m`（以球拍为球心的球形，明显大于视觉拍面）：球进入该范围且球拍已伸到球附近才判定击中

### 2. 击球时保证真实的球拍击打动作
- 接触窗口内球拍实时跟踪球（拍面略越过球），不再隔空“吸”球
- 击球瞬间拍面落到球上，任何球速下都是球拍真实打到球
- 实现：`strokes.js updateStroke` 接触窗口改为 `t ∈ [windup, windup+live]`，无条件跟踪 + 击球瞬间把拍面放到球位置

### 3. 测试
- 物理边界测试新增场景全部通过（近球/远球/横移快球/高球扣球/快球侧飞扣球，击球瞬间拍球距离 ≤ 0.05m）

验证：`node test/physics-boundary-test.js`、`engine-stress.js`、`ai-test.js`、`browser-smoke.js`、`net-test.js` 全部通过

## 六、2026-08-04 修改记录（移动范围扩展到己方半场）

### 1. 移动范围：以球网线为界的己方半场，不能上球桌
- 前后移动前向极限从“台面端线之后 1.48m”扩展到球网线（0.1m）
- 不能上球桌：台面正投影范围内不允许站立——台面宽度内最多走到台面端线；台面两侧可以继续走到球网线
- 实现：`RULES.Z_FWD 1.48→0.1`；`engine.js` 移动后按台面范围夹取（|x|≤半宽 且 |z|<半长 时退到端线）

### 2. 任意可移动的位置都能击球，移动中也能击球
- 击球判定范围实时跟随球员当前位置（不再固定为挥拍开始时位置），挥拍中移动同样能打
- 台面两侧、近网、底线后等任意位置推球/扣球均验证可击中

### 3. 发球适配
- 持球点/持拍位置自动限制在离球网 0.7m 的己方一侧：球员站到网前时，发球点自动回退，保证“先落本方半台”的发球仍可解
- 修复：`solveServe` 缓存键未包含站位 z（球员可前后移动后，不同站位会拿到过期轨迹）

### 4. 测试
- 物理边界新增：台面中间向前止步于端线、台面外侧可走到球网线、任意移动不上球桌、网前站位发球仍可解

验证：`node test/physics-boundary-test.js`、`engine-stress.js`、`ai-test.js`、`browser-smoke.js`、`net-test.js`、`serve-path-online.js` 全部通过

## 七、2026-08-04 修改记录（左上角显示球与球拍判定范围）

### 1. 游戏画面左上角判定范围显示
- 进入游戏后，页面左上角（网络信息条下方）显示“击球判定范围”面板：
  - 球判定范围：1.4m（球员可及球形范围，球在此范围内点击即可击球）
  - 球拍判定范围：0.18m（以球拍为球心的球形判定范围）
- 数值从引擎 `RULES.HIT_ZONE_R / PADDLE_HIT_R` 读取并同步，修改判定参数后显示自动更新
- 实现：`index.html` 新增 `#hitRangeInfo` 面板、`style.css` 样式、`hud.js` 启动时填充数值；`strokes.js` 改用 RULES 常量（原硬编码 1.4 / 0.18）

验证：`node test/browser-smoke.js`（新增面板显示断言）、其余全部测试通过

## 八、2026-08-04 修改记录（电脑端蹲下 / 跑步）

### 1. 按键
- Shift（左右均可）：跑步，移动速度 ×1.45
- Ctrl（左右均可）：蹲下，移动速度 ×0.55，火柴人摆出蹲姿

### 2. 蹲下的作用
- 击球点变低：球拍待机高度降到 0.80m、击球判定中心降到 0.85m、可击球的最低球高降到 0.55m
- 能接远台很低的球：蹲下时推球改用“更快 + 更高弧线”的防守性回球（放高球），
  实测球高 0.80~0.85m、落点贴近底线时，不蹲下接不到、蹲下可以接并合法过网

### 3. 实现
- `RULES` 新增 `RUN_SPEED_MUL / CROUCH_SPEED_MUL / CROUCH_PADDLE_Y / CROUCH_HIT_Y / CROUCH_MIN_BALL_Y`
- 引擎：移动速度按跑步/蹲下乘倍率；球员带 `crouch/run` 状态；蹲下时球拍/判定中心降低
- `shots.js`：蹲下推球走防守性高弧线求解（更高角度 + 更快出球）
- 联机：输入帧带 `c/rn` 字段（server.js / room-core.js 解析），快照带 `cq/rn`
- 渲染：`characters.js` 蹲姿——髋部降低、膝盖前屈（双腿分段）、身体前倾、步幅缩小

### 4. 测试
- 冒烟新增：Shift 跑步加速、Ctrl 蹲下减速且蹲姿生效、蹲下可接远台低球 / 不蹲下接不到

验证：`node test/browser-smoke.js`、`engine-stress.js`、`ai-test.js`、`physics-boundary-test.js`、`net-test.js` 全部通过

## 九、2026-08-04 修改记录（首页判定范围虚线开关）

### 1. 首页开关
- 主菜单新增“判定范围虚线显示（球 / 球拍周围）”勾选项，由用户选择显示或隐藏
- 选择会存入 localStorage，下次打开保持上次选择

### 2. 游戏内虚线指示
- 开启时，游戏画面中：
  - 球周围一圈水平虚线圆环，半径 = 球判定范围（HIT_ZONE_R 1.4m）
  - 双方球拍周围一圈水平虚线圆环，半径 = 球拍判定范围（PADDLE_HIT_R 0.18m）
- 虚线随球/球拍实时移动，关闭开关即不再绘制

### 3. 实现
- `index.html` 菜单新增复选框；`state.js` 读取/保存开关状态（默认开启）
- `render.js` 新增 `drawHitRangeRing`（水平虚线圆环），`drawScene` 按开关绘制球与球拍判定范围
- 视图模型携带 `showHitRanges`

验证：`node test/browser-smoke.js`（新增开关默认开启/关闭隐藏/重新开启用例）全部通过

## 十、2026-08-04 修改记录（球判定范围缩小为球形）

### 1. 球判定范围缩小
- `HIT_ZONE_R`：1.4m → 1.0m，球必须更靠近球员可及范围才能点击击球
- 左上角判定范围面板自动显示新值（1m）
- AI 接球判定距离同步 1.05 → 0.9，与缩小的范围保持一致

### 2. 显示改为球形
- 球周围的判定范围虚线由“水平圆环”改为“球形线框”（水平环 + 面向相机的垂直环），直观呈现球形判定
- 球拍判定范围仍为水平虚线圆环
- 实现：`render.js` 新增 `drawDashedCircle`（任意平面虚线圆）与 `drawHitRangeSphere`（球形线框）

验证：`node test/browser-smoke.js`、`physics-boundary-test.js`、`engine-stress.js`、`ai-test.js`、`net-test.js` 全部通过

## 五、测试

```bash
node test/net-test.js        # 本地联机：建房/加入/输入/计分/断线
node test/do-smoke.js        # DO 房间逻辑（mock WS）
node test/do-persist.js      # DO 持久化（驱逐恢复/重挂/补发广播）
node test/serve-path-online.js # 联机发球预测轨迹（快照 sp + 客户端采样）
node test/ws-live.js         # 公网协议冒烟（默认连 wss://ping-pong-duel.pages.dev/ws，可用 WS_URL 覆盖）
node test/ws-evict.js 35000  # 公网驱逐验证（建房后等 35s 再加入）
node test/ws-sp.js           # 公网发球瞬间快照带 sp 字段
node test/engine-stress.js   # 引擎压力
node test/ai-test.js         # 人机：发球/回球/追球/难度
node test/browser-smoke.js   # 前端：按钮接线/渲染/联机 HUD
npm run test:all             # 本地测试集合
```

## 六、待办（下次修改）

- [ ] 参考视频动画/物理效果：`SVID_20260803_003214_1.mp4`（球的拖尾/镜头/震屏/粒子等尚未实现，需用户确认是否要加）
- [ ] 联机断线自动重连（当前断线弹遮罩返回菜单）
- [ ] DO storage 数据量监控（MVP 每消息全量落盘，量大可改增量）

## 七、踩坑备忘

- `lerp` 是标量函数，向量平滑必须用 `vadd + vscale(vsub(...))`
- Canvas 绘制 NaN 坐标会被**静默忽略**，不抛错——调试渲染问题要直接读像素/截图，不能只看控制台
- `wrangler pages deploy` 目录下如有 `wrangler.toml`，加目录参数会忽略配置（绑定丢失）
- **Pages 命令不支持 `--config` 自定义路径**（会直接报错），配置必须放项目根 `wrangler.toml`
- **Pages 配置不支持 migrations**；DO 绑定必须 `script_name` 指向独立 Worker，migrations 放该 Worker 的配置
- **`*.workers.dev` 域名在国内网络被 DNS 污染**（nslookup 解析到 Facebook IP 段），凡涉及公网访问一律挂 pages.dev 或自定义域名
- **DO 驱逐**：WebSocket Hibernation 下 DO 约 30s 无消息即被驱逐，内存状态全丢——联机服务器必须持久化或保持活跃
- `wrangler deploy` 会重启 DO 断开现有连接，发布时避开活跃对局


## 📦 v1.3.0 合并记录（2026-08-05）

### 背景
- 存在两个并行分支：**v1.1.0**（乒乓对决(5).zip，玩法手感分支）与 **v1.2.0**（桌面版 乒乓对决.zip，赛场氛围分支），两者共享同一代码基座（27 个文件字节级一致）。
- 本次把两份合并为 **v1.3.0**，完整保留两边的全部功能。

### 合并方式
- 以 v1.2.0 为结构基座；引擎/服务端等 v1.1.0 为超集的文件直接采用 v1.1.0 版本（engine/rules|state|shots|strokes、engine.js、src/engine.js、src/room-core.js、server.js、ai.js、app/input|main|modes.js、index.html、style.css、5 个测试），`public/js/engine/*` 与 `src/engine/*` 保持字节级一致。
- 手工合并：render.js（v1.2.0 坐姿观众席/长椅/分阵营/投影 + v1.1.0 判定范围虚线）、characters.js（队色上衣 + 蹲姿）、app/state.js（fan 分阵营状态 + 发球瞄准/判定范围状态）、app/hud.js、app/render.js（低机位相机 + 发球瞄准逆投影）、app/loop.js、app/net.js。
- 音效采用 v1.2.0 的真实掌声 WAV（`public/audio/applause.wav`）+ 合成回退（完整取代 v1.1.0 合成欢呼）。
- 相机采用 v1.2.0 低机位转播视角（y4.8）。

### 合并后的完整功能
- **玩法（v1.1.0）**：发球瞄准（鼠标实时跟随 + 手机两下点按）、Shift 跑步、Ctrl 蹲下、移动范围到网前（不上球桌）、球拍自动伸向球真实接触、推球抬高弧线/扣球更快（20m/s+）、判定范围虚线开关与左上角数值显示、球形判定（球 1.0m / 拍 0.18m）
- **氛围（v1.2.0）**：坐姿火柴人观众席 + 钢蓝色看台长椅、红/蓝分阵营欢呼摇头、真实掌声录音、队色上衣（P1 红/P2 蓝）、球台/球员投影、低机位转播视角、赛事风格背景音乐（比分胶着/赛点提速）
- **联机协议**：输入字段 `c/rn`（蹲/跑）与 `a:[x,z]`（发球瞄准落点），快照字段 `cq/rn/sp/sb`

### 验证
- 全部自动化测试通过（`npm run test:all` + `npm run test:seated`）；`node --check` 全部脚本通过；`node server.js` 页面与资源加载正常；浏览器 GUI 冒烟验证通过。

### 说明
- v1.1.0 独有的 `app.log` 为运行期产物，未并入。
- `tools/shots/menu.png` 采用 v1.1.0 的大图（含判定范围开关的菜单截图）；`game-crowd.png`、`action-stick.png` 采用 v1.2.0。
- 待办（六）保持不变：参考视频特效（拖尾/震屏/粒子）、联机断线自动重连、DO 存储增量写。


## 十一、2026-08-05 修正：判定范围虚线指示与实际判定对齐

- **问题**：开启「判定范围虚线显示」后，球的判定范围虚线画在以**球**为球心的 1.0m 球体上；而引擎实际判定是「以**球员**为球心的 1.0m 球形可及范围」——球心跟随球员、蹲下时降低至 0.85m（`CROUCH_HIT_Y`）、向网前偏移 0.42m，且要求球高 > 0.72m（蹲下 0.55m）。球拍判定实际是「以球拍为球心的 0.18m 球形」，虚线却只画了水平环。指示与判定不一致。
- **修正**：
  - `engine/rules.js`（含 `src/engine/` 镜像）新增常量 `HIT_ZONE_Y: 1.04`（站立击球点高度）、`HIT_MIN_BALL_Y: 0.72`（站立最低可击球高），消除引擎硬编码。
  - `engine/strokes.js`（含镜像）zone 球心高度与球高门槛改用上述常量，渲染与判定共用同一数值来源。
  - `public/js/render.js`：球的判定范围虚线改为以**球员 zone 球心**绘制 1.0m 球体（蹲下用 `CROUCH_HIT_Y`）；球拍判定范围虚线改为**球形**（水平环 + 垂直环），与 0.18m 球形判定一致。
- **验证**：`npm run test:all` 127 项 + `test:seated` 22 项全部通过；`node --check` 通过；`src/engine` 镜像字节级一致；实机加载渲染正常。


## 十二、2026-08-05 接球判定改为碰撞箱（进箱即命中 + 下蹲真正有用）

- **问题（实测）**：原判定链「球在以球员为球心的 1.0m 球内 **且** 球高 > 0.72（蹲 0.55）→ 球拍 40/s 速率瞬间自动伸球 → 球进球拍 0.18m 球才命中」中，球拍自动伸球太快，0.18m 触球条件几乎必满足，**外层 1.0m 球体不构成真正门槛**；真正的拦路虎是独立的球高门槛——低于 0.55m（下蹲下限）的低球完全无法接起，下蹲键作用有限。
- **修正**：
  - 判定改为**接球碰撞箱**：以球员为中心、向网前偏移 0.42m 的长方体（`HITBOX_HX 0.80 / HITBOX_HZ 0.60`），站立箱 `y∈(0.55, 1.55)`，蹲下箱 `y∈(0.20, 1.30)`。
  - `engine/strokes.js`（含 `src/` 镜像）：**进箱即命中**——窗口内球进箱 + 合法（mayHit）即 computeShot + applyPaddleHit；移除球拍 0.18m 接触门槛，球拍自动伸球保留为击球动画。
  - `engine/rules.js`（含镜像）：新增 `HITBOX_*` 常量，移除原 `HIT_ZONE_R/HIT_ZONE_Y/HIT_MIN_BALL_Y/PADDLE_HIT_R/CROUCH_HIT_Y/CROUCH_MIN_BALL_Y`。
  - `public/js/ai.js`：AI 改用同一站立箱体判断（与玩家一致）。
  - `public/js/render.js`：判定范围虚线改为**接球箱线框**（`drawHitBox`，蹲下自动用蹲伏箱高）。
  - `public/js/app/hud.js` + `index.html`：左上角改为「接球箱 1.6×1.2×1.0m / 蹲下最低接球 0.2m」。
  - 测试：`physics-boundary-test.js` 第 9 节改为 7 项碰撞箱断言（进箱即命中/出箱不命中/过高过低/下蹲接低球/贴地球仍不可接/伸球动画）；`browser-smoke.js` HUD 文案断言同步。
- **交互影响**：横向范围（±0.8m）略窄于原 1.0m 球体，但纵向大幅放宽（站立 0.55 起）+ 下蹲可接贴地球 → 总体更易接球、下蹲有明确收益（代价是减速 ×0.55）；发球、违例、连击规则不变；无协议改动。
- **验证**：`npm run test:all` + `test:seated` 全部通过（含 7 项新碰撞箱断言）；`node --check` 通过；`src/engine` 镜像字节级一致；实机浏览器发球/对打/计分回路正常，HUD 显示新文案。


## 十三、2026-08-05 左上角新增「球高 + 进箱」实时指示

- **需求**：对打时能直观看到球的高度，并实时知道球是否已进入（自己的）接球碰撞箱。
- **实现**：
  - `public/index.html`：判定面板（`#hitRangeInfo`）新增两行「球高」与「进箱」。
  - `public/css/style.css`：新增 `.hr-dot.box`（白色方点）与 `#inBoxStatus.on/off`（绿=进箱 / 灰=未进箱）。
  - `public/js/app/state.js`：注册 `ballHeight`、`inBoxStatus` DOM 引用。
  - `public/js/app/hud.js`：`updateHud()` 每帧调用 `updateHitRangeLive()`——球高取引擎/快照球位（联机飞行 `b` / 持球 `bh`）；进箱判定用与引擎 `strokes.js` 逐字一致的公式（`HITBOX_*` 常量）。判定对象：人机=红方(你)、联机=自己、本地双人=P1+P2（任一进箱即绿）。
  - 测试：`browser-smoke.js` 新增 2 条断言（球高文本形如 `x.xxm`、进箱状态带 on/off 类）。
- **说明**：纯 UI 指示，不改任何判定逻辑；发球阶段照常显示（信息无害）。
- **验证**：`npm run test:all` + `test:seated` 全部通过（含新增断言）；实机浏览器：球高随球实时跳动（0.98m→0.02m），进箱状态随球进出切换（进箱→未进箱）。


## 十四、2026-08-05 蹲下收益提升：任何低球都能接住

- **需求**：蹲下后无论多低的球都能接住（原蹲下箱底 0.20m，低于它的贴地球接不到）。
- **问题**：仅调低箱底不够——蹲下防守回球速度固定 `outSpeed×1.35`（约 4.5m/s）、角度只到 60°，从 y≈0.1m 起跳无法同时「过网（网顶 0.9125m+净空）」又「落在对方台面」，`computeShot` 返回 null，命中不触发。
- **修正**：
  - `engine/rules.js`（含镜像）：`CROUCH_HITBOX_Y_BOTTOM 0.20 → 0.02`（贴地，任何还在空中的低球都能接；落地球已判分）。
  - `engine/shots.js`（含镜像）：
    - `computeShot` 蹲下防守速度按球高自适应——`defSpeed = 1.35 + 0.55 × (1 - y/0.55)`，贴地球 ≈1.9×（约 6.4m/s），普通低球 1.35×；
    - `solveRally` 蹲下防守角度列表扩到 `[8..72]`（含极低球陡弧）。求解器自带全物理校验，只返回合法轨迹。
  - 文案：HUD/README/使用说明「蹲下最低接球 0.02m（贴地，任何低球都能接）」。
  - 测试：`physics-boundary-test.js` 9f 改为正向（蹲下可接贴地球 y=0.15）、新增 9h（蹲下可接极低球 y=0.08）、9i（低于箱底 0.02m 贴地即落接不到）。
- **交互**：蹲下收益=可接任何低球（越低的球回球越高吊），代价=移动减速 ×0.55、箱顶降到 1.30m（高球不如站立好扣）。站立判定不变；AI 不下蹲不受影响；无协议改动。
- **验证**：`npm run test:all` + `test:seated` 全部通过（含 9f/9h/9i）；`src/engine` 镜像一致；实机 HUD 显示「低至 0.02m」。


## 十五、2026-08-05 AI 状态按方位区分 + 新增模拟球赛工具（功能实现度验证）

- **问题**：`public/js/ai.js` 的 AI 状态按「引擎」为 key（`getState(engine)`），同时用 AI 控制双方时（AI vs AI 模拟/观战），双方共用一份状态（`hitDelay/errTarget/catchRolled/moveT/rng` 互相踩踏），AI 永不挥拍。
- **修正**：`getState(engine, side)` 按 (引擎, 方位) 区分（种子 `20260802 + side*7919` 保持确定性）。真实单人对战（只有 side 1 用 AI）行为完全不变。
- **新增**：`tools/sim-match.js` —— AI vs AI 模拟球赛工具：完整 11 分制对局统计 + 低球注入测试（验证蹲下接任何低球）。
- **模拟球赛结论（功能实现度）**：
  - ✅ 完整对局：地狱vs地狱 11:8、困难vs地狱 11:0 均正常打完；发球轮换、计分、判分原因（opp-miss/double）正常；无 NaN（物理稳定）；最长回合 15/39 拍。两次地狱对局结果完全一致（确定性可复现）。
  - ✅ 蹲下接低球：低球注入 12 颗——纯 AI 接住 0/12（站立箱底 0.55m，设计使然），蹲伏 AI 接住 **12/12**，实机对局中生效。
  - ℹ️ 发现：熟练 AI 对打出现**无限回合**（AI 求解回球必合法落台、双方又都接得住），且 AI 对打中球高从未低于 0.78m——**AI 不制造低球**，低球来自人类失误/下压球，蹲下救低球正是为此设计。
- **验证**：`npm run test:all` + `test:seated` 全部通过（ai-test 5/5 保持）；`node tools/sim-match.js` 输出如上。


## 十六、2026-08-05 新增「AI 观战（AI vs AI）」模式

- **需求**：主页加入 AI vs AI 入口，可分别设定红方/蓝方 AI 难度（简单/中等/困难/地狱，地狱沿用"击败困难解锁"）；比赛页支持暂停、暂停中调整双方难度、返回主页面。
- **实现**：
  - `public/index.html`：主页新增 `#btnAIVsAI` + `#aiLevelA`/`#aiLevelB` 双难度下拉；暂停面板新增 `#pauseAIVsAI` 区块（`#pauseAiLevelA`/`#pauseAiLevelB`，仅 AI 观战显示）；「退出比赛」文案改为「退出比赛（返回主页面）」。
  - `app/state.js`：新增 `aiLevelA/aiLevelB` 字段与 6 个 DOM 引用。
  - `app/modes.js`：抽取 `readAiLevel(sel)`（地狱解锁校验），新增 `startAIVsAI()`（mode='aivai'）。
  - `app/loop.js`：新增 aivai 分支——120Hz 固定步进，双方 `AIC.control`（依赖上一轮 ai.js 按 side 区分状态）+ step + 事件；暂停冻结；`renderSingle` 观战渲染；难度修改即时生效。
  - `app/input.js`：`updateGameTools`/Escape 支持 aivai；`togglePause` 打开时同步并显示双方难度下拉，change 写回 `app.aiLevelA/B`。
  - `app/hud.js`：aivai 计分/发球横幅（红方/蓝方 发球）/netInfo（AI 观战 · 红X vs 蓝Y）/结算文案（红方 AI/蓝方 AI）；`winnerName()` 统一得分方命名。
  - `app/main.js`：绑定按钮；地狱锁定同步到 4 个难度下拉。
  - `css`：`.aivai-row`/`.pause-aivai` 样式。
- **验证**：`npm run test:all`（含新增 8 条 AI 观战冒烟断言：启动/双难度读取/netInfo/自动对打/暂停面板/改难度生效/继续/退出回主页）+ `test:seated` 全部通过；实机浏览器：AI 观战对局比分推进（3:0→4:0）、暂停中把红方改为困难后 netInfo 实时显示「红困难 vs 蓝中等」、退出返回主页。


## 十七、2026-08-05 拉大 AI 难度差距（中等 vs 困难 可感知）

- **问题（实测反馈）**：中等与困难 AI 打起来没区别。根因：各难度接球率都近乎 100%（此前只有地狱有 catchProb），反应差 0.09s、站位误差差 0.08m 在 1.6×1.2m 的接球箱面前不产生可见失误；回球又由求解器保证合法落台 → 观战/对打都看不出档位差距。
- **修正**（`public/js/ai.js` LEVELS）：
  - 给每档难度增加**接球率 catchProb**（漏接=不挥拍让球飞过，档位间最直观的差距）：
    简单 0.55 / 中等 0.78 / 困难 0.90 / 地狱 0.88（地狱刻意少 2pp 保持可战胜）
  - 拉大中等与困难差距：中等 react 0.14→0.18、err 0.12→0.16；简单 react 0.28→0.30。
- **验证**：AI vs AI 模拟——困难 vs 中等 **11:2**、困难 vs 简单 11:0、地狱 vs 困难 **14:12（抢七，地狱险胜）**、中等 vs 简单 11:7、地狱 vs 中等 11:4；档位阶梯清晰且地狱强于困难。`npm run test:all`（含四档参数递增断言）+ `test:seated` 全部通过。


## 十八、2026-08-05 AI 观战暂停面板：4 项 AI 参数微调（难度基准上）

- **需求**：AI 观战暂停时，玩家可在选定难度基础上，用滑杆微调红方/蓝方 AI 的战斗力。
- **实现**：
  - `app/state.js`：`aiTuneA/aiTuneB`（`{reactMul, catchMul, smashMul, agilityMul}`，默认 ×1）+ 8 个滑杆 UI 引用。
  - `index.html`：暂停面板 `#pauseAIVsAI` 内红/蓝各一组 4 滑杆（反应/接球/攻击/敏捷，50%~150%，step 5）。
  - `ai.js`：`control(engine, side, dt, level, tune)` 新增可选第 5 参——反应 `L.react/×`、接球率 `clamp(L.catchProb×, 0.20, 0.99)`、攻击性 `clamp(L.smashProb×, 0, 1)`、敏捷 `clamp(L.agility×, 0, 1)`；`err/smashY` 随难度基准。不传 tune 时行为完全不变（人机/模拟工具不受影响）。
  - `app/loop.js`：aivai 分支把 `aiTuneA/B` 传给双方 AI（下一帧生效）。
  - `app/input.js`：打开暂停时同步滑杆到当前倍率；`input/change` 写回 `app.aiTuneX` 并更新 ×文本（拖动即生效）。
  - `app/hud.js`：netInfo 难度名后加 `⚙` 表示已微调。
  - 测试：`browser-smoke.js` 新增 2 条断言（滑杆写回 aiTuneA、netInfo ⚙）。
- **验证**：`npm run test:all` + `test:seated` 全部通过；模拟——困难vs困难 基准 11:8 → 蓝方接球×0.5 变 11:3（明显变弱）→ 中等vs中等 蓝×1.5接球+反应×2 变 0:11（碾压）；实机：暂停把蓝方接球拉低后 netInfo 出现「蓝中等⚙」且蓝方开始「未能回球」漏接。


## 十九、2026-08-05 提高玩家接球成功率：输入零延迟 + 感知辅助（仅判定范围显示开启时）

- **问题（实测反馈）**：玩家接不到快球的最大头是**输入延迟**——对打单击推球要等 280ms 双击判定（`setTimeout`) 才按下，快球时挥拍窗口已过。
- **方案一：输入零延迟**（`public/js/engine.js` + `src/engine.js` 镜像 + `public/js/app/input.js`）：
  - 对打**单击立即推球**：去掉 280ms 延迟与 `tapTimer`，单击下一帧即出拍；
  - **双击升级扣球**：280ms 内同侧第二击发扣球边沿，引擎在 `step()` 检测——推球挥拍未出球且仍在挥拍窗口（`t < windup+live`）、对打阶段时，把该推球挥拍**重算为扣球**（`ctx.startRallyStroke(state, i, 2)`）；
  - 发球路径不受影响（升级仅对打、球离手时生效）；联机一致（客户端只发 pu/sm 边沿，服务端同一引擎升级，无需协议改动）。
- **方案四：感知辅助**（仅首页"判定范围虚线显示"开启时生效）：
  - `public/js/render.js`：判定箱线框——球在该球员箱内时画**绿色高亮**（`rgba(74,222,128,0.9)`），否则白色虚线；进箱即高亮，所见即所得；
  - `public/js/audio.js`：新增 `ready()` 柔和短上升提示音；
  - `public/js/app/hud.js`：`updateHitRangeLive()` 跟踪球进"人类控制方"（人机=你；本地=P1/P2）箱体的**上升沿**，对打阶段触发 `GameAudio.ready()`——球进箱瞬间"嘀"一声提示出手时机；关闭显示后无提示；
  - `public/js/app/main.js`：调试句柄 `__PPD` 暴露 `GameAudio`（供测试断言）。
- **测试适配**：`browser-smoke.js` 对打单击/双击断言改为零延迟模型（单击后 3 帧内立即出拍；双击升级为扣球），新增 5 条进箱提示断言（进箱响一次/持续不重复/出箱停/再进再响/关闭显示不响）。
- **顺带修复两个预先存在的过时断言**（与本次改动无关，上一轮调优遗留）：
  - `ai-test.js`「困难AI 多拍回合」：困难 AI 有 10% 刻意漏接（catchProb=0.90），单回合可能漏 → 改为跨多回合累计 AI 回球数（实测回球 10 次）；
  - `browser-smoke.js`「AI 观战 netInfo 显示」：netInfo 在帧循环内刷新，断言前补跑 2 帧。
- **验证**：`node --check` 全部改动文件；引擎 6 模块 `public/js/engine/*` 与 `src/engine/*` SHA256 镜像一致（两套 assembler 为 UMD/ESM 双变体，升级逻辑均已同步）；`npm run test:all`（net-test 在整套负载下偶发超时、独立运行稳定通过——环境时序抖动，非本次改动）+ `test:seated` 通过；实机浏览器（127.0.0.1:8765）：单击后 ~40ms 内推球挥拍已激活（无延迟）；球在对方半场时双击 → 挥拍类型升级为扣球（type 2）；进箱截图像素分析——球进箱且开启显示时绿色像素 0.123%（绿框），出箱/关闭显示均为 0.000%。


## 二十、2026-08-05 平衡性调整：削弱碰撞箱、AI 刻意低球、速度微调

- **需求**：碰撞箱 1.6×1.2×1.0 → **1.2×0.8×0.7**；困难 AI 有 **1/5** 概率刻意打出低球、地狱 AI 有 **1/2** 概率；蹲伏速度 ×0.55 → **×0.50**；跑动速度**小幅度削弱**。
- **实现**：
  - `engine/rules.js`（双树镜像）：`HITBOX_HX 0.80→0.60`、`HITBOX_HZ 0.60→0.40`、站立箱 `HITBOX_Y_TOP 1.55→1.40`、`HITBOX_Y_BOTTOM 0.55→0.70`（高 1.0→0.7，中心不变 1.05）；蹲箱不变（0.02~1.30，蹲下仍可接任何低球）；`RUN_SPEED_MUL 1.45→1.30`、`CROUCH_SPEED_MUL 0.55→0.50`。HUD 箱体尺寸/左上面板实时跟随。
  - **新增击球类型 3「低平快球」**（AI 刻意低球的载体）：
    - `engine/state.js`（双树）：输入新增 `lp` 字段；
    - `engine.js` 双 assembler：`lpEdge` 触发 `startRallyStroke(type 3)`（仅对打；发球不受影响）；
    - `engine/shots.js`：type 3 = 贴网平击（过网净空 0.8~5cm）、中等上旋、快而平的抽击（~11.4m/s），落点深（对方半台 ~0.99m）、到达对方箱体时低弹（贴近站立箱底 0.70，须准确站位/蹲下更稳）；解不出合法轨迹时退回高吊推球保证命中；
    - `engine/strokes.js`：type 3 挥拍 0.32s、恢复系数 0.50；`characters.js`：type 3 用与扣球相同的平击摆臂动画。
  - `ai.js`：LEVELS 新增 `lowShotProb`（简单/中等 0、困难 0.20、地狱 0.50），`control()` 每球一掷"是否刻意打低球"→ `lp` 输出（与 catch 掷骰同生命周期）。
- **测试**：
  - `physics-boundary-test.js`：9d 文案更新（箱底 0.70）；新增 9j 四条断言——lp 边沿触发 type 3 并击中 / 贴网低飞（净空 0.8~5cm）/ 首落点深（>0.7m 对方半台）/ 到箱体低弹（<0.85m）；
  - `ai-test.js`：新增「刻意低球概率：简单0/中等0/困难0.2/地狱0.5」与「地狱AI 刻意低球」行为断言（回球中 type 3 计数 ≥3）；
  - `engine-stress.js`：高空球扣球用例起始球高 1.45→1.35（新箱顶 1.40，1.45 已不可接）。
- **验证**：`node --check` 全部改动；6 模块镜像 SHA256 一致；六套件（engine-stress / physics-boundary / ai-test / browser-smoke / net-test / seated）全部通过；实机浏览器（127.0.0.1:8765）：HUD 显示 **1.2×0.8×0.7m**；困难 AI 实测 20 次回球中 **4 次低平快球（type 3，=20%）**，与 lowShotProb 0.2 一致。


## 二十一、2026-08-05 扣球强化（低球+快球）与 raw 游戏音乐接入

- **需求**：① 玩家扣球更容易打出**低球和快球**；② 把 **raw 游戏音乐**接入游戏音乐按钮与背景音乐；③ 不做"AI 匹配"（见文末待办），全部改动备份并记入修改记录。
- **① 扣球强化**（`engine/shots.js` 双树镜像，type 2 参数调整）：
  - `padSpeed 9.5 → 10.4`（挥拍更快）、上旋 `f*95 → f*120`（强上旋贴网下压，过网后下坠更狠——更容易造成低球）；
  - 出球速度实测 **20.9 → 22.7 m/s（+8.6%）**；
  - 求解器敏感性：更快/更旋的扣球在低球位与"己方台面弹起球"位会解不出合法轨迹（强上旋下坠+高速掠过台面不落地）——经反复标定，`10.4/120` 在保证解算合法（engine-stress 高空球扣球回合、扣球出球速度用例全绿）的同时最大化提速；`10.8` 会破坏解算（已回退）。
  - **待办（AI 匹配需求，按用户要求本次不做，留给接手者）**：玩家扣球变快变低后，**AI 的应对未调整**——困难/地狱 AI 自身扣球同样受益（同引擎），其接球率/站位/回球选择是否需要匹配玩家更强的扣球（例如 AI 对低球/快球扣球的接球率、是否蹲下应对），由接手者按平衡需求评估。
- **② raw 游戏音乐接入**：
  - 文件来源：微信视频目录 `...\cfd5fb3c34024d843707d28cbfef4244_raw.mp4`（**带视频轨的 mp4 视频容器**）→ 复制为 `public/audio/music.mp4`（3.35MB）；
  - `index.html`：新增 `<audio id="bgmAudio" loop preload="auto">`（隐藏）——`<audio>` 元素可靠播放视频容器 mp4 的音频轨（`decodeAudioData` 无法解码带视频轨文件，已实测回退）；
  - `audio.js`：`loadBGM()`（挂接元素、设 src/loop/音量 0.3，幂等、无 AudioContext 也执行）→ `startMusic()/stopMusic()` 优先走 `<audio>` 播放/暂停，回退合成音乐；`setMuted` 联动元素音量（与合成音乐经 master 静音一致）；新增 `musicMode()`（'raw'/'synth'，供调试/测试）；
  - 音乐按钮（🎵 菜单/比赛页）行为不变：开启 → 播放 raw 音乐，关闭 → 暂停；文件缺失/加载失败 → 合成赛事风格音乐兜底（胶着/赛点加速逻辑仅合成路径生效）。
- **测试**：`browser-smoke.js` 新增 3 条 raw 音乐断言（挂接元素 musicMode=raw / src 指向 music.mp4 / 音乐按钮切换播放↔暂停）。
- **验证**：`node --check` 全部改动；6 模块镜像 SHA256 一致；六套件全部通过；服务器实测：`/` 返回新 HTML（含 bgmAudio）、`/js/audio.js` 为 no-cache 新代码（含 loadBGM）、`/audio/music.mp4` HTTP 200。**实机浏览器点击验证本次受浏览器控制环境故障阻断**（broker 持续"response id mismatch"、locator 点击超时、evaluate 守卫随机拦截）——音乐接入逻辑（挂接/切换/静音联动）由冒烟测试覆盖同一代码路径，用户在浏览器首次交互后即可听到 raw 音乐；如环境恢复可复核 `GameAudio.musicMode() === 'raw'` 与播放状态。

## 十九、2026-08-05 最终融合（乒乓对决 × 乒乓对决_合并 → v1.4.0）

### 融合决策（按用户确认）
- 判定系统：采用合并版**碰撞箱（长方体，进箱即命中）**（HITBOX_HX 0.60 / HZ 0.40 / Z_OFF 0.42 / Y 0.70~1.40；蹲下箱 0.02~1.30）
- 相机：低机位（y4.8）
- 跑步/蹲下参数：1.30 / 0.50
- 保留工作区四项修复：**整身上桌（PLAYER_BODY_W/D + 最近边投影 + padX 同步）、不会上桌、人机条件同步、球不飘（发球球始终在拍前）**
- 其余全部按合并版（含全部新功能）

### 并入的合并版新功能
- 接球碰撞箱 + 进箱状态指示/提示音、第三种击球「低平快球」（AI 困难 1/5、地狱 1/2）
- 扣球强化（更快 ~22m/s、强上旋、贴网下压）
- AI 观战（AI vs AI，红/蓝双难度 + 暂停面板 4 项参数微调）
- AI 漏接率 catchProb + 刻意低球 lowShotProb + 按 (engine,side) 状态
- raw 背景音乐 music.mp4（回退合成）、队色上衣、双击扣球升级（零延迟）
- 工具 sim-match/check-table-shadow/wavinfo、合并说明.md

### 输入改动
- 电脑端：**左键推球 / 右键扣球**（取代双击扣球；左键推球后 280ms 内右键仍可升级为扣球）
- 手机端：左下**全方位摇杆**（可斜向移动）取代四方向键；右下**蹲下按钮**；单击推球 / 双击扣球保留
- 电脑端不显示手机控件，Ctrl 蹲下不变；保留 Ctrl+W 组合键拦截

### 验证
- 全部测试通过：browser-smoke（摇杆/蹲下按钮/右键扣球/升级/Ctrl+W）、physics-boundary（碰撞箱/整身上桌/防飘）、ai-test（条件同步）、engine-stress、net-test、serve-path-online、seated-crowd
- 真实浏览器：低机位渲染、AI 观战完整对局、music.mp4/applause.wav 可加载、摇杆与蹲下按钮可用


## 二十二、2026-08-05 采纳 v1.4.0 为桌面版并发布 GitHub

### 本次动作
- 桌面应用（乒乓对决_合并）全面同步为 **v1.4.0**（外部融合版本，含：整身上桌碰撞 PLAYER_BODY_W/D、右键扣球 + 左键推球（280ms 内可升级）、手机端全方位摇杆 + 蹲下按钮、人机条件同步、球不飘，以及既有全部功能——碰撞箱判定、低平快球、AI 观战、raw 背景音乐等）
- **版本控制**：初始化 Git 仓库（首条提交保留 v1.3.0 桌面版历史），再提交 v1.4.0
- **发布**：GitHub 公开仓库 `ping-pong-duel`（https://github.com/hxj-2888/ping-pong-duel）
- 备份目录 `乒乓对决_合并_备份` 与压缩包 `乒乓对决_合并.zip` 已按规则刷新

### 验证
- 六套件全部通过：engine-stress / physics-boundary / ai-test / browser-smoke / net-test / seated-crowd
- 引擎镜像 `public/js/engine/*` 与 `src/engine/*` SHA256 一致
- 桌面服务（8765）已重启并提供新版本（HTTP 200）


## 二十三、2026-08-05 主页红蓝难度框防出框 + 音乐无缝循环 + 部署 Cloudflare

### ① 主页红/蓝方难度选择框缩进防出框（`public/css/style.css`）
- `.aivai-row`：`flex-wrap: wrap` + `padding: 0 10px`（缩进）；选择框 `max-width:100% / min-width:0 / box-sizing:border-box`
- 效果：窄窗口/卡片宽度不足时红蓝难度框换行而不溢出卡片边框

### ② 游戏音乐：进游戏即可听 + 零间隙无缝循环（`public/js/audio.js`）
- **进游戏即可听**：任意首次交互（点按钮/画布）即 `ensure()` → 挂接 `<audio id="bgmAudio">` 并 `play()`（音乐开关默认开），无需再手动开音乐
- **零间隙无缝循环**：优先 **WebAudio `decodeAudioData`** 解码 mp4 → `AudioBufferSourceNode.loop=true`（样本级无缝，结束零间隙回到开头）；解码失败（带视频轨）自动回退 `<audio loop>` 元素（Chromium 媒体循环亦无缝）；解码成功时自动停掉元素兜底避免双音
- **首尾相连（修断裂感）**：解码后 `trimBgmBuffer()` 裁掉缓冲**开头/结尾近静音**（20ms 窗口 RMS<0.005）——实测原文件**开头有 3.38s 静音**（结尾响亮、无淡出），循环从响亮结尾跳回开头死寂造成"断裂"；裁剪后缓冲以音乐开头（head0_2 RMS 0.29），循环点响亮衔接响亮
- `musicMode()/bgmPath()/bgmInfo()` 调试接口（buffer=WebAudio 无缝 / element=兜底；bgmInfo 报告裁剪量与首尾响度）

### ③ 部署 Cloudflare
- `wrangler pages deploy public`（前端 → ping-pong-duel.pages.dev）+ `wrangler deploy --config wrangler.room.toml`（联机房间 Worker）

### 验证
- 六套件全部通过（engine-stress / physics-boundary / ai-test / browser-smoke / net-test / seated-crowd）；冒烟 raw 音乐断言保持全绿
- 实机（8765）：点击进入人机对战后 `musicMode=raw`、音乐开关开启


## 二十四、2026-08-05 电脑端控制 UI 适配（纯 UI/判定，不删手机端代码）

- **背景**：用户的触摸屏电脑/浏览器被 `isTouch` 误判为触屏，桌面也出现"左下摇杆 / 右下蹲按钮"与手机版操作说明。
- **① 设备判定增强**（`public/js/app/state.js`）：
  - 手机端判定 = "粗指针/触屏事件" **且 窗口宽 ≤1024px**（仅按宽度——现代手机竖/横屏都窄，触摸屏笔记本、IAB 预览等大窗口判定为桌面）；
  - 保留 `?touch=1` 强制手机端，新增 `?desktop=1` 强制桌面端（调试）。
- **② 桌面操作说明补全**（`public/js/app/modes.js` 三处桌面 hintBar 文案）：
  - 人机/联机/本地双人桌面文案补全全部键位：WASD 或 A/D/W/S 移动、Shift 跑步、Ctrl 蹲下（可接贴地球）、↑/↓ 推球/扣球、发球移动鼠标瞄准后单击左键（右键扣球式发球）、对打左键推球 / 右键扣球、Esc 暂停；
  - `if (PPD.isTouch)` 手机端分支**保持不动（未删）**。
- **③ 发球提示设备化**（`public/js/app/hud.js` 发球横幅、`public/js/app/input.js` 发球被挡提示）：
  - 桌面只提"移动鼠标瞄准落点"，触屏才显示"移动鼠标/手指"。
- **不改**：`#touchControls`（摇杆/蹲按钮）DOM 与手机端交互逻辑——全部保留，仅靠判定不再在桌面显示。
- **验证**：`node --check` 4 个文件；browser-smoke 新增 5 条断言（桌面说明含左键/右键/Ctrl 且无摇杆/按钮；设备矩阵——触屏+大窗口=桌面、`?touch=1`=手机端、`?desktop=1`=桌面、手机尺寸+触屏=手机端）共 109 条全过；六套件全绿（net-test 偶发超时为环境抖动、独立运行通过）；实机点击验证受浏览器控制环境故障阻断（broker 点击失灵），设备判定逻辑由冒烟矩阵覆盖。


## 二十五、2026-08-05 修复 AI 观战地狱未解锁 + 通关记录（后端保存 + 主菜单展示）

### ① 修复：通关困难后 AI vs AI 观战的地狱未解锁
- **现象**：人机困难模式获胜提示"已解锁地狱"，主页人机下拉出现"地狱"，但 **AI 观战红/蓝与暂停面板**共 4 个难度下拉的地狱仍是锁定状态。
- **根因**：`unlockHell()` 只解开了主页人机下拉 `#aiLevel` 的 option[3]；其余 4 个下拉（`#aiLevelA/B`、`#pauseAiLevelA/B`）的"地狱 🔒"由 index.html 硬编码 `disabled`，唯一能全量同步的 `syncHellOption()` 只在页面启动时跑一次，解锁后从不重刷。
- **改动**（`public/js/app/state.js`）：抽成 `syncHellOption(sel)` + 新增 `syncHellOptions()`（一次遍历 5 个下拉），`unlockHell()` 改为调用它；`window.PPD` 导出 `syncHellOptions`；`public/js/app/main.js` 启动处 5 个同步调用合并为 `PPD.syncHellOptions()`，`__PPD` 调试句柄暴露 `unlockHell/isHellUnlocked/syncHellOptions`（冒烟测试用）。
- **验证**：browser-smoke 新增断言——未解锁时 5 个下拉 option[3] 全锁定 → `unlockHell()` 后全部解锁且文字"地狱"。

### ② 通关记录：后端保存 + 主菜单展示
- **范围**：人机（ai）模式玩家获胜计入；后端永久保存 + 主菜单"🏆 通关记录"面板显示最近几条。
- **后端两套（API 兼容）**：`POST /api/records`（body `{name, mode, winner, score:[a,b], difficulty, ts}`，非法字段清洗：name 截 20、score 夹 0-99、难度 0-3 默认 1）→ `{ok, id}`；`GET /api/records?limit=N`（最新在前）；CORS `*` + OPTIONS 预检（兼容桌面"公网"跨域）。
  - **Cloudflare**（`src/room.js`）：复用全局 GameRoom DO 单实例 `ctx.storage` 键 `records`（无 TTL，与 rooms 键分离，cap 200），`fetch()` 路由 `/api/*`；`src/index.js` 与 `public/_worker.js` 在静态资源前转发 `/api/*` 到 DO。
  - **本地**（`server.js`）：静态回退前加同路由，`records.json` 落盘（`RECORDS_FILE` 可用环境变量覆盖，供测试用临时文件）。
- **客户端**（新增 `public/js/app/records.js`）：`apiBase()` 复用联机同源/公网判断（本地默认同源、桌面切公网/网页版走 `https://ping-pong-duel.pages.dev`）、`saveRecord()`、`fetchRecords()`、`refreshRecords()`（渲染最近 5 条，失败静默显示"暂无"）。
  - **保存钩子**（`public/js/app/hud.js` 'over' 事件）：`mode==='ai' && e.s===0`（玩家胜）时 `saveRecord({name, mode:'ai', winner:0, score, difficulty, ts})`——困难/地狱获胜时同时解锁地狱（原有逻辑保留）。
  - **主菜单**（`index.html` `#recordsPanel` + CSS；`modes.js` `backToMenu()` 与启动时刷新面板）。
- **验证**：新增 `test/records-test.js`（spawn server.js + 临时 records.json：OPTIONS/非法清洗/limit/静态页不受影响，全过）；browser-smoke 新增 6 条断言（启动 GET 拉取、暂无占位、困难 AI 获胜 POST 内容、同时解锁地狱、返回主菜单刷新）共 61 条全过；六套件全绿（engine-stress / physics-boundary / ai-test / net-test / seated-crowd / browser-smoke）。
- **后续待办（下一处理人）**：`records.json` 已加入 .gitignore（本地运行时数据不入库）；生产部署后 curl 验证线上 `/api/records` 往返。


## 二十六、2026-08-05 页面打开即播：背景音乐加载页面即自动播放

- **需求**：点开页面（主菜单）音乐就得直接放出来，无需再点 🎵。
- **改动**（`public/js/audio.js` + `public/js/app/main.js`）：
  - 新增 `autoplayMusic()`：`ensure()` 后**立即尝试启动**背景音乐；若被浏览器「自动播放策略」拦截（AudioContext 处于 `suspended` 或 `<audio>` 未能播放），在 **第一次用户交互（pointerdown/keydown/touchstart/click，一次性监听）** 上自动恢复出声，用户任意点一下即响，无需再点 🎵；
  - `startMusic()` 的 `<audio>` 分支给 `play()` 的 Promise 挂 `.catch`，避免自动播放被拦截时刷未处理拒绝（console 报错）；
  - `musicTick()` 增加 `ctx.state === 'suspended'` 防护，挂起期不再堆叠音符（合成兜底路径在拦截期不再堆积同一时刻的音符）；
  - `main.js` 启动处调用 `PPD.GameAudio.autoplayMusic()`。
  - 说明：桌面应用（localhost/127.0.0.1）浏览器默认放行自动播放 → 打开即响；网页版（pages.dev）首访可能被拦截，第一次点击立即出声。
- **验证**：browser-smoke 新增 2 条断言（拦截期保持待播 → 模拟首次交互立即恢复播放）共 62 条全过；六套件全绿；`node --check` 通过；引擎镜像 SHA256 一致（audio.js 不属于镜像）。
- **顺带修复**：browser-smoke 的 `<audio>` 桩 `paused` 原用 `Object.assign` 定义 getter 被求值成粘滞数据属性（play/pause 后不变）——改为 `Object.defineProperty` 访问器，使音乐开关断言真正反映播放状态。


## 二十七、2026-08-05 通关记录 DELETE 维护接口 + 清掉线上测试记录

- **需求**：线上 DO 里残留部署验证用的测试记录（「部署验证 · 困难 · 11:7」，id `1785943594839_1033p8`）显示在主菜单面板最近位置，需要清掉。
- **改动**：两套后端同步新增 `DELETE /api/records?id=<id>`（按 id 删除，返回 `{ok, removed}`；无 id → 400；不存在 → removed=0）：
  - `src/room.js`（DO）：DELETE 分支 + CORS `Access-Control-Allow-Methods` 加 `DELETE`；
  - `server.js`：同路由 + CORS 同步。
- **验证**：`test/records-test.js` 新增 4 条断言（按 id 删除 removed=1、删除后少一条、不存在 id removed=0、无 id 400）共 13 条全过；browser-smoke 62 条全过；`node --check` 通过。
- **上线清理**：部署 room worker + pages 后，等旧 DO 实例空闲被替换（Hibernation 约 30s），`curl -X DELETE .../api/records?id=1785943594839_1033p8` → `{ok:true,removed:1}`，`GET /api/records` 返回空数组 —— 线上测试记录已清空。
- **说明**：DELETE 无鉴权（源码公开，常量密钥无实际保护力），轻量榜单可接受；面板渲染端不使用 DELETE。


## 二十八、2026-08-05 左上角判定面板随主页开关整体隐藏 + 地狱通关后解锁人机暂停数值调控

### ① 左上角判定面板：主页关闭开关 → 隐藏全部提示内容
- **需求**：主页「判定范围虚线显示」开关关闭时，左上角判定面板（接球箱尺寸 / 蹲下最低接球 / 实时球高 / 进箱状态）也应整体隐藏——之前只有虚线框与进箱提示音随开关变化，面板始终显示。
- **改动**（`public/js/app/hud.js` `updateHitRangeLive()`）：每帧按 `app.showHitRanges` 同步 `#hitRangeInfo.style.display`（开=`''`、关=`'none'`，仅变化时写入）；虚线框/提示音原有逻辑不变。

### ② 地狱通关 → 人机暂停变「电脑 AI 数值调控」
- **需求**：玩家击败地狱难度后，人机对战暂停面板变成数值调控功能（4 项滑杆：反应/接球率/攻击性/敏捷，即时生效），与 AI 观战暂停面板一致。
- **新增状态**（`public/js/app/state.js`）：`ppd_hell_cleared`（localStorage，与「解锁地狱」区分——解锁=击败困难、**通关=击败地狱**）；`isHellCleared()`/`markHellCleared()` 导出到 PPD。
- **标记**（`public/js/app/hud.js` 'over' 事件）：人机模式在地狱难度（aiLevel===3）玩家获胜 → `markHellCleared()`。
- **暂停面板**（`index.html` 新增 `#pauseAITune` 块 + `input.js` `togglePause()`）：人机暂停且已通关地狱 → 显示 4 项滑杆（`tuneOpp*`，写 `app.aiTuneB`——对手=蓝方）；未通关/本地/观战模式不显示；观战模式显式隐藏避免跨模式残留。
- **生效**（`public/js/app/loop.js` 人机分支）：`AIC.control(side1, aiLevel, aiTuneB)`——暂停中拖滑杆即时生效。
- **接线**（`input.js` `TUNE_SPEC`）：新增 `tuneOpp*` → `aiTuneB` 四项映射；`state.js` ui 注册 4 个新滑杆 id。
- **调试句柄**：`__PPD` 暴露 `isHellCleared/markHellCleared`（冒烟测试用）。
- **验证**：browser-smoke 新增 4 条断言（关开关面板隐藏/恢复；未通关无人机调控、通关地狱后调控块显示、滑杆 ×1.2/×0.5 写入 aiTuneB）共 66 条全过；六套件全绿；`node --check` 通过；引擎镜像 SHA256 一致。
- **待办（下一处理人）**：无。

## 二十九、2026-08-05 加强地狱难度：快球预判 + 数值拉满（保留可战胜）

### 背景（探索结论）
地狱当时最大漏洞：**AI 只能等球进箱后才起手**（hittable 要求 inBox + 0.02s 反应，命中窗在挥拍 0.08s 后才开）→ 球速 >~7-8m/s 时球已飞出箱体，**玩家扣球（22.7m/s）= 白拿分**；另有 12% 刻意漏接（catchProb 0.88）、0.70~1.05m 球带逼 AI 推弱球（smashY 1.05）、发球全难度同一种弱慢发。按用户确认方向：**修快球漏洞 + 数值拉满，catchProb 留 3~5% 保持可战胜**（纯 `public/js/ai.js` 改动，无 engine 模块修改，镜像规则不受影响）。

### ① 快球预判起手（仅地狱 level 3 生效）——最大增强
- 现有 `predictCrossing(b, zc, 1.4)` 已预测球到箱平面 z=zc 的时间/位置/高度；新增"预测可接"判定：`cross.t < 0.14s`（短时窗）且预测交叉高度在箱体 y 范围、预测 x 距球员 < HX → 视为 hittable **提前起手**（挥拍 windup 0.08s 内球入箱，命中窗内命中）——修"球进箱后已来不及起手、高速扣球必漏"的漏洞；保持原 inBox 判定（球已在箱内照常接），catchOk 掷骰仍先于起手（可战胜保留）。
- **蹲下预判**：来球或预测交叉 y < 0.95 时提前蹲（修低球逐帧反应 + 蹲速 0.5× 来不及到位）。
- 困难/中等/简单不变（解锁链"打败困难→解锁地狱"不被破坏）。

### ② 数值拉满（`LEVELS[3]`）
- catchProb 0.88 → **0.95**（刻意漏接 12%→5%，留可战胜空间）、react 0.02 → **0.01**、err 0.015 → **0.01**、smashY 1.05 → **0.95**（缩小 0.70~1.05 弱推球带）、lowShotProb 0.50 保持。

### ③ 发球加速（serve 分支，地狱）
- 地狱发球由 `pu`（慢发）改为 `sm` → 引擎 `startServeStroke(type 2)` 走 fast serve（速度/角度档更高）——实测地狱发球 **5.6 m/s vs 困难 4.6 m/s**，发球即抢攻。

### ④ 扣杀判定高度（探索与取舍）
- 初版把判定高度改为"抛物线近似命中时高度"（`y + vy×0.08 - ½g×0.08²`），想对慢推球（进箱 0.90~0.94m、命中时 0.95~0.98m）也扣杀——**实测灾难**：type 2 扣球对该球况求解失败 → 整拍挥空丢分，sim 困难 vs 地狱从 8:11 崩到 **11:1 困难狂胜**。已撤销，恢复保守判定（crossNear 用预测到箱高度、否则当前高度）：慢推球推回去更稳，快球/高球（cross.y ≥ 0.95）扣杀正常（22m/s 高球命中已验证）。

### 验证（sim-match 量化）
- **困难 vs 地狱：8:11 地狱胜**（强化前 11:4 困难胜——地狱实力明显反超）；击球 79 → **234**、最长回合 14 → **52**；地狱 vs 地狱击球 229、最长回合 33、抢七 10:12——长回合、强对抗。
- **非地狱档位零变化**：困难 vs 困难 13:11 与强化前逐帧一致（改动全部包在 `hell` 分支内）。
- **ai-test 2c 新增断言全绿**：LEVELS[3] 数值（catch 0.95/react 0.01/smashY 0.95/err 0.01）；**地狱接住 22m/s 快球**（高球 1.35 站立 + 低球 0.95 蹲下，困难接不住——W1 修复直接验证）；**地狱快速发球快于困难**（5.6 vs 4.6 m/s，断言对比式）。
- **ai-test 2b 窗口修正**：低球掷骰每球一次（对照实验 lowShotProb=1 → 24/24 全低球、=0 → 0 低球，逻辑正确），12 回球窗口因确定性 rng 序列偶发偏低 → 扩至 48 回球（实测 44% ≈ 50%）。
- 六套件（engine-stress / physics-boundary / ai-test / browser-smoke / net-test / seated-crowd）全绿；`node --check` 通过；6 模块镜像字节一致。

### 二十一遗留待办更新
- 二十一待办"**AI 的应对未调整**（玩家扣球变快变低后）"——**地狱已解决**：快球预判 + 蹲下预判让地狱能接住 22m/s 高球与低球（不再白拿分）；**困难/中等按设计不预判**（保持档位差距与解锁链）；AI 自身扣球受益同引擎（共享），未额外改动。
- **待办（下一处理人）**：无。


## 三十、2026-08-06 修复：桌面应用重启后通关记录丢失（旧版服务器被复用）

- **现象**：用户通关后重开桌面应用，主菜单「🏆 通关记录」显示"暂无"。
- **根因**：`desktop-launcher.js` 发现 8765 端口已有本游戏服务器就**直接复用**（`alreadyRunning=true`），且关闭窗口只杀**自己拉起的**服务器进程。若某次会话遗留一个**旧版本 `server.js` 进程**（早于记录功能，无 `/api/records` 路由），之后每次打开应用都被复用 → 前端（静态文件按请求从磁盘读，总是新的）照常展示记录面板，但 `POST /api/records` 落到旧服务器 → 静态回退 404 → `saveRecord` 静默失败 → **记录从未保存**。实测当时 8765 上正是旧进程：`/api/records` 404、`/js/app/records.js` 200。
- **改动**（`desktop-launcher.js`）：
  - 新增 `isCurrentGame(port)`：探测 `/api/records` 是否 200（当前版 server.js 才有该路由，旧版 404）——识别"本游戏但版本过旧"的服务器；
  - 新增 `killPort(port)`（netstat 找 PID + taskkill）与 `waitPortFree(port)`（轮询端口释放）；
  - 启动逻辑：8765 已有本游戏 → 当前版本则复用；**版本过旧则杀掉并重启新版**（保证记录功能可用）；被其他程序占用仍走空闲端口。
- **验证**：杀掉旧进程、用当前 server.js 重启 8765 → `/api/records` 返回 `{ok:true,records:[]}`；新增 `test/restart-persist.js`（POST → 杀进程 → 重启 → GET 记录仍在，模拟"重开桌面应用"）全过；`node --check` 通过；六套件不涉及（无 public/ 改动，镜像不受影响）。
- **待办（下一处理人）**：无。


## 三十一、2026-08-06 设置面板重构（右上角 ⚙）+ 判定虚线默认关闭 + 记录规范后端

### ① 设置面板：指示 / 音乐 / 音效统一收进右上角 ⚙
- **需求**：去掉原主页「判定范围虚线显示」勾选框与 🔊/🎵 音乐按钮、比赛页 🎵 按钮；新增「设置」面板，主页与比赛页右上角都有 ⚙ 按钮打开；设置内含 **判定范围虚线 / 背景音乐 / 游戏音效** 三个开关。
- **改动**：
  - `index.html`：主页 `#btnSettings`（fixed 右上角）、比赛页 `gameTools` 内 `#btnSettingsGame`；删除 `#showHitRanges` 行、`#btnMute`、`#btnMusic`、`#btnMusicGame`；新增顶层 `#settingsPanel` 弹层（`#setShowHitRanges` / `#setMusic` / `#setSound` / `#btnSettingsClose`）。
  - `state.js`：ui 引用更新；**判定范围虚线默认关闭**（`showHitRanges` 默认 false，localStorage 记忆，设置面板开关）。
  - `main.js`：`openSettings()/closeSettings()`（打开时同步三个开关状态），三个开关 change 即生效并持久化（虚线 → `ppd_show_hit_ranges`；音乐/音效 → `audio.js` 内部持久化 `ppd_music_on`/`ppd_sound_on`）；移除旧按钮接线。
  - `audio.js`：`musicOn`/`muted` 初始值从 localStorage 恢复，`setMusicOn`/`setMuted` 写回（设置跨会话记忆）。
  - `input.js`：Esc 在设置面板打开时优先关设置，再考虑暂停。
  - **局内可关**：比赛页右上角 ⚙ 随时打开设置关闭虚线，`hud.js` 每帧按 `app.showHitRanges` 隐藏判定面板、`updateHitRangeLive` 门控提示音（原有逻辑不变，默认关后自动全隐藏）。
- **验证**：browser-smoke 重构为设置面板断言（默认关、设置开/关立即生效、面板显隐、音乐/音效开关切换、设置面板开合）共 72 条全过；六套件全绿；镜像一致。

### ② 记录规范后端（确保桌面端记录生效）
- **根因补充**：除三十的"旧服务器被复用"外，`records.js` 的 `apiBase()` 原本跟随"联机:公网"切换——桌面切公网时记录 POST 到 pages.dev（可能失败/网络不通），重开应用 `publicServer` 复位读本地 → 面板显示"暂无"，用户以为记录没保存。
- **改动**：`records.js` `apiBase()` 一律返回同源（桌面=本地 server.js records.json，必然可达；网页=pages.dev DO），不再跟随公网切换；`server.js` `saveRecords()` 改**原子写**（先写 `.tmp` 再 rename，防中断写坏 records.json）。
- **验证**：`records-test.js` 与 `restart-persist.js` 全过（原子写路径同逻辑）；8765 端到端 POST/GET/DELETE 通过。
- **待办（下一处理人）**：无。


## 三十二、2026-08-06 修复：重进桌面应用后地狱/AI 观战重新上锁（本地记忆丢失）

- **现象**：用户通关困难/地狱后，重开桌面应用，地狱难度与 AI 观战又上锁；判定虚线/音乐/音效等设置也复位。
- **根因**：`desktop-launcher.js` 用 `mkdtempSync` 给 Edge 建**临时浏览器配置目录**，关闭窗口时 `rmSync` 删除——地狱解锁（`ppd_hell_unlocked`）、地狱通关（`ppd_hell_cleared`）、判定虚线、音乐/音效等 **localStorage 每次启动都被清空**，启动时 `isHellUnlocked()` 恒为 false → 全部上锁。
- **改动（双保险）**：
  1. **持久化浏览器配置**（`desktop-launcher.js`）：改用固定配置目录 `%LOCALAPPDATA%\PingPongDuel\edge-profile`（`PP_PROFILE` 可覆盖），退出时**不再删除**；顺带清理旧的 `ppd-app-*` 临时目录残留。→ 所有 localStorage 记忆（解锁/通关/虚线/音乐/音效）跨会话保留。
  2. **解锁判定从持久化记录推导**（`public/js/app/records.js` 新增 `syncUnlocksFromRecords()`，`main.js` 启动调用）：拉取后端记录——人机获胜且难度≥困难 → `unlockHell()`（全量同步 5 个难度下拉），=地狱 → `markHellCleared()`。即使浏览器存储被清空（清缓存/换浏览器），只要 records.json / Cloudflare DO 里还有获胜记录，地狱与 AI 观战就不会上锁。
- **验证**：browser-smoke 新增 4 条断言（启动时 localStorage 被清未解锁 → 记录含困难获胜=解锁、含地狱获胜=通关、5 个下拉全部可用）共 74 条全过；六套件全绿（net-test 批量偶发超时为已知环境抖动，独立运行通过）；`node --check` 通过；镜像一致。
- **注意**：修复前的解锁记录已随旧临时配置删除，无法找回——**重新通关一次困难**即永久解锁（记录会持久化在 records.json）。
- **待办（下一处理人）**：无。


## 三十三、2026-08-06 音乐与音效完全独立

- **需求**：设置里「背景音乐」与「游戏音效」两个开关应完全独立——关音效不能把音乐一起静音，关音乐不能影响音效。
- **根因**：WebAudio 路径 `musicGain.connect(master)`——音乐接在音效总线上，`setMuted(true)` 把 `master.gain` 归零时音乐一起静音；`<audio>` 元素路径 `setMuted` 里 `bgmEl.volume = 0` 同样把 raw 音乐静音。
- **改动**（`public/js/audio.js`）：
  - `musicGain.connect(ctx.destination)` **直连输出**，不再经过 `master`（音效总线）——音乐只受「背景音乐」开关（musicOn/musicGain）控制；
  - `setMuted()` 只静音 `master`（游戏音效），**删除** `bgmEl.volume` 归零逻辑——`<audio>` 元素音乐不再跟随音效开关；
  - 顺带一致性：WebAudio 音乐音量 0.3 与 `<audio>` 元素 0.3 统一（此前 WebAudio 路径经 master 0.5 衰减实际 0.15）。
- **验证**：browser-smoke 新增 2 条独立性断言（关音效后音乐照常播放且音量不变 0.3；关音乐不影响音效开关）共 76 条全过；六套件全绿；镜像一致；README/使用说明同步"完全独立"。
- **待办（下一处理人）**：无。


## 三十四、2026-08-06 设置面板：音乐 / 音效音量调节滑杆

- **需求**：设置里「背景音乐」与「游戏音效」在独立开关之外，再各加一个**音量滑杆**（0~100%）。
- **改动**：
  - `public/js/audio.js`：新增 `musicVol`（默认 0.3）/ `sfxVol`（默认 0.5）音量状态（localStorage 持久化 `ppd_music_vol`/`ppd_sfx_vol`）；`setMusicVol()`/`setSfxVol()`（即时生效：WebAudio 总线 + `<audio>` 元素音量 / master 总线），导出 `getMusicVol()/getSfxVol()`；新增 `applyMusicGain()` 统一按「开关 + 音量 + 紧张强度」刷新音乐增益——`setMusicOn`/`setIntensity`/`ensure`/`loadBGM`/`setMuted` 全部改走音量值（紧张强度在用户音量基础上按比例抬升）。
  - `index.html` 设置面板：新增「音乐音量」「音效音量」两个滑杆（`#setMusicVol`/`#setSfxVol`，复用 tune-row 样式带百分比标签）。
  - `state.js` ui 注册两个滑杆；`main.js` `openSettings()` 同步滑杆值与标签、`input/change` 接线（拖动即生效）。
- **验证**：browser-smoke 新增 5 条断言（默认 30%/50%、调到 70%/20% 生效、两音量互不影响）全绿（0 FAIL）；六套件全绿；镜像一致；README/使用说明同步。
- **待办（下一处理人）**：无。

## 三十五、2026-08-06 扣杀机制完善 + 人机能力分层（漏球<3%，差距靠技巧）

### 需求
1. 人机与玩家都能打出扣杀球并**反击扣杀球**（一定角度内蹲下+推球可反击）。
2. 蹲下速度 0.5→0.4，蹲得越久越慢（最低 0.2）；**蹲↔站初始瞬发，3 秒内反复蹲站会加大转换延迟（每次 +0.15s，最多 0.5s）**，写入界面下方提示。
3. 人机变招：对打两回合不进球后可能改变击球方式并横移 0.4~0.7m，概率随难度/数值/未变招次数累积（封顶 100%，变招后清空）；中等(数值=1)基础 20%。
4. 判断是否可扣杀加入**指示提示**（左上角面板"可扣杀 ✓/暂不可"）。
5. 人机卡在半场时**自动向本方半场驱逐回位**。
6. 人机判定无法扣杀时放弃扣杀：困难 80%、地狱 100%（降低失误）。
7. 平衡逻辑改为：**困难/地狱刻意漏球均 <3%，差距只靠时机把控与能力技巧**（地狱有高吊、刁钻方向射球、扣杀等）。

### 实现
- **扣杀可解性（根因）**：AI 对打回球接触高度 0.83~0.99m，旧 smashY 1.05/1.10 从不触发扣杀；且 type2 无求解兜底 → 扣杀决策=挥空送分。修复：
  - shots.js computeShot（双树镜像）：type2 求解失败逐级降级（减力扣 8.0/sp80 → 推球），永不落空；返回 degraded 标志供"能否真扣杀"判定。
  - 扣杀判定用**预测命中高度**（风起 0.08s 后抛物线近似）替代起手瞬间球高；smashY 降到真扣杀可解高度 ~1.00m。
  - **高吊球（lb 输入）**：新增 AI 专用高净空弧线（过网净空 0.55~1.3m），喂出 ≥1.0m 高球制造真扣杀；蹲下防守路径保持低净空不变（低球救球不受影响）。
  - **接扣杀预判起拍**：扣杀来球未进箱但预测到达 ≤0.18s 时提前起拍（命中窗罩住球进箱时段），否则 21m/s 球 0.038s 穿过箱体、风起后早已飞出；接扣杀成功率 = 档位 smashDef × 接球率微调，封顶 50%（仅困难/地狱）。
- **蹲下**：CROUCH_SPEED_MUL 0.5→0.4，蹲满 2s 衰减到 0.2；crouch 改为 0~1 连续值（箱体/速度插值）；蹲↔站转换延迟初始 0、3 秒内反复蹲站每次 +0.15s 封顶 0.5s（引擎层，玩家与人机一致）；服务端/镜像同步。
- **变招（ai.js）**：trickBase（简单0/中等0.20/困难0.24/地狱0.28），实际概率 = 基准 × 数值因子(四个微调倍率均值) + 未变招回合累积(+0.10/回合)，封顶 100%，变招后清空；变招回球在低平快球/高吊球中二选一，并横移 0.4~0.7m（1.2s）。
- **可扣杀指示**：HUD 左上角面板新增"扣杀"行，节流 0.12s 用 computeShot(2) 判定（不降级=可扣杀 ✓）。
- **半场驱逐**：人机卡在网前禁区或远离基础位（>0.9m）且球不在本方超 0.8s → 强制向本方半场回位。
- **无法扣杀放弃**：failSkip（中等0.5/困难0.8/地狱1.0）——判定扣杀求解会降级时按概率改打安全球。
- **刁钻方向射球**：新增 aimBias（引擎 p.aimBias，双树镜像）——人机按 trickyProb（简单0/中等0.15/困难0.25/地狱0.40）把回球打向对方反方向/边角（±0.35~0.7），逼对方追球。
- **发球稳定性修复**（根治 LET 触网风暴）：发球轨迹对站位 z 极敏感——服务器站稳（|z-base|<0.05 且 |vz|<0.05）才发球；发球挥拍期间冻结移动（残存 vz 惯性也会漂移发射点）；修复发球阶段回位方向（乘 facing，原对 side1 反了）；发球缓存 key 粒度加密（H.z×16/H.y×8/H.x×8）。
- **平衡参数（ai.js LEVELS）**：困难/地狱刻意漏球均 <3%（困难 0.975、地狱 0.98），差距靠 react/err 时机、smashDef（0.3/0.5）、lowShotProb（0.2/0.5）、lobProb（0.15/0.12）、trickyProb（0.25/0.40）、trickBase（0.24/0.28）；中等 react 0.12/catch 0.86/smash 0.70。

### 验证
- node test/ai-test.js：15 项全过（新增 smashDef/变招/failSkip/蹲速配置/扣杀兜底/玩家反击扣杀）。
- node test/browser-smoke.js：全过（蹲站延迟断言已适配转换延迟）。
- 模拟（多种子平均）：目标对阵结果见修改记录 二十二 附注（以最终跑数为准）；玩家反击扣杀：困难AI扣杀 9 次玩家接住 3（33%）、地狱 6 次接住 4（67%）——蹲下+推球+预判起拍可反击。
- 引擎镜像 SHA256 一致（public/js/engine/* = src/engine/*）。
- 备份流程：node tools/backup.js [备注]（每次修改后执行，备份到 backups/<时间戳>-<备注>/）。

### 三十五·附注（最终平衡数字，12 种子平均，每对阵 ≥100 次发球数据量）
- 地狱 vs 困难：**11.3:7.6**（地狱胜率 ~60%）——地狱明显强于困难，但未达 11:5（69%）。
- 困难 vs 中等：**11.0:4.2**（困难胜率 ~72%）——困难明显强于中等，未达 11:6（65%）。
- 说明：按"困难/地狱刻意漏球均 <3%、差距只靠时机把控与能力技巧"约束，两点因素使目标无法完全达到——
  ① 时机性漏接（快球/蹲站转换）占比大且两侧近乎对称，稀释了技巧差距；
  ② 中等 smashDef=0（按"仅困难/地狱可应对扣杀"），困难扣杀对中等近乎必得分。
  最终档位阶梯（简单<中等<困难<地狱）与"地狱最强、仍可战胜"均成立。


### 三十五·附注2（回归修复 + 复验，2026-08-05）
**回归发现**：box 判箱系统改造后，挥拍起拍延迟（windup 0.08s）使扣杀接触点从原始的 1.03m 压到 0.93m——
该高度仅高于网顶 1.75cm，紧贴净空带 [0.006,0.08] 的快速扣杀几何上解不出（即使放宽净空带到 0.30/0.50 仍无解，
球太快会飞出球台尾端），computeShot 一路降级成 4.07m/s 软推 → engine-stress「扣球质量 ≥6.0」回归失败。

**修复（strokes.js，双树镜像）**：判箱窗口对扣杀(type2)从挥拍开始(t=0)即生效——球已入箱就按入箱瞬间
的高度击球；推球/低平保留起拍延迟（挥拍蓄力动画与时机感）。扣杀接触回到 1.03m、出球 22.9m/s；
推球与全局对打时机不变，平衡不受扰动。

**复验（12 种子，当前代码）**：
- 地狱 vs 困难：**11.6:7.5**（地狱胜率 ~61%，12 局全胜）；困难扣杀 2~5/局，地狱接扣杀成功 ~38%（≤50% 上限）；
  地狱因 failSkip=100% + 低平球优先(50%)，只在绝对可扣时出手，故自身扣杀稀少（0~1/局）——保守策略属设计使然。
- 困难 vs 中等：**11.0:3.1**（困难胜率 ~78%）；双方都有扣杀（1~8/局）；中等对困难扣杀 0/43=0%（smashDef=0 精确生效），
  困难接中等扣杀 ~28%（smashDef 0.30）。
- 全量 test:all 187 断言全过、0 失败；LET=0；引擎镜像 SHA256 一致。

## 三十六、2026-08-06 反击扣杀实测 + 强化地狱扣杀 + 观战/人机小差异

### 需求
1. 实测玩家反击扣杀的实际成功率，纠正文档指引。
2. 强化地狱扣杀能力；允许 AI 观战与人机对战的人机强度有小差异。
3. 维持现有玩家收益链（通关记录 / 战胜困难解锁地狱 / 战胜地狱解锁 AI 数值调控）。

### 实测结论（脚本化玩家，8~12 种子）
- 反击扣杀（站立 + 预判起拍 + 推球）：困难 AI 扣杀接住 ~65%、地狱 ~45%，**接住即合法回球**（0 下网/0 出界）。
- **预判起拍是命门**：球到前 0.18s 提前按推球 → 62~65%；只在球进箱才按 → **0%**（21m/s 扣杀 0.03s 就穿过箱体）。
- **蹲下在这类扣杀上反而有害**：扣杀到达玩家高度平均 0.92m、无低于站立箱底 0.70m → 蹲下不增加覆盖；蹲下把箱顶 1.40→1.30 漏掉高位来球（强制蹲成功率 困难 51%/地狱 23% vs 站立 64%/53%）；且真游戏蹲下+推球会转高吊，对快球更易打空。文档由「蹲下+推球反击」更正为「预判起拍+推球（站立即可），贴地低扣才需蹲下」。

### 实现
- **B（引擎，双镜像）**：扣杀来球（ball.hitType===2）时 lb 输入不生效——蹲下+推球保持蹲防推球（反击），不转高吊；普通来球的进攻性高吊不变。
- **决策顺序（ai.js）**：可扣杀优先于低平快球/高吊——球够高就扣杀，低球才用技巧。地狱对可扣高球扣杀率 36%→100%。
- **强化地狱（ai.js LEVELS）**：smashY 1.00→0.95（真实对局扣杀 0→2~7/局）、failSkip 1.0→0.9（10% 冒险出手降级扣杀）、catchProb 0.98→1.0（0% 刻意漏球）；catchProb clamp 上限 0.99→1.0。
- **观战/人机小差异**：LEVELS 地狱 catchProb 保持 1.0（观战强版）；人机对战 loop.js 传 hellCatchMul=0.97（仅地狱生效，3% 刻意漏球，高手可战胜）；暂停面板 catchMul 滑杆可覆盖。
- **测试基建修复**：net-test 原 process.exit 跳过 finally → 每次运行泄漏服务器进程，后续运行连到旧服务器串扰（曾致「无回球时发球方得分」假失败）；改为记录 exitCode、finally 先 kill 再退出。

### 验证
- test:all 188 断言全过（新增 §5：地狱 catchProb=1.0 / smashY=0.95 / failSkip=0.9）；引擎镜像 SHA256 一致。
- 平衡 12 种子：**地狱vs困难 11.0:5.3**（地狱 68%，命中 11:5 目标；地狱扣杀 2~7/局）、困难vs中等 11.0:3.4。
- 玩家胜率（脚本完美玩家，追球+蹲防+反击）：困难 2/8（25%）；地狱观战版 0/8（0:11~4:11），人机版（catch 0.97）0/8 但比分拉近至 5:11~9:11（真实玩家可进攻取胜）。


## 二十三、2026-08-06 手机端流畅度优化（渲染/观众席/HUD/画质）

### 需求
低端手机全速渲染(120Hz 屏无上限)、观众席 ~380 火柴人每帧全量重画(5,600+ 路径/帧)、
无 DPR(模糊)、HUD 每帧 7+ 处 DOM 写、常显 backdrop blur、暂停/结算屏仍全量渲染。

### 实现（仅 public/ 前端，引擎零改动，镜像规则不涉及）
- **观众席离屏缓存（最大头）**：相机 ±0.62m 死区 → 多数帧静止；静态层（地板/看台/静止观众）
  预渲染进离屏 canvas，键=(相机 x 桶 0.04, 视口, DPR)，相机平移超桶/尺寸/DPR 变化才重建，
  否则每帧 1 次 drawImage blit；得分后 1.7s 欢呼/摇头观众由动画层动态叠加；
  无 document.createElement 环境（测试桩）自动回退逐帧直画。
- **60fps 渲染门控 + FPS 滚动均值**：物理仍 120Hz 步进，渲染最多 60fps（120Hz 屏省 GPU/发热）；
  帧间隔滚动均值供自动降级。
- **HUD 脏检查 + 节流**：hudP1/P2、比分、netInfo、serveDot、球高/进箱变化才写 DOM；
  updateHitRangeLive 节流 ~15Hz；音乐紧张度随比分变化才更新；主菜单跳过 HUD。
- **暂停/结算停渲染**：暂停物理冻结即不再渲染（画布保留末帧）；'over' 阶段只步进重开计时不渲染；
  窗口/DPR 变化补一帧（resizeDirty）避免画布空白。
- **DPR 清晰度**：resize 按 min(devicePixelRatio, 低画质?1:2) 设画布 + setTransform，坐标仍 CSS 像素。
- **画质系统**：主菜单「画质」select（自动/低，localStorage 记忆）；低画质=跳过观众席/看台/尾影 +
  DPR=1 + 临时关虚线（不改用户勾选）；自动=帧均值 <40fps 持续 2s 降级、>55fps 持续 2s 还原（滞回防抖）。
- **触屏去 blur**：.touch 下 #hud/#hitRangeInfo 去掉 backdrop-filter（桌面保留）。
- **掌声合成分帧**：~200 次鼓掌 × 7 节点先收集调度表，再每 tick 40 个分帧创建（when 绝对时间，时序不变）。

### 验证
- 全量 test:all 194 断言全过、0 失败（新增画质开关写回/生效标记、DPR 上限 2↔1、低画质视图模型 low 且
  虚线临时关闭且不改用户勾选、观众席直画回退不抛错）。
- 实机帧耗时对比需浏览器环境；无 broker 时以冒烟 + 代码审查为准。


## 三十七、2026-08-06 画质档位(高/低) + 帧率上限(30/45/60) + 右上角估测帧数

### 需求
1. 画质只分高/低两档，默认高画质（去掉"自动"档）。
2. 帧率上限可选 30/45/60（默认 60；物理仍 120Hz 步进，只限渲染频率）。
3. 游戏右上角实时显示估测帧数（上限 60，低于 45 变黄提示）。

### 实现
- state.js：quality.mode 改为 high/low（默认 high）；新增 quality.frameRate（30/45/60，默认 60），localStorage 记忆（ppd_quality / ppd_frame_rate）；新增 setFrameRate；setQuality 只处理 high/low。
- loop.js：移除"自动"降级逻辑（autoQuality）；渲染门控改为按所选帧率（1000/frameRate）；帧间隔滚动均值（约 1s 窗口）驱动右上角 fpsMeter（约 5 次/秒刷新，min(60, 1000/avg)，低于 45 加 low 类变黄）。
- index.html：画质选择器（高画质/低画质）+ 帧率选择器（60/45/30）；游戏右上角 gameTools 内新增 #fpsMeter。
- css：.fps-meter 样式（绿色数字，low 时黄色）。
- main.js：画质切换监听（高/低状态提示）+ 帧率切换监听。
- app/render.js：low 标记（低画质跳过观众席/看台/尾影）不变，随 quality.low 生效。

### 验证
- 开发版 test:all 198 断言全过（新增：默认高画质 DPR=2、帧率默认 60、切低 DPR→1、切高恢复、帧率 30/60 切换）。
- 桌面版 test:all 238 断言全过（含新增画质/帧率断言 + devicePixelRatio=2 沙箱）。

## 三十八、帧率上限新增「无上限」选项（2026-08-05）
### 变更
- 帧率上限新增「无上限」档（下拉框第 4 项，value=unlimited）：渲染不做帧间隔门控，每帧 RAF 都渲染（跟随显示器刷新率，60/120/144Hz 均满帧）；物理仍 120Hz 步进不变。
- state.js：frameRate 支持 'unlimited'（localStorage 记忆，初始化读取兼容该字符串）。
- loop.js：shouldRender 在 unlimited 时恒真（renderDt<=0 的测试环境放行逻辑不变）。
- main.js：帧率切换监听对 'unlimited' 字符串处理；状态提示显示「无上限」。
- index.html：帧率选择器新增 `<option value="unlimited">无上限</option>`。
### 说明
- 右上角估测帧数仍封顶显示 60（沿用此前需求）；无上限时高刷屏实际帧率可更高，仅显示 60。
- 无上限更耗 GPU（每帧重绘），高刷屏/低端机可退回 60/45/30 档。
### 验证
- 开发版 test:all 200 断言全过（新增：切无上限生效、无上限渲染 8 帧无异常、切回 60）。
- 桌面版 test:all 240 断言全过（同上新增断言）。

## 三十九、右键扣杀重构 + AI 应对定标 + 观众动作修复 + 真实帧率 + 局内难度锁定（2026-08-05）
### 1. 右键扣杀重构：只出快扣杀，不可过网=撞网（引擎）
- shots.js：type2 移除「减力扣/推球」降级链；快扣求解无解 → 返回 `{ netHit: true }` 标记。
- strokes.js：type2 遇 netHit → 挥拍命中但把球打进网（低平朝网速度，抛物线控制过网面高度 < 网顶）→ 物理 net 事件 → 判对方得分。
- ai.js smashAttemptAllowed / hud.js canSmashNow 改用 `!shot.netHit` 判定可扣杀；failSkip 保留（困难 0.8 / 地狱 0.9，冒险出手 10-20% = 撞网失分）。
- 效果：右键不再产出慢速降级球；低球右键=撞网判负（配合「可扣杀 ✓」指示）；玩家反击扣杀实测 ~47-48%（AI 只出真快扣，更难接）。
### 2. AI 对玩家真扣杀应对：位置门 × 概率 + 定标
- 机制：扣杀来球 |落点-站位| ≤ 0.35m 且高度/时序成立（预判起拍/进箱同门槛）→ 再按 smashDef 概率掷骰；扣杀来球回守本位（站深，让深落点反弹穿箱）；引擎推球接扣杀兜底（chip，消挥空）。
- 常数：困难 smashDef 0.55 / 地狱 0.95（等级递增）；实测有效反击率 困难 ~50% / 地狱 ~80%（探针 tools/smash-defense-probe.js）。
- 普通球「刻意漏球」计数化：固定间隔必漏（困难 0.975 → 每 40 球、地狱 0.97 → 每 33 球、观战地狱 1.0 → 永不），平均概率不变、方差大降。
### 3. 观众动作 bug 修复（render.js）
- 根因：drawPerson 守卫 `(cheer && cheer[0] != null) ? cheer : 0` 把标量欢呼值归零 → 观众永远静止（adbbd20 引入，双树同病）。
- 修复：`const c = cheer || 0; const sh = shake || 0;`；loop.js over 阶段 fan 动画未平息时放行渲染（决胜欢呼可见）。
- 回归：seated-crowd-test 补 drawPerson 全路径断言（cheer=1 手高于肩）。
### 4. 无上限真实帧率
- loop.js：帧率档=unlimited 时 `Math.round(1000/avg)` 不封顶（60/45/30 档仍封顶 60）；#fpsMeter title 更新。
### 5. 人机局内禁切难度
- 移除 #btnDiff（index.html/input.js/state.js）：人机难度开局菜单锁定；AI 观战暂停面板难度/参数、地狱数值调控保留。
### 6. 复验与测试
- 玩家胜率（脚本化玩家 8 局）：困难 2/8、地狱 1/8（可战胜但极难）。
- sim-match 12 种子：地狱vs困难 11.0:8.7、困难vs中等 11.0:5.7、困难vs简单 11:0（等级秩序保持）。
- dev test:all 204 断言全绿、desk test:all 242 断言全绿；引擎镜像 SHA256 双树一致；ai.js 双树一致。
- 新增断言：低球右键=撞网判负、扣杀求解 低接触=netHit/高接触=快扣、无上限真实帧率>60、局内难度按钮已移除、drawPerson 欢呼手高于肩。

## 四十、手机端分辨率/扣球按键 + 联机部署修复（2026-08-06）
### 1. 手机端画面分辨率对齐电脑端
- state.js resize：高画质 DPR 上限 触屏设备 3（电脑仍 2）——手机 DPR 常见 3，此前被压到原生 2/3 显模糊；手机默认高画质即生效；低画质仍 1。
### 2. 手机端扣球按键 + 去掉双击扣球
- index.html 触控区新增「扣」按钮（btnSmash，右下「蹲」上方）；input.js 单按=扣球（fireShot sm，人机/本地=红方/P1、联机=自己）。
- 移除双击扣球：对打（触屏）单击固定推球；双击判定（DOUBLE_TAP_MS）删除。
- 使用说明/README 手机操作段同步更新。
### 3. 引擎加载器改静态 require（联机 DO 可部署）
- engine.js 的 loadPart 用变量 require(nodePath) 无法被 Workers 打包器解析 → game-room DO 部署失败（既有问题）。
- 改为 Node 静态字面量 require 列表（浏览器仍走全局工厂），行为不变；四份镜像同步。
### 4. 联机发球测试缺陷修复（do-smoke/ws-live）
- 根因：测试把球员移动到台边（x≈±1.53）后直接发球——台边站位解不出合法发球（服务端阻止），非游戏 bug、非本次回归（969507f 同样 FAIL）。
- 修复：发球前回中（与 net-test 同法）；ws-live 对公网端点全绿（创建/加入/移动/发球/ping/退出）。
### 5. 部署
- GitHub：master 推送至 hxj-2888/ping-pong-duel（ab78732..0afc2f7）。
- Cloudflare：game-room DO Worker 部署成功（Version 58e8e4f5，https://game-room.hexiangjie694.workers.dev）；Pages 生产部署 db7b9dd3（main 分支）→ 根域名 ping-pong-duel.pages.dev 已上新内容（扣球按钮/无上限帧率/帧率表，局内难度按钮已移除）。
- 联机验证：新 DO 下 ws-live 12/12 全绿。

## 四十一、手机端掉帧修复：观众欢呼动画 30Hz 烘焙进缓存（2026-08-06）

### 1. 掉帧定位（浏览器实测）
- 主因：得分后 ~1.7s 内 `drawCrowdAnimated` 每帧全量重绘 ~376 名观众叠加在离屏缓存上。实测（1280×720 高画质/无上限帧率）：`fan.cheer>0` 时 FPS 165→86~137，cheer 归零即恢复 165，逐采样相关性 100%；低画质（无观众）全程 165。
- 手机放大：高画质 DPR3 画布 ≈ 2.96M 像素（约测试窗口 3.2×），且移动 GPU 更弱 → 每得一分就卡顿 ~1.7s。

### 2. 修复（render.js 双树镜像）
- 欢呼/摇头动画改按 **30Hz 烘焙进离屏缓存**（drawFloor 内 animDue 判定 + 浮点容差），帧间隔内只 drawImage blit、不逐帧重绘；动画开始/结束即时重建（回静止位姿）。删除 `drawCrowdAnimated` 每帧叠加调用。
- 删每帧冗余 `drawFloorBg`（缓存含不透明背景，blit 已覆盖）。
- `CROWD_CAM_BUCKET` 0.04→0.06m（平移重建阈值 0.02→0.03m，重建频率降约 1/3，视觉不可察）。
- 低画质 / 无缓存环境（测试桩）回退逐帧直画路径不变。

### 3. 验证
- 浏览器实测（同环境）：得分欢呼期间 FPS 稳定 165，不再掉帧。
- 新测试 `test/crowd-anim-cache-test.js`（16 断言）：静止帧零全量重绘、动画 60 帧烘焙 30 次（30Hz）、动画起止即时重建、相机过桶才重建、低画质/无缓存回退。

### 4. 文案修正
- 手机提示「双击扣球」→「扣球键扣球」（双击扣球早已移除，input.js/modes.js 双树）；engine.js「推球挥拍中按扣球升级为扣杀」注释为存活机制，保留。

### 5. 测试
- dev test:all 219 PASS / desk test:all 259 PASS 全绿（新增 16 断言）；render.js 双树 SHA256 一致。

### 6. 二次优化：静态/动画双层离屏缓存分离（同日）
- 30Hz 烘焙后单次重建仍需全量重画 ~376 观众 + 地板/看台，手机 DPR3 下单次重建仍可能 10ms+。再拆分：
  - **静态层**（地板/看台/静止观众，全分辨率）：相机过桶/尺寸/DPR 变化才重建，每帧 1 次 blit；
  - **动画层**（欢呼/摇头观众，**半分辨率 CROWD_ANIM_SCALE=0.5 + 快速绘制**——跳过头/肢体描边）：只画有动作的观众，按 30Hz 烘焙；动画结束直接停 blit（静止姿态由静态层呈现，无需重建）。
- 单次动画烘焙成本：~376 人 × 去描边 × 半分辨率填充 ≈ 原全量重建的 1/4~1/3。
- `crowdCacheSupported()` 改为只探测 `document.createElement` 存在（不再每帧创建探针画布）。
- 测试更新为双层断言（20 项）：静态层仅首帧重建、动画 60 帧烘焙 30 次、动画层半分辨率 640px、动画结束零重建直接显现、相机过桶才重建静态层、低画质/无缓存回退。
- 实测（1280×720 高画质/无上限）：得分欢呼全程 FPS 稳定 165。dev test:all 223 PASS / desk test:all 263 PASS 全绿。

## 四十二、比赛页提示条极简化 + 主页上/下滑动钮（2026-08-06）

### 1. 比赛页底部提示改「极简按键说明」
- 移除底部长文案（蹲得越久越慢/反复蹲站转换延迟/发球瞄准流程/高吊技巧/Esc 暂停等解释），只留「按键=动作」最简一行（modes.js 双树）：
  - 桌面：`WASD/方向键=移动 · 左键=推球 · 右键=扣球 · Ctrl=蹲下 · 单击=发球`
  - 触屏：`摇杆=移动 · 点按=推球 · 扣=扣球 · 蹲=蹲下 · 发球=点两下`
  - AI 观战：`红方 X vs 蓝方 Y · 暂停中可调难度`
- browser-smoke 桌面/触屏提示断言同步更新（左键=推球/右键=扣球/蹲下 无 摇杆/按钮；触屏含 摇杆/蹲）。

### 2. 主页上/下滑动钮（同日改为右端拖动条，见四十三 §4）
- 主菜单内容超高（矮屏手机）时底部居中显示 ▲/▼ 滚动钮：点击按一屏 55% 平滑滚动（scrollBy）；内容放得下时自动隐藏。
- 实现：#menu 改 overflow-y auto（隐藏滚动条）+ 内容包一层 .menu-scroll（margin:auto：矮屏居中、超高两端可滚）；main.js updateMenuScroll/scrollMenu（resize/返回主页/通关记录刷新时重算显隐）；state.js 挂 menuScrollUp/Down 引用；index.html 加按钮。
- 顺带把 dev 树滞后的 19 个文件与 desk 对齐（audio.js autoplayMusic、联机发球测试修复、AI 数值调控、通关记录等）——两树全量一致。

### 3. 测试
- browser-smoke 新增 4 断言：滚动钮内容超高时显示、下滑/上滑滚动生效、内容放得下时隐藏。
- dev test:all 267 PASS / desk test:all 267 PASS 全绿。

## 四十三、电脑误判手机端修复 + 桌面提示按键准确化 + 音乐恢复加固（2026-08-06）

### 1. 电脑误判手机端修复（state.js 设备判定）
- 原判定 `(pointer:coarse) || ontouchstart || maxTouchPoints>0` 再叠加窗口 ≤1024px：触屏笔记本/Windows 触摸设备（maxTouchPoints>0，如 ASUS 触屏本）在窄窗口下被误判为手机，电脑上出现手机端摇杆/蹲/扣按钮与触屏提示。
- 改为：**主指针为粗指针（pointer:coarse）才算手机**；或 支持触摸且主输入非细指针（无鼠标触屏设备/旧安卓兜底）。触屏笔记本主指针仍是鼠标（pointer:fine）→ 判定桌面。`?touch=1 / ?desktop=1` 强制参数不变。
- 回归测试：触屏能力 + 主指针鼠标 + 窄窗口 → 桌面处理（无摇杆/蹲按钮、触控按钮隐藏）。

### 2. 桌面提示按键准确化（modes.js）
- 人机/联机桌面提示按真实键位：`WASD/←→=移动 · 左键/↑=推球 · 右键/↓=扣球 · Ctrl=蹲下 · 单击=发球`（↑/↓ 是推球/扣球而非移动——此前「方向键=移动」不准确）。
- 本地：`P1: WASD=移动 · 左键=推球 · 右键=扣球 ｜ P2: ←→=移动 · ↑=推球 · ↓=扣球`。
- 触屏提示不变（摇杆=移动 · 点按=推球 · 扣=扣球 · 蹲=蹲下 · 发球=点两下）。

### 3. 音乐恢复加固（audio.js）
- 自动播放拦截后的恢复由「一次性监听」改为「持续重试」：每次用户交互都尝试 resume/startMusic，直到音乐真正在播才卸载监听（避免首次交互时机过早/被策略拦截而丢失恢复机会）。
- 线上资源验证：music.mp4（3.35MB）/applause.wav（564KB）线上==本地且完整；audio.js 线上==本地；音乐相关断言（bgmAudio 挂接/src/开关/音量/独立性）全绿。

### 4. 测试
- dev test:all 269 PASS / desk test:all 269 PASS 全绿（新增 2 条设备判定回归断言）。

## 四十三、电脑误判手机端修复 + 桌面提示按键准确化 + 音乐恢复加固 + 主页滑动条改右端拖动（2026-08-06）

### 1. 电脑误判手机端修复（state.js 设备判定）
- 原判定 `(pointer:coarse) || ontouchstart || maxTouchPoints>0` 再叠加窗口 ≤1024px：触屏笔记本/Windows 触摸设备（maxTouchPoints>0，如 ASUS 触屏本）在窄窗口下被误判为手机，电脑上出现手机端摇杆/蹲/扣按钮与触屏提示。
- 改为：**主指针为粗指针（pointer:coarse）才算手机**；或 支持触摸且主输入非细指针（无鼠标触屏设备/旧安卓兜底）。触屏笔记本主指针仍是鼠标（pointer:fine）→ 判定桌面。`?touch=1 / ?desktop=1` 强制参数不变。
- 回归测试：触屏能力 + 主指针鼠标 + 窄窗口 → 桌面处理（无摇杆/蹲按钮、触控按钮隐藏）。

### 2. 桌面提示按键准确化（modes.js）
- 人机/联机桌面提示按真实键位：`WASD/←→=移动 · 左键/↑=推球 · 右键/↓=扣球 · Ctrl=蹲下 · 单击=发球`（↑/↓ 是推球/扣球而非移动——此前「方向键=移动」不准确）。
- 本地：`P1: WASD=移动 · 左键=推球 · 右键=扣球 ｜ P2: ←→=移动 · ↑=推球 · ↓=扣球`。
- 触屏提示不变（摇杆=移动 · 点按=推球 · 扣=扣球 · 蹲=蹲下 · 发球=点两下）。

### 3. 音乐恢复加固（audio.js）
- 自动播放拦截后的恢复由「一次性监听」改为「持续重试」：每次用户交互都尝试 resume/startMusic，直到音乐真正在播才卸载监听（避免首次交互时机过早/被策略拦截而丢失恢复机会）。
- 线上资源验证：music.mp4（3.35MB）/applause.wav（564KB）线上==本地且完整；audio.js 线上==本地；音乐相关断言（bgmAudio 挂接/src/开关/音量/独立性）全绿。

### 4. 主页滑动条改右端拖动（index.html/css/state/main）
- ▲/▼ 滚动钮改为**页面右端竖向长条**（10px 自定义滚动条）：可**拖动拇指**按比例滚动（窗口级 pointermove 跟手），也可**点按轨道**直接跳转到点击位置；滚轮/触控板滚动时拇指位置实时同步；内容放得下时自动隐藏。
- 手机端（≤720px）：主页「乒乓对决」标题**置顶常驻**（sticky + 渐变遮罩），滚动菜单时仍可见。
- 测试改为 4 断言：内容超高显示、拖动拇指滚动生效、点按轨道滚动生效、内容放得下隐藏。

### 5. 测试
- dev test:all 269 PASS / desk test:all 269 PASS 全绿（设备判定 2 条回归 + 滑动条 4 条）。

## 四十四、说明书回归设置面板（2026-08-06）

### 1. 设置面板新增「📖 说明书（操作说明）」折叠块（index.html/css）
- 设置（主页与比赛页右上角 ⚙）内新增可折叠说明书，内容与《使用说明.txt》操作部分一致：
  - 电脑操作：A/D 或 ←/→ 横向、W/S 前后、Shift 跑步、Ctrl 蹲下；鼠标瞄准单击发球；左键推球 · 右键扣球（快扣杀，球不够高撞网判负）；蹲下+推球=高吊（扣杀来球=接扣杀）
  - 手机操作：摇杆移动、单击推球、「扣」扣球、「蹲」蹲下、发球点两下
  - 模式：人机（简单/中等/困难/地狱）· AI 观战 · 本地双人 · 联机（4 位房间码）
  - 规则：11 分制（10 平后净胜 2 分）· 每 2 分发球轮换 · 发球先落本方再落对方半台 · 触网入界重发
  - 提示：判定范围虚线开启后左上角实时提示球高/进箱/可扣杀
- 折叠块默认收起（`<details>`），展开不占设置卡片空间；样式沿用设置卡片风格（深色圆角块、金色标题、内容区可滚动）。

### 2. 测试
- 浏览器实测：主菜单渲染正常；⚙ 设置 → 说明书展开/收起正常，内容完整（电脑/手机/模式/规则/提示）。

## 四十五、UI/生涯/帧率综合改造（2026-08-06）

### 1. 取名生效：昵称代替比分表里的「你」（modes/hud/main/state）
- 主菜单昵称（#nameInput）对人机/本地双人/联机生效：人机 P1 与本地 P1 的比分表、胜负提示、判定面板、发球横幅里的「你」→ 昵称（空回退「你」/「玩家1」）；人机记录保存名随之变为真实名字（此前全部记成「你」）。
- 昵称输入即存 localStorage（ppd_name），重开应用仍在。

### 2. AI 观战：暂停可改双方 AI 名字（index/input/modes/hud）
- 暂停面板红/蓝侧各加名字输入框（pauseAiNameA/B）：打开暂停回填当前名，改动即时写回 PPD.app.names（比分表/胜负/发球横幅同步），持久化 ppd_ai_names；AI 观战开局读取持久化名（缺省 红方 AI/蓝方 AI）。

### 3. 手机端固定标题栏（style/index）
- 移除 sticky + 渐变遮罩（内容被渐变盖住的问题）；≤720px 下标题固定顶部（不透明实底）、菜单内容在下方独立滚动——不再遮住下面选项。

### 4. 通关记录 → 个人生涯 + 后端近 60 条战绩（hud/server/room/records/index）
- 人机模式每局结束都记录胜负（此前只记玩家获胜）；面板改名「个人生涯」：总场次 / 胜 / 负 / 胜率 + 最近 60 条（🏆 胜 / 💔 负 · 难度 · 比分 · 时间 · 名字）。
- 后端 RECORDS_CAP 200→60、GET 默认 limit 20→60（server.js 与 src/room.js 同步）。

### 5. 移除自定义滑动条 → 浏览器原生（index/style/main/state/modes/records）
- 删除主页右端自定义滑动条（HTML/CSS/JS/refs），主菜单改回浏览器原生滚动条；手机端滚动交给系统。

### 6. 观众生成/缓存帧率优化：分屏缓存按 viewSide 分键（render.js）
- 静态层/动画层离屏缓存由单例改为按 viewSide 分键（本地双人两视口各自缓存）——修复此前分屏每帧重建 2 次 376 人观众席的帧率抖动。
- 新增分屏回归测试（viewSide 0→1→0 不互踢缓存）。

### 7. 测试与文档
- test:all（dev+desk）全绿；browser-smoke 移除滑动条断言、ELEMENT_IDS 更新；crowd-anim-cache-test 新增分屏回归；README/使用说明同步更新。

## 四十六、帧率优化第一批+第二批 + 个人生涯收录真人对战（2026-08-06）
### 1. 真人对战记录收录（hud/server/room/records）
- 战绩保存由「人机获胜」扩展为 ai/local/online 三种模式：人机（胜负都记）、本地双人对战（胜负都记）、联机（胜负都记，胜方=己方 side）。
- 后端 mode 清洗保留 ai/local/online，列表模式标签：人机 / 双人 / 联机。
### 2. 个人生涯移入主框（index/records/style）
- 主菜单内直接显示「个人生涯」小方框（总场次 · 胜率 · 点击展开），点击展开整页分页列表（10 条/页）。
### 3. 第一批帧率优化（hud/render）
- #2 隐藏求解器门控：判定范围虚线关闭时 canSmashNow/canLobNow 不再每帧求解。
- #8 HUD 按需 DOM 写入：文本/显示未变化不写 DOM。
- #9 低画质地板缓存：低画质也走静态层缓存（每帧 1 blit，不再逐帧重画地板）。
- #3 球/阴影/球员阴影径向渐变精灵化：预烘焙到离屏 canvas（drawImage 替代 createRadialGradient）。
### 4. 第二批帧率优化（loop/characters）
- #1 AI 控制降频 60Hz：每 2 物理步调用一次（dt 加倍保持累计时间一致）——predictCrossing 高频求解减半。
- #5 火柴人 exp 常量提出：6 处 1-exp(-k·dt) 预计算为常量，每帧每角色 0 次 Math.exp。
### 5. 测试与实测
- crowd-anim-cache-test 低画质断言更新（缓存地板 2 帧 2 blit）。
- test:all 全绿。
- 实测：1280×720 无上限 AI 观战 avg 165/min 165（优化前基线 avg 164/min 157——最差帧消除）；1920×1080 与手机低画质均稳定 165。

## 四十四、反击扣杀难度微降:扣杀来球命中窗免起拍延迟(2026-08-06)

### 1. 改动(strokes.js,双树 × public/src 镜像)
- 实测发现:真人接扣杀(蹲下+推球)需提前 0.15~0.2s 预判起拍;等球弹台再按(反应式)→ 96% 漏接。根因是推球命中窗 [0.08, 0.28]s 的 0.08s 起拍延迟——扣杀 10~15m/s 过箱仅 0.04~0.1s,延迟把命中窗挤出。
- 修复:扣杀来球(hitType===2)命中窗**从挥拍 t=0 起**(免 0.08s 起拍延迟),窗口 **[0, 0.28]s**——与 type2 扣杀自 t=0 判箱同机制(三十五·附注2 先例);普通推球/低平保留起拍延迟(挥拍蓄力动画 + 时机感)。
- 未动 windup/live 数值 → 挥拍动画(easeOut)、engine.js「推球中按扣球升级」组合窗、hud 均不受影响。

### 2. 实测
- 探针(引擎求解器生成真实扣杀):提前起拍 100% 保持;**反应式按压(球进箱才按)由"几乎全漏"→ 100%**(下蹲/站立均如此)——不再强制预判,反击难度下降。
- 新测试 test/smash-counter-test.js(6 断言):扣杀来球 t=1/120 即命中、普通来球仍等 windup 才命中、窗末端 t≈0.25s 进箱命中 / t≈0.30s 进箱不命中(窗止 0.28s,未额外加长)。

### 3. 平衡复验
- sim-match 12 种子:地狱vs困难 9.3:9.0(旧引擎同种子集 10.0:9.7——偏移 ~0.7 分在种子方差内)、困难vs中等 11.3:5.8(基准 11.0:5.7,稳定)——等级秩序 简单<中等<困难<地狱 保持;困难/地狱 smashDef 概率门未动,有效反击率 50%/80% 定标不变。
- dev test:all 275 PASS / desk test:all 276 PASS 全绿;引擎镜像 SHA256 4 份一致。

## 四十七、本地"进房卡死"防护 + 局域网联机补齐（2026-08-07）

### 背景
- 用户报告"进入房间后双方卡死"发生在**本地（node server.js）**。排查结论：当前 server.js 源码链路健康（解析 `k` 位掩码、60Hz 固定步长、0.0.0.0 绑定、加入方按 `location` 推导 `ws://IP:8765/ws`），**最可能是"8765 端口上跑着旧版 server.js"**（旧版不解析 `k` 位掩码 → 输入被静默丢弃 → 双方进房后角色不动/发不了球）。桌面启动器原有"当前版本"校验只查 `/api/records`，识别不了"有 records API 但缺 k 解析"的过渡版 → 静默复用 → 卡死。
- 局域网联机（同一 Wi-Fi / 手机热点 / Radmin VPN 虚拟局域网，无需公网）**链路可行**：`node server.js` 已绑定全部网卡 + 任意路径 WS 升级 + 静态同源分发 + 4 位房间码。本次补齐体验缺口：房主看不到联机地址、防火墙未放行、无操作文档。

### 1. server.js：版本与诊断（防静默复用旧服务器）
- 新增 `VERSION = package.json.version + '-local'`（单源版本）；新增 `GET /api/info` 返回 `{ ok, version, port, ips, hostname }`（`os.networkInterfaces()` 过滤 internal——局域网与 Radmin VPN 虚拟网卡 IP 全列出）；心跳 `pong` 带 `ver`。
- 启动时打印版本 + 全部局域网/VPN 联机地址；每 10s 打印诊断统计（rooms / in / broadcast / create / join / close，有活动才打）——排查卡死时可直接判断"输入到底有没有到服务器"（房间在、in=0 → 输入没到；in 有、broadcast=0 → 引擎没推进）。
- 版本号 1.4.0 → 1.4.1（package.json）。

### 2. desktop-launcher.js：版本校验升级（堵静默复用漏洞）
- `isCurrentGame(port)` 由"`/api/records` 是否 200"改为"`/api/info` 的 `version === EXPECTED_VERSION`（同取 package.json + -local）"——旧服务器（缺 `/api/info` 或版本不符）自动 `killPort` 重启，杜绝"复用了旧服务器 → 进房后双方卡死"。

### 3. 客户端：局域网地址展示 + 旧服务器提示
- `app/net.js`：建房等待面板（本地模式）拉取 `/api/info`，在 `#lanUrls` 列出"对方请打开 `http://IP:端口`"（多网卡/Radmin 26.x 全列出）+ 复制按钮（clipboard 优先、textarea 兜底）；公网/非 localhost 不显示；`/api/info` 失败（旧服务器）静默保留默认提示。
- `app/net.js` `pong` 处理：本地模式下 pong 缺 `ver` 字段（旧服务器）→ 状态栏提示一次"⚠ 本地服务器版本过旧，请重启本地服务器"。
- `app/state.js`：ui 注册 `#lanUrls`；app 新增 `lanInfo / serverVersion / serverStaleWarned`。
- `public/index.html`：roomPanel 新增 `#lanUrls` 容器；`css/style.css` 新增 `.lan-urls/.lan-url/.lan-copy` 等样式。

### 4. 防火墙一键放行
- 新增 `tools/局域网放行.cmd`：自动请求管理员权限，`netsh advfirewall` 幂等放行 TCP 8765（删旧规则再建）——Windows 防火墙默认挡 Node 入站是"对方连不上"的头号原因。

### 5. 文档
- `使用说明.txt`：新增「局域网联机」章节（同一 Wi-Fi / 手机热点 / Radmin VPN 三场景步骤 + 注意事项：房主窗口保持打开否则关窗停服、路由器 AP 隔离、端口被占自动换端口由 /api/info 反映）+「进房卡死排查三步」（确认 server.js 为当前版本/重启、确认 8765 无旧进程、防火墙放行）。
- `README.md`：联机说明补充局域网/Radmin 与防火墙放行说明。

### 6. 验证
- `test/net-test.js` 扩展：断言 `/api/info` 返回 `ok/version/ips`（启动服务器时）。
- 实测：`node server.js` 起服 → 标签页 A `http://127.0.0.1:8765` 本地建房 → 标签页 B `http://<本机局域网IP>:8765` 输码加入 → 双方进房、角色可动、发球能发、无冻结、快照持续变化；`[stats]` 日志输出 in/broadcast 递增。
- 防火墙：未放行时加入方连不上 → 运行 `tools/局域网放行.cmd` 后恢复。
- 注：公网 DO 侧的 Alarm 兜底（`src/room.js`/`src/room-core.js` + `test/do-alarm.js`）由并行会话实现，本次未改动 `src/`；未执行 wrangler deploy。

## 四十八、观众欢呼覆盖全体观众（动画层剔除修复）+ 观众密度再减半（2026-08-07）

### 1. 欢呼/摇头动画覆盖全体观众（public/js/render.js drawPerson）
- 根因：`drawPerson` 屏幕外剔除用 `ctx.canvas.width/height`（**设备像素**）当边界，与 `cam.project()` 输出的 **CSS 像素**坐标比较。动画层是半分辨率画布（CROWD_ANIM_SCALE=0.5），dpr=1 桌面屏上剔除边界≈vw×0.5 → 屏幕右半场观众被误判为屏幕外、从不进动画层 → 得分时右半场观众保持静态坐姿（无欢呼无摇头）——"欢呼动作未覆盖全体观众"。
- 修复：用 `ctx.getTransform().a` 把剔除边界换算回 CSS 像素（静态层 scale=dpr、动画层 scale=dpr×0.5 口径一致）→ 全体可见观众都进入动画层，**胜者欢呼、败者摇头覆盖全场**。
- 分阵营逻辑（triggerCheer / hud / net）未动。

### 2. 观众密度再减半（public/js/app/render.js ×2：本地/联机 viewModel）
- `density: isTouch ? 0.5 : 1` → `isTouch ? 0.25 : 0.5`：电脑 ~376→~188、手机 ~192→~96（render.js crowdLayout 注释同步）。覆盖修复使动画层绘制量增加，人数减半恰好抵消性能开销。

### 3. 测试（test/crowd-anim-cache-test.js）
- 假 ctx 增加 `getTransform`（静态层=1 / 动画层=0.5）让测试走真实剔除路径；`animLen` 改为与静态层同口径（覆盖全体可见观众）并新增断言「动画层覆盖 = 静态层（右/下半屏不再被误剔）」；新增断言 `crowdLayout(0.25)` ≈ `crowdLayout(0.5)` 的一半。
- 动画计数断言改 ±4 容差：动画中观众姿态（举手/摇头/起伏）让人头/髋在 ±60px 剔除边沿 ±10px 抖动，绘制人数与 rest-pose 相差 ±4 内属正常；真重建一次 +~animLen，不会被容差掩盖。

### 4. 验证
- `node test/crowd-anim-cache-test.js`（28 断言）、`node test/seated-crowd-test.js`（24 断言）全绿。
- 待游戏内目测：得分后全场观众有反应（左侧胜方举手欢呼、右侧败方摇头），观众人数约 ~188。

### 5. 版本与桌面应用
- 版本号 1.4.1 → 1.4.2（package.json；桌面版使用说明、安装包使用说明、安装器标题同步）。
- 桌面应用安装包更新：`dist/installer/package/乒乓对决_安装包/` 内 `game/` 同步本条目全部改动（render.js / app/render.js / 修改记录.md / package.json / 使用说明.txt）。

## 四十九、观众欢呼动画"复制体"修复：动画期观众只画在一层（2026-08-07）

### 1. 根因（public/js/render.js 双层离屏缓存）
- 静态层预渲染 **rest 姿态**观众（全分辨率、带描边），动画层按 30Hz 烘焙**动势**观众（半分辨率、去描边）叠加其上。
- 得分欢呼时：动画层观众举手/起身浮动（±0.045m 起伏）、败方摇头（±0.055m 头部摆动），与静态层 rest 身影错位 → **同一观众两重身影叠出"复制体"**（静止层 + 动画层同时可见）。
- 像素实测（tools/probe-crowd-dup.js）：常态观众 21,769 像素，欢呼动画期飙至 ~28,000（**129%**，红方 +31% / 蓝方 +27%）——正是叠影。

### 2. 修复：任一时刻观众只画在一层
- **动画期**：静态层重建为「地板+看台（不含观众）」；全部观众由动画层按 30Hz 烘焙绘制（有动作按动作、其余 rest 兜底）。
- **非动画期**：静态层 = 地板+看台+rest 观众；动画层停止 blit；动画结束静态层重建回 rest 观众。
- 实现：`crowdMode`（off=无观众 / static=静止层 / anim=动画层）并入静态层缓存 key → 模式切换自动触发重建（动画开始/结束各重建一次静态层，开销可忽略）；`rebuildCrowdCache` 增加 `withCrowd` 参数；`rebuildAnimCache` 改画**全部**观众（不再只画有动作的）。
- 半分辨率+去描边的动画层画风不变（CROWD_ANIM_SCALE=0.5 原设计，小人物放大无感知）。

### 3. 验证
- `node test/crowd-anim-cache-test.js`（28 断言）、`node test/seated-crowd-test.js`（24 断言）全绿；新增断言：动画期静态层无观众（sArc 不增）、动画结束静态层重建回 rest 观众（sArc=2×statLen）。
- `node tools/probe-crowd-dup.js`（新增开发工具，CDP 驱动真实游戏）：修复后欢呼动画期观众像素稳定 ~11,000-11,500（单份半分辨率，无翻倍、无叠影）。

### 4. 版本与桌面应用
- 版本号 1.4.2 → 1.4.3（package.json；桌面版使用说明、安装包使用说明、安装器标题同步）。
- 桌面应用安装包更新：`dist/installer/package/乒乓对决_安装包/` 内 `game/` 同步（render.js / 修改记录.md / package.json / 使用说明.txt / tools/probe-crowd-dup.js）。

### 5. 部署与安装包精简
- Cloudflare 部署成功（`npm run deploy:all`）：DO game-room → https://game-room.hexiangjie694.workers.dev；Pages → https://b4697fdd.ping-pong-duel.pages.dev（public/ 本轮更新 render.js / app/render.js）。
- 安装包精简：`game/tools/` 剔除全部开发工具（analyze-crowd / cdp-crowd / probe-crowd-dup / sim-match / shot 等 13 项 + shots/ 截图），仅保留「局域网放行.cmd」（玩家联机放行防火墙所需）；`%PKG%` 暂存区同步。
- `dist/`、`%PKG%/` 加入 .gitignore（各 ~93MB 构建产物：内置 Node 运行时 + 安装器，不入库）。
- GitHub：推送 master 至 origin（hxj-2888/ping-pong-duel）。
---

## 四十七、v1.6 版本发布（画质帧率 / 属性机制 / 计分判定 / 联机框重构 / 说明书页 / 音频）

> 需求：画质帧率机制优化、角色属性线性化、计分判定修复、联机稳定性与联机框重构、说明书独立页、版本 1.5.3→1.6（定稿为 1.6）。

### 1. 画质 / 帧率（需求 1）
- 保留「高画质」档位名称；**高画质不再封顶**——resize 直接用 `devicePixelRatio`，自动适配设备最高屏幕分辨率（低画质保留 1080p/400p 封顶）。
- 帧率默认改为**无上限**（自动匹配设备最高刷新率，120Hz 设备自动跑满 120 帧）；下拉仅保留 无上限/60/30，**彻底移除 45 档**（含读取/写入白名单、注释、fpsMeter 标题）。
- 涉及：`app/state.js`（resize/setFrameRate/默认值）、`index.html`（帧率下拉）、`app/loop.js`（门控兜底）。

### 2. 角色属性（需求 2、3）
- 接球参数改为**漏球率线性模型**：`漏球率 = 基准漏球率 / 倍率`（夹取 0.5%~80%），0.5~1.5 全程线性有效，修复旧版 1.2 后无变化（中等 ×1.5→3.3%、×0.5→10%）；地狱基准恒不漏球。
- **敏捷 >1 移动速度加成**：最大 +25%（×1.5 封顶），经新增 `player.speedMul` 由引擎 step 应用（玩家恒 1，AI 由 ai.js 设置）。
- 文案：滑杆等效值「接球率 X%」统一改为「**非刻意漏球率 X%**（每 N 球漏 1）」，敏捷行加「速度加成 +X%」；说明书/主页相关文案同步。
- 涉及：`ai.js`、`engine/state.js`（speedMul）、`engine.js`（step 移动速度）、`app/input.js`（tuneEffText）、说明书页。

### 3. 计分判定（需求 4）
- 新增 `ball.netBlocked`：球撞网（net 事件）置位，击球/发球复位——失分统一显示「**未过网**」，不再误显示 两次弹跳/未能回球/出界（`engine/physics.js` onBallBounce/onBallFloor → reason `no-cross`；`hud.js`/`net.js` 文案映射）。
- **补全合法回球校验**（偶像球=AI 变招球）：`engine/strokes.js` applyPaddleHit 物理兜底路径用 `rallyFlightOk` 校验，非法擦网顶过网强制改打撞网（对方得分），杜绝「未过网落地却判得分」。
- 涉及：`engine.js`（stepBall）、`engine/state.js`、`engine/physics.js`、`engine/strokes.js`（public + src 双镜像）、`hud.js`、`net.js`（联机点原因显示）。

### 4. 联机稳定性与联机框重构（需求 5-9 + 新增）
- **公网回退/回溯修复**：插值间隔改按实测快照间距自适应（ema，本地 60Hz≈17ms / 公网 20Hz≈50ms）；渲染 alpha 不再钳下限 0（改 -0.5~1.2，落后时线性外推），消除「画面回退/人物回溯」（`app/net.js` interpClock + `app/render.js` renderOnline）。
- **本地建房失败修复**：`setupNet` 新会话清空 roomCode、`backToMenu` 清联机残留状态，hostMode 始终新建房间。
- **移除公网服务器 IP 输入框**：删设置面板输入框、`initPublicServerUrl`、`wsUrl` custom 分支。
- **联机框重新设计**：主页联机元素全部收进「联机对战」入口打开的全屏 `#netPanel`（等同新开页面）——模式切换（本地/公网 + ⚠ 感叹号说明）、建房/加入、房间码、对方设备 IP 输入（本地模式显示，需求 9）、房主等待区（本地联机网址显示在房间码上方，需求 8；防火墙提醒）、精简联机教程；原 `#roomPanel` 并入。
- 涉及：`index.html`、`app/state.js`、`app/main.js`、`app/net.js`、`app/modes.js`、`css/style.css`。

### 5. 设置=全局暂停（需求 10）
- 对局中打开设置（手机/电脑共用）→ `paused=true` 冻结本地引擎与渲染；关闭恢复。联机暂停期间冻结输入发送（服务端继续，恢复后快照自动锚定）。
- 涉及：`app/main.js`（openSettings/closeSettings）、`app/loop.js`（联机分支暂停）、`app/modes.js`（backToMenu 复位）。

### 6. 手机端精简（需求 11-14）
- **取消手机本地分屏**：删除 `#p2Controls`、P2 摇杆/蹲绑定、`local-duo` 样式、手机端「本地双人」入口与分屏提示（电脑端本地双人保留）。
- **人机数值调节 UI 压缩**：pause 面板/调参区间距字号压缩、max-height 兜底，内容不超屏幕、无需滚动条（原需求 21 滚动条取消）。
- **手机说明书按钮**：触控底栏原 P2 空白位新增 📖 按钮（68px，与功能方块一致），打开说明书页。
- **手机虚线精简**：手机端仅保留 接球箱/配套虚线/球高标识，隐藏并跳过 可扣杀/可高吊 两行与求解。
- 涉及：`index.html`、`app/state.js`、`app/input.js`、`app/hud.js`、`app/main.js`、`app/modes.js`、`css/style.css`。

### 7. 独立说明书页面（需求 15-18、20）
- 新增全屏 `#manualPanel`（等同新开页面，z-index 60，自身可滚动 + 防缩放）：收纳全部操作/判定/规则/AI 特性/虚线/生涯/联机/安装/国际赛事标准，按平台过滤、无重复。
- 主页通用说明全部移除（`#tips`、`#lanManual`、设置面板说明书 details）；比赛页仅在比分栏下方保留一行**简易操作说明**（hintBar 重定位；国际赛事标准仅放说明书）。
- 电脑端：设置按钮正下方新增同尺寸 📖 按钮（`#btnManualMenu`）；手机端：触控底栏按钮。
- 修复「点击说明书导致设置页面放大」（说明书移出设置面板结构性消除）。
- 涉及：`index.html`、`app/main.js`、`app/state.js`、`css/style.css`、`app/input.js`（Esc 关闭说明书/联机框）。

### 8. 版本号与公网感叹号（需求 19）
- 版本号 **1.6**：`app/state.js` version='1.6'、`package.json` 1.6.0（semver，server VERSION=x.y.z-local 校验）、`android/AndroidManifest.xml`（versionName 1.6 / versionCode 5）、`public/download.html`、`使用说明.txt`（launcher/server 自动读 package.json）。
- 公网联机 ⚠ 感叹号：联机框模式切换行公网模式下显示，点击展开「玩家量过大将导致服务器负载卡顿」、再点收回。

### 9. 音频（需求 22 + 掌声拔除 + 应用端缓存保底）
- **拔除掌声合成音**：删除 `applauseSynth` 与 `applause()` 合成兜底——掌声仅用真实 WAV（WebAudio 缓冲优先、`<audio>` 元素兜底），WAV 不可用时静默。
- **应用端音乐音效缓存保底**：新增 `decodeCache`（url→Promise<AudioBuffer>）幂等缓存，重复 ensure 不再重复 fetch 解码；file://（APK WebView）自动走 `<audio>` 元素直载；autoplayMusic 增加 focus/visibilitychange 重试兜底。
- **对局开场鼓掌声**：startLocal/startAI/startAIVsAI/startOnlineGame/restartMatch 开场播放真实掌声（修复开场掌声消失）。
- 涉及：`audio.js`、`app/modes.js`。

### 10. AI 变招距离限制（需求 23）
- 变招判定时若距球桌 >0.7m 不触发变招（改打常规球）；变招生效窗口内目标 z 钳制到 ±(台半长+0.7=2.07)，杜绝 AI 远距离异常走位。
- 涉及：`ai.js`。

### 11. 验证
- `npm run test:all` 全绿（net-test / engine-stress / physics-boundary / ai-test / browser-smoke / 观众席 5 项 / smash-counter / net-retry / input-mask / do-alarm / do-smoke / http-robustness / check-engine-sync）。
- browser-smoke 同步：DOM 桩补充联机框/说明书新元素；帧率默认断言改无上限；等待面板断言改联机框；删除已废弃的 P2 分屏触控用例。
- 引擎双镜像（public/js/engine ↔ src/engine）字节级一致；`android/assets/www` 副本已同步。
---

## 四十八、v1.6.1 版本迭代（音频重叠修复 / 发球轨迹 / 说明书滑钮 / AI 数值线性化 / 联机容错）

> 需求：v1.6.1 全量更新清单——音频多层重叠杂音、发球轨迹渲染、手机端布局、说明书滑钮、接扣球/地狱反应线性、AI 发球重构、建房冗余删减、公网联机回归。

### 1. 音频重叠杂音修复（需求 1）
- **根因**：`audio.js autoplayMusic().resume()` 中 `else if (bgmEl && bgmEl.paused) bgmEl.play()` 在 WebAudio 缓冲源（bgmSource）已存在时仍启动 `<audio>` 元素 → 双音源叠加 = 多层重叠杂音（缓存逻辑与实时播放冲突）。
- 修复：`resume()` 缓冲路径存在时只恢复 ctx/补启缓冲源，绝不额外启动元素；`startMusic()` 缓冲路径开头防御性暂停 `<audio>` 元素（**单一音源互斥**）；`ensure()` 幂等保持。
- 资源核对：`music.mp4`（3352708B，ftyp 容器，含视频轨时解码失败自动走元素兜底）、`applause.wav`（564558B，RIFF-WAVE）均完好。

### 2. 发球轨迹（需求 2、3）
- **需求 2 根因**：① 未瞄准默认预览（defaultServePlanAt）未经求解校验，可能画落台外路径；② 瞄准后移动站位 → 旧 servePlan 失效，按新发球点发射致发球出界/无法落台。
- 引擎（双镜像）`strokes.js startServeStroke`：使用瞄准 plan 前以 `serveLanding(当前 serveBallPos, plan.vel, plan.spin)` 复验，失效回退 `solveServe` 默认求解。
- 渲染 `app/render.js sampleServePath`：仅合法落到对方半台台面才返回轨迹，否则返回 null → **整条虚线隐藏、不显示落点标记**（需求 3 天然覆盖出界场景）。

### 3. 手机端布局（需求 4）
- 移动端 `#hintBar`（简易操作说明）移至**页面最顶部**（比分栏/观众行之上，`top: env(safe-area-inset-top)+4px`），不再被虚线面板遮挡；`#hitRangeInfo` 虚线面板压缩尺寸（padding/font/gap 收紧）。
- 手机虚线仅保留 接球箱/配套虚线/球高（v1.6 已实现）。

### 4. 说明书统一与滑钮（需求 5、7③）
- 移除 `netPanel` 内「📶 联机教程」details（取消分散内嵌说明，完整内容在说明书页）。
- `manualPanel`：全部 `.manual-capsule` 默认 `open`（分区默认完整展开）。
- **滑钮滚动**（非 ▲/▼ 按钮）：说明书页右侧纵向滑轨 + 可拖拽滑钮，拖动滑钮 ↔ 内容滚动双向同步、内容滚动时滑钮位置实时更新；手机端显示、桌面保留原生滚动条。
- `openManual()` 打开时隐藏主菜单（真正页面切换、不叠加），关闭恢复。

### 5. AI 数值线性化（需求 8、9）
- **接扣球加成**（需求 8）：`smashDef` 改漏球率线性模型 `1 − (1 − L.smashDef)/catchMul`（×1 与基准一致；0.5~1.5 全程线性、无 1.0 封顶失效段）；等效值显示同步。
- **地狱反应线性**（需求 9）：地狱反应延迟 = `0.02 × (1.5 − reactMul)`（滑块 0.5→0.02s、1→0.01s、1.5→0s 均匀递减）；其余难度保持 `L.react / reactMul`；显示同步。

### 6. AI 发球重构 + 发球距离滑杆（需求 10）
- `ai.js` 新增 `aiServeAim`：AI 发球经 `solveServeTo` 主动设定目标——方向混合（50% 朝对手 / 30% 远离对手镜像 / 20% 边线中路），发球深度随滑杆调节。
- 新增「发球距离」滑杆（`serveDistMul`，×0.5~×1.5 默认 ×1）：三侧（tuneA/tuneB/tuneOpp）各加一行，观战/人机共用即时生效；重置按钮同步。

### 7. 建房冗余删减（需求 11）
- `netPanel` 操作区包一层 `#netOperate`；房主建房进入等待态时整体隐藏操作区（无需再输房间号），`netBack`/关闭时恢复显示。

### 8. 公网联机回归排查（需求 12 升级）
- **线上真实握手 + 建房/加入/快照全链路实测 PASS**：`wss://ping-pong-duel.pages.dev/ws` 101 正常，房主建房 → 加入方加入 → 双方持续收到 state 快照。
- 结论：v1.6 部署后「连接服务器失效」为 DO 冷启动瞬时慢 + 旧 SW 缓存所致，非代码回归；客户端增强容错——建房/加入重试首次 8s（冷启动宽限）后续 6s、共 3 次；ppd-v3 缓存名（v1.6）已强制各端刷新。

### 9. 版本与验证
- 版本升 **1.6.1**：state.js、package.json 1.6.1、AndroidManifest（versionName 1.6.1 / versionCode 6）、build.cmd、download.html（v161 + 字节数）、使用说明.txt。
- 重新构建 APK → `public/PingPongDuel-v161.apk`（aapt 验证 code 6 / 1.6.1，3522366B）。
- `npm run test:all` 全绿（引擎镜像字节一致；ai/engine/physics/smash-counter/input-mask/net/browser-smoke 全过）；全渠道同步（android/assets/www、dist/installer game、%PKG%/game、dist/ecs、桌面三副本，保留 records.json / release.keystore）。
---

## 四十九、v1.6.2 版本（音频欢呼/音乐修复、说明书扩容、联机框间距、建房容错）

> 需求：欢呼音效与得分不匹配、音乐消失、说明书内容截断、联机框卡片间距、建房容错；版本 1.6.1 → 1.6.2（不做热修）。

### 1. 欢呼音效与得分匹配（移除开场欢呼）
- 移除 `modes.js` startLocal/startAI/startAIVsAI/startOnlineGame/restartMatch 的 5 处开场 `cheer()`（v1.6 加的对局开场鼓掌声改为不合时宜——「进比赛就放」）；得分/终局欢呼掌声保留（与得分时机匹配）。

### 2. 音乐消失 + 缓存逻辑排查（杜绝重复/静默）
- **根因**：`audio.js startMusic()` 缓冲路径在 `ctx.state==='suspended'`（自动播放策略挂起）时仍 `src.start()` 并暂停 `<audio>` 元素 → 元素停、缓冲源静默 = **音乐没了**。
- 修复：
  1. `startMusic()` 缓冲路径加 `ctx.state !== 'running'` 守卫——挂起时不建缓冲源、不停元素（元素兜底继续出声）；
  2. `loadBGM` 解码完成与 `decodeCache` 命中路径：仅 `ctx.state==='running'` 且无活动音源才停元素并 `startMusic()`（切零间隙缓冲）；
  3. `resume()`（autoplayMusic）：缓冲路径存在时 ctx 挂起先恢复运行、恢复后补启缓冲源，绝不多启元素；ctx 未运行时元素兜底——**任意时刻单一音源，杜绝重复/重叠**。

### 3. 说明书页面扩容 + 右侧滑钮
- `.manual-scroll { flex:1 1 auto; min-height:0; }`——flex 子项正确收缩滚动，容纳**全部内容**；`.manual-card { max-height:96vh; }` 适度扩容；右侧滑钮滚动完整内容（双向同步已实现）。

### 4. 联机框卡片间距
- `#netOperate { display:flex; flex-direction:column; gap:8px; }`——「联机:公网」与下方两卡片、「创建联机房间/加入房间」与「房间码输入框」各留约 2mm（8px）空隙（根因：v1.6.1 包裹后子项无 gap 粘连）。

### 5. 联机建房容错
- `net.js scheduleJoinRetry`：首次重试窗口 8s→12s、重试 3→4 次（DO 冷启动/驱逐恢复宽限），状态栏持续提示「自动重连中」。
- 公网联机逻辑保持 Cloudflare 不变（不恢复 IP 输入框，用户后续自建服务器再扩展）。

### 6. 版本与验证
- 版本升 **1.6.2**：state.js、package.json 1.6.2、AndroidManifest（versionName 1.6.2 / versionCode 7）、build.cmd、download.html（v162）、使用说明.txt。
- 重新构建 APK → `public/PingPongDuel-v162.apk`（aapt 验证 code 7 / 1.6.2，3522366B）。
- `npm run test:all` 全绿（引擎镜像一致；net/ai/engine/physics/smash/input-mask/browser-smoke 全过）；全渠道同步（android/assets/www、dist/installer game、%PKG%/game、dist/ecs、桌面三副本，保留 records.json / keystore）。

