# Cloudflare Pages 自定义响应头：APK 强制以附件下载并给出 ASCII 文件名，
# 避免下载管理器/微信浏览器把中文文件名搞坏导致"无效安装包"
# 文件名带版本号：手机下载管理器保存为 PingPongDuel-v301.apk，不会误装旧文件
# 全站静态资源一律回源校验（no-cache），杜绝边缘缓存对普通请求提供旧版（JS/CSS 改动频繁）
# 审计 2026-08-30：补安全响应头（此前完全缺失）——
#   CSP 说明：首页无内联 <script>（全部外部文件），script-src 可收紧为 'self'；
#             页面大量使用 style= 内联属性，style-src 需保留 'unsafe-inline'；
#             联机必须连 WebSocket（ECS/Cloudflare/局域网多线路），connect-src 放行 ws:/wss:；
#             frame-ancestors 'none' 阻止第三方站点 iframe 本页（防点击劫持）。
/*
  Cache-Control: no-cache, must-revalidate
  X-Frame-Options: DENY
  Strict-Transport-Security: max-age=31536000
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' ws: wss:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'
/
  Cache-Control: no-cache, no-store, must-revalidate
/*.html
  Cache-Control: no-cache, no-store, must-revalidate
/download*
  Cache-Control: no-cache, no-store, must-revalidate
# 审计 #17：public 只保留当前版本 APK（PingPongDuel-v301.apk），历史版本 APK 已全部清理
# 注意：文件名随版本更新——发布新 APK 时此处路径/filename 必须同步改（v270 漂移曾致规则失效）
/PingPongDuel-v301.apk
  Cache-Control: no-cache, no-store, must-revalidate
  Content-Disposition: attachment; filename="PingPongDuel-v301.apk"
