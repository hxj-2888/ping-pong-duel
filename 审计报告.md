# 乒乓对决 · 代码审计报告

> 审计日期：2026-08-13 · 基线：v1.7.0（commit fbbef18）
> 审计范围：server.js / src/（DO）/ public/js/（客户端）/ PWA / 文档 / 构建一致性
> 已知已修复项不计入：decodeURIComponent 崩溃、保底广播、快照缓冲插值、预测纠偏平滑化、挥拍/持球门控、动画死区

---

## 🔴 高危（真实 bug / 安全风险）

### 1. WebSocket 帧/缓冲无任何大小上限 → 内存/CPU DoS
- 位置：`server.js:225-267`（parseFrames）、`server.js:356-359`（client.buf 无界拼接）
- 问题：① 帧长字段无上限（`len===127` 直接 `Number(readBigUInt64BE)`）；② 分片消息无上限（fragParts 无限累积）；③ 每 chunk 全量 Buffer.concat + 未完成帧重扫 = O(n²)；④ 大 payload JSON.parse 一次性大分配
- 影响：任何恶意客户端（无鉴权）可缓慢拖死服务器内存/CPU；JSON.parse 大 payload 单请求内存尖峰
- 修复：缓冲上限（如 256KB）超限 `socket.destroy()`；单帧/分片 payload >64KB 拒绝

### 2. create/join 不检查 client.room → 房间永久泄漏
- 位置：`server.js:384-416`（create/join 均不检查 `client.room` 是否已存在）
- 问题：一个连接先建房 A 再 create/join 房 B，房间 A 的 `room.clients[0]` 残留引用 → 主循环只删"双空"房，A 永不清理，永久 60Hz 模拟空转；该 ws 同时收 A、B 两个房间广播互相污染
- 修复：create/join 开头 `if (client.room) leaveRoom(client)`（与 room-core 对齐）

### 3. 生涯记录玩家名存储型 XSS
- 位置：`public/js/app/records.js:127-136`（careerItemHtml 用 innerHTML 拼 `r.name`）；`server.js:74-85`（sanitizeRecord 只截断 20 字符不转义）
- 问题：名字可含 `<svg onload=...>` 等 20 字符内 HTML 载荷；手机端 localStorage 完全无清洗；渲染进 innerHTML
- 修复：渲染改 textContent 或 escapeHtml；服务端 sanitize 去 `<` `>`

### 4. 陈旧 joinTimer 复活已关闭连接 → 幽灵建房/UI 劫持
- 位置：`public/js/app/net.js:88-104`；`js/network.js:93-98`（用户 close 不 emit 'close'）；`app/main.js:33-44`（closeNetPanel）；`app/modes.js:78-104`（backToMenu）
- 问题：用户退出联机面板走 `net.close()`（closedByUser=true），onclose 跳过 emit('close') → joinTimer 不清理 → 12s/6s 后触发 `net.connect()` 复活已放弃的连接，后台 create/join 建幽灵房间，room 回调把 roomCode 覆盖并弹回等待页；最多重复 4 次持续 ~30s；与后续新会话并发时互相覆盖
- 修复：joinTimer 提升为会话级字段，setupNet/closeNetPanel/backToMenu 统一清理；回调加 `PPD.app.net !== net` 守卫

### 5. 重连在途 room/state 响应在退出后仍执行 → 强制拉回对局
- 位置：`public/js/app/net.js:128-172`（room 回调无条件 startOnlineGame）、`:185-342`（state 回调无 mode 守卫）；`app/modes.js:78-104`
- 问题：用户已退回主菜单，事件队列中迟到的 room/state 仍会执行 → 被硬拉回对局；snapB/names/pred 被陈旧数据污染
- 修复：setupNet 分配会话 token，backToMenu/closeNetPanel 递增；消息回调校验 token 失配即返回

### 6. 本地服务器无僵尸连接清理 + 无 side 接管 → 本地重连必失败
- 位置：`server.js:355,377-380`（alive 只写不读、仅 close 事件清理）；`server.js:399-416`（join 忽略 msg.side、双满报"房间已满"）
- 问题：拔网线/AP 断开时 close 可能数分钟不触发 → 席位永久占用、对手卡死；客户端重连按 DO 语义发带 side 的 join，本地不支持且槽位被占 → 局域网一次抖动即永久终结对局
- 修复：镜像 room-core 的宽限+side 接管（保留槽位/名字，15-30s 后清扫）；或客户端本地模式跳过重连直接提示

### 7. snapBuf 跨会话残留 → 新房间开局卡 ~1s
- 位置：`public/js/app/net.js:199-218`（单调门）；`app/modes.js:90`（backToMenu 只清 snapA/snapB 不清 snapBuf）
- 问题：新房间引擎 t 从 0 开始，旧缓冲末帧 t 若 <1000ms，单调门判定不满足"大幅变小"条件 → 开局 ~1s 快照被丢弃，画面"等待服务器数据"，且旧帧造成一次插值跳变
- 修复：backToMenu/setupNet 清 snapBuf/interpClock/_interpLast/interpGap

### 8. "公网联机服务器地址"功能缺失 → 手机端战绩同步死代码 + 文档误导
- 位置：`public/js/app/records.js:36-41`（serverBase() 读 `PPD.app.publicServerUrl`，全仓库从未赋值）；设置面板（index.html:256-294）无该字段；但 使用说明.txt:89,92 / README.md:62 / MainActivity.java:13 四处承诺该功能
- 影响：手机端跨设备战绩同步永远只存本地；按文档操作无入口
- 修复：二选一——设置面板加回服务器地址输入并持久化（wsUrl/records 复用）；或删除全部文档描述

---

## 🟡 中危

| # | 问题 | 位置 |
|---|---|---|
| 9 | 本地服务器实际 60Hz 全量广播：快照含 t 每 tick 变化 → 去重恒失效，CPU/带宽是 DO 的 3 倍（注释意图 20Hz 节流） | server.js:453-467 |
| 10 | 无背压处理：socket.write 返回值被忽略，慢客户端发送缓冲无界增长 | server.js:284-288,422-426,462-466 |
| 11 | 加入方重连用房主名 join（`names[0]`）→ 槽位被改名、生涯记录记错名 | net.js:119,283 |
| 12 | NetClient 内部重试 × 看门狗双层未协调 → 实际 ~6 次握手/20-30s（注释"最多 2 次"不符） | network.js:18-21 + net.js:28-32,43-69 |
| 13 | 房间码 4 位（~1M 组合）+ create/join 无速率限制 → 公网可枚举/刷房占 DO 存储 | server.js:272-280；room-core.js:186-197 |
| 14 | DO 全局单实例 `idFromName('global')` → 玩家量大时单点/卡顿 | _worker.js:19,26；src/index.js:18,25 |
| 15 | 版本号 4 处手工维护（package.json/state.js/Manifest/build.cmd）无单一来源 → 易漂移 | — |
| 16 | test:all 未聚合 records-test / seated-crowd-test / serve-path-online（均本地可跑） | package.json:9 |
| 17 | public 积压 12 个旧 APK（~42MB）随 Pages/ECS 全量发布；_headers 引用不存在的 v160/v161、PingPongDuel.apk 文件名误标 v163；download.html 页脚 1.6.5、fetch 带 `?v=166` 残留参数 | public/_headers；download.html:144,173 |
| 18 | /api/records DELETE 无鉴权（公网可任意删改战绩） | server.js:159-171；room.js:91-101 |
| 19 | APK 竖屏（AndroidManifest）vs PWA 横屏声明（manifest.webmanifest）冲突 | — |
| 20 | DO Alarm 100ms 常驻 → 有房间期间 DO 无法休眠（计费），且 accTime 0.5s 追赶上限使断流后游戏时间永久落后墙钟 | room.js:17,145-158；room-core.js:212 |

---

## 🟢 低危（小问题 / 文档）

- 僵尸连接 `alive` 字段只写不读；records 持久化错误全吞 + GET 同步读盘；无 Origin 校验（CSWSH）；房间码 50 次碰撞后回退 'ABCD' 不校验；静态文件 `startsWith(ROOT)` 前缀检查的理论旁路（建议改 path.relative 判 `..`）
- 退出联机后 heartbeat/watchdog 定时器空转不清理（R6）；lastEventKeys 未在 startOnlineGame/backToMenu 清理（M2）；rallyTouches 失焦残留（M3）；fireShot 70ms 脉冲吞并快速二次点击（M4）
- 手机端菜单固定 logo/按钮硬编码 150px 遮挡内容（style.css:259-263,368-382）；updateHitRangeLive 每帧写隐藏 DOM（hud.js:235-236）；setIntensity 跨档音乐从头重播（audio.js:268-277）
- state.js 死 DOM 引用（hitPaddleVal/tips/btnSmash/btnLeft 等，index.html 已无对应元素）
- sw.js 缓存名 ppd-v5 五版未递增（离线兜底旧文件）；build.cmd 不校验 assets 同步状态（git log e55b807 教训）
- 修改记录.md 待办过时（断线重连/拖尾/DO 2s 节流已实现未更新）；README/使用说明与代码不符（画质三档、帧率默认无上限、掌声无合成兜底、README 项目结构旧）；修改记录.md:4 日期陈旧（写 08-09 实为 08-13）
- tools/局域网放行.cmd 仅放行 8765，启动器自动换端口时不覆盖

---

## ✅ 已核实无问题

- 原型污染无注入路径；客户端伪造 side/输入越界被封死；瞄准输入被 clamp
- records 单进程并发写安全、原子写（tmp+rename）正确；广播期间修改 clients 无竞态
- 引擎镜像一致性：public ↔ src ↔ dist/ecs ↔ android/www ↔ %PKG% 全同（md5）
- audio.js 无 AudioContext/节点泄漏；autoplay 与真实音频兜底完善；APK 下载绕过 SW 缓存
- 脚本加载顺序正确；window/DOM 监听无重复绑定；摇杆/触控卡键无残留
- 页面前后台切换对插值时钟/看门狗的处理正确；engine resetMatch 清理充分

---

## 备注

- 修复优先级建议：先修 #1 #2 #3（安全）→ #4 #5 #7（联机会话生命周期）→ #6（本地重连）→ 其余
- 任何客户端代码修复走版本升级 + 全量分发流程（版本号 4 处 + APK 文件名 + 21 处分发 + ECS + zip + pages.dev + portfolio）
