# 乒乓对决 v1.3.0 · 合并说明（2026-08-05）

> 📦 **已归档**：本文档记录 v1.3.0 双分支合并过程，属历史存档，不再更新。当前版本与结构见 `README.md`，历次修改见 `修改记录.md`。

本目录由两份并行分支合并而成，完整保留两边的全部功能：

- **v1.1.0**（乒乓对决(5).zip）— 玩法手感分支
- **v1.2.0**（桌面\乒乓对决.zip）— 赛场氛围分支

## 合并策略

以 v1.2.0 为结构基座，把 v1.1.0 的玩法功能整体移植进去。

### 直接采用 v1.1.0 的文件（v1.1.0 是超集）
- 引擎核心：`public/js/engine/{rules,state,shots,strokes}.js`、`public/js/engine.js`、`src/engine.js`、`src/room-core.js`
- 服务端：`server.js`（含蹲/跑转发与 `a:[x,z]` 联机发球瞄准分支）
- 客户端：`public/js/ai.js`、`app/input.js`、`app/main.js`、`app/modes.js`
- UI：`public/index.html`、`public/css/style.css`（判定范围开关 + 左上角数值面板）
- 测试：`browser-smoke.js`、`serve-path-online.js`、`physics-boundary-test.js`、`engine-stress.js`、`ai-test.js`

### 直接采用 v1.2.0 的文件
- `public/js/audio.js`（真实掌声 WAV 加载 + 合成回退，完整取代 v1.1.0 合成欢呼）
- `public/audio/applause.wav`、`test/seated-crowd-test.js`、`tools/check-table-shadow.js`、`tools/wavinfo.js`、`tools/shots/game-crowd.png`

### 手工合并（取 v1.2.0 为基底 + 移植 v1.1.0 独有）
| 文件 | 合并内容 |
|---|---|
| `public/js/render.js` | 坐姿观众席/长椅/分阵营/球台与球员投影（B）+ 判定范围虚线绘制（A） |
| `public/js/characters.js` | 队色上衣（B）+ 蹲姿骨架渲染（A） |
| `public/js/app/state.js` | fan 分阵营状态（B）+ 发球瞄准状态、判定范围 UI 引用与 localStorage（A） |
| `public/js/app/hud.js` | triggerCheer(e.s) 分阵营接线（B）+ 发球瞄准提示、判定范围数值（A） |
| `public/js/app/render.js` | 低机位相机 y4.8 与 fan 视图（B）+ unprojectToTable/serveAimFromPointer/轨迹 sb 处理（A） |
| `public/js/app/loop.js` | fan 衰减（B）+ c/rn/a 输入帧字段（A） |
| `public/js/app/net.js` | triggerCheer(e.s)（B）+ serve-ready 瞄准恢复（A） |
| 文档 | 修改记录.md（B 时间线 + A 的 9 个玩法小节 + v1.3.0 合并记录）、README.md、使用说明.txt |
| `package.json` | version 1.3.0、描述合并、scripts 保留全部 |
| `tools/shots/menu.png` | 采用 v1.1.0 大图（含判定范围开关的菜单界面） |

## 合并后的完整功能

**玩法（v1.1.0）**
- 发球瞄准：鼠标实时跟随 + 手机两下点按；瞄准解不出合法发球时轨迹消失、拒绝发球
- Shift 跑步（×1.45）/ Ctrl 蹲下（×0.55，可接远台低球）
- 移动范围到网前（不上球桌），移动中可击球
- 球拍自动伸向球、真实接触判定（球 1.0m 球形 / 拍 0.18m）
- 推球抬高弧线 / 扣球更快（20.9 m/s+）
- 判定范围虚线开关 + 左上角数值显示

**氛围（v1.2.0）**
- 坐姿火柴人观众席 + 钢蓝色看台长椅，红/蓝分阵营欢呼摇头
- 真实掌声录音（applause.wav）+ 合成回退；赛事风格背景音乐（胶着/赛点提速）
- 队色上衣（P1 红 / P2 蓝）、球台/球员投影、低机位转播视角（y4.8）

**联机协议**：输入字段 `c/rn`（蹲/跑）+ `a:[x,z]`（发球瞄准落点）；快照字段 `cq/rn/sp/sb`；本地与公网（Cloudflare）双后端一致。

## 验证结果（全部通过）

- `node --check`：56 个 JS 文件全部通过
- `npm run test:all`：127 项断言全部通过（联机 11 / 引擎 13 / 物理边界 32 / 人机 5 / 浏览器冒烟 66）
- `npm run test:seated`：22 项坐姿观众骨架断言全部通过
- 一致性检查：38 项（引擎 src 镜像字节级一致、快照字段两端一致、无遗留引用）
- `node server.js` 启动：首页 + 21 个 JS + CSS + applause.wav 共 23 个资源全部 200
- 浏览器实机冒烟：菜单 → 人机对战 → 两次点击瞄准发球 → 进入对打 → 得分 → HUD 更新，全流程正常

## 未并入 / 说明

- `app.log`（v1.1.0 运行期产物，不并入）
- 待办（修改记录.md 六）：参考视频特效（拖尾/震屏/粒子）、联机断线自动重连、DO 存储增量写 —— 两份文件中的待办一致，保持原样
