# 运行时日志
app.log

# Node
node_modules/
npm-debug.log*

# 系统/编辑器
.DS_Store
Thumbs.db
*.swp

# 本地配置
.env

# wrangler 本地缓存
.wrangler/

# 本地通关记录数据（运行时生成，不入库）
records.json

# 安装包/分发产物（Node 打包时 + 安装时需排除）
dist/
%PKG%/

# 分发压缩包（*.zip 合成产物，不入库）
*.zip

# 签名密钥与 APK（密钥绝不上传；APK 由构建脚本生成）
android/release.keystore
*.keystore
*.jks

# 测试/运行日志输出
*.log
test-out*.txt
# local packaging staging (node runtime + old apks) - never commit
乒乓对决_安装包/
